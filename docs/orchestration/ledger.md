# Orchestration ledger — alexandria

## User decisions (verbatim)
- 2026-09-14: "Make our scale as resilient as possible... always provide the best quality Deep Researched report under 60 minutes... Whatever happens, we will always deliver... Burn tokens if you need to. I just want the best possible quality." (skill = the Alexandria skill)
- Delegation rule: orchestrator skill (/orchestrator).

## Standing orders
- Git: worktrees for edits; merge to main; no PRs; orchestrator owns git.
- Baseline: 503 passed, 3 pre-existing README subtest failures in tests/test_repository_contract.py (from commit 361298e). PARKED unless fixed by the README task.

## Task log
- USER (09-16, verbatim key lines): "Those removed parts … did you remove them or someone else? … I want all the useful mechanisms recovered … do only recovery, not reinventing … get rid of the laughable rules like living versus dead … you will not change anything that will impact the quality of the report." → R31. Answer given: the 09-15-04 removals were the orchestrator's (this session); yesterday's Deep-reads cut (b19e85e) was also the orchestrator's. Recovery source = d85add1 (+ the Deep reads section from b19e85e^ in SKILL.md). Contract docs/orchestration/09-15-05-restore.md; workers D (docs) and E (code), Cursor grok-medium; a first pair of workers with a reinventing contract was stopped and their worktrees reset.
- R31 delivered (main after d37022b, 938 pass, ruff clean). Recovered verbatim from d85add1: all validate_ledger finding families and their tests, gate-errors sections, review staleness findings, the default live spot-check at issue/render, review-start protocol pointers, references/*.md (incl. rewild-gate.md), READMEs ×4, INSTALL.md, SKILL.md (with the optional editorial block and Deep reads). Kept from 09-15-04 as burden removals (the only differences from d85add1): no refusals (init keeps/re-inits a workspace; --subject-status never required; fetch --id implies refresh; ledger merge skips forbidden keys; review finish/restores exit 0); the clock never decides (fetch never stops, fetch_document ignores the deadline, no RESERVE skip; the 4-min online cap stays); ledger/quantity WARN; closest-passage hint + Simplified/Traditional folding in the probe; lenient claim input; no dry-run step in SKILL.md; one status line per check and the HARD header "fix, or alx issue drops them"; remedies never name --deliver; fidelity/rewild auto-restores at issue; set-field remedies for ledger fields complete to "…then alx ledger merge ledger-patch.json" (bug fix). Deliberately NOT recovered, user informed: the "Verification note" paragraph alx used to insert into report.md/PDF (reader-facing tool text) — restorable on request. SKILL.md edits vs d85add1: `--archetype person` without status; claim add without dry-run and with the closest-passage sentence; editorial block "With ≥ 20 min remaining … in this order" / "Their absence never blocks delivery."; Deep reads section back.
- STANDING ORDER (R31): any step, command, check or document that leaves the prescribed path is named to the user before it goes; recovery from git history, never rewriting, when something has to come back.
- USER (09-16, verbatim key lines): "two parts … the real quality gates … described in the README in both Chinese and English … must have … recover them from git history, not rewrite, not reinvent … second, delete all the fat … useless burdens like the living versus dead verification … update the skill to WorkBuddy so I can do a final acceptance test." → FINAL INSPECTION (Workflow wf_6ddfada2-ce5: 4 auditors — README-EN map, README-ZH map, fat finder, live run — then 11 adversarial verifiers; 15 agents). 78 mechanisms confirmed PRESENT; 59 findings, 29 confirmed, 30 refuted.
- Confirmed and acted on. (1) SKILL.md had lost, in the September compression to alx command lines, the mechanism prose the README promises; recovered verbatim from 41aca26 (2026-07-28, "Harden Alexandria research and PDF pipeline"): non-negotiable standard, framing, archetype choice, depth/deepening rule, research design with parallel sub-agents and disjoint id ranges, freshness rule, ledger-entry field list (kind/confidence/limitations/supports/contradicts + importance: key), pre-draft checks, argument structure, drafting requirements with editorial modes, evidence-safe edit lock list and separate-reviewer rule, manual verification list, page-inspection checklist and fix-and-rerender, delivery criteria; Rewild + both reviews now required steps (333973b non-negotiable), never blocking. (2) README "PDF reopened to check text, links, fonts, pagination, overflow": `alx render` now calls validate_report.validate_pdf per PDF and prints `<template> PDF check:` lines (the July flow ran validate_report --pdf right after md_to_pdf; the standalone command needs a consistent receipt set and is brittle by hand). (3) Five defects fixed (wip/defects): issue prints delivery notes (stdout half of d85add1 recovered, no report paragraph); review finish lists each missing path once; ledger merge remedies name the real claims file; find prints each window once; status ends at "deliver …" after render.
- Refuted (kept as is): 30 findings, incl. every FAT proposal that touched a README mechanism (Deep reads, Rewild profiles, W-tier research rules, classification flags) and unreachable-string defects. Rejected by orchestrator against the verifier: FAT-01 (make fetch_document refuse past the deadline) — contradicts R29/R30 "the clock never decides" and the online phase is already bounded (sample_size × 10 s). Recorded as a ruling, not a bug.
- FINAL MAIN 3443489 + this docs commit: 943 pass, ruff clean, pushed; installs refreshed (~/.workbuddy, ~/.codex, ~/.cursor); full-path replay on the GLM fixture: init (kept) → ledger merge → check --fix → snapshot → review start/finish rewild + content (empty skeletons: warnings only) → issue (notes printed, receipts) → render (2 PDFs, PDF checks printed) → status "Next: deliver …". Burden removed and still removed (the only differences from history): living/deceased requirement, dry-run step, every refusal, clock-decided behaviour, quantity as HARD, `--deliver`/Class A wording, the PDF verification paragraph, 09-15-01/02/03 message fixes. Next: the user's own WorkBuddy / GLM-5.3-Flash acceptance run.
- USER (09-16 00:53, verbatim key lines): "I worry that some useful quality gate, some useful mechanisms were dropped … Any mechanisms, anything related to quality gates that meaningfully control the quality, uphold the value of the report must stay … not compare the README this time, you've already done that … only inspect git history in past three days … if you think they are useful … recover them … massive Cursor CLI … extra high … Grok 4.6 only, let them do the legwork, the cross examinations … you are the arbiter … after this run, let's have a pause … this is job part one … [part two, later] inspect the fat and delete the ridiculous useless rules, for example the living versus dead rules." → R32: history-only recovery pass (baseline 1edb929 = last commit before the September window, 173 commits); README comparison excluded; fat deletion deferred to part two.
- R32 dispatch: contract docs/orchestration/09-16-01-recovery-inspection.md; 15 read-only scouts (Cursor `cursor-grok-4.6-xhigh-fast --mode ask`, one per §A–§O: SKILL.md history, flow map, alx early/late, ledger+schemas, fidelity, content/report, rewild, pdf, references, tests, orchestration records, three per-commit slices); fleet state scratchpad/insp2; cross-verification round follows on the merged findings.
- R32 scouts delivered (15/15 exit 0; 141 raw findings, 219 burden sightings; merged scratchpad/insp3/merged.json). Orchestrator dedupe → 69 mechanisms (+3 orchestrator-found: untrusted-content rule, below-minimum-length rule, August non-negotiables 6–8). Verification: contract §V/§V.1, 10 batches × 3 lenses (presence / quality / recovery), Cursor xhigh ask-mode, two waves of 15 (scratchpad/verify).
- R32 verification delivered (30/30 verifiers exit 0; verdicts scratchpad/verify/verdicts.json). Arbiter: 36 of 69 mechanisms recovered (contract §W, waves 1–2), 33 rejected as user-ruled burden, cosmetic, or already present (orch-01 untrusted-content rule lives at SKILL.md:13 reworded). Workers: recA (alx.py), recB (validate_ledger + schemas), recC (content_gate + source_fidelity), recD (references + bundled Rewild SKILL.md), recE (rewild_gate + gate_severity + bundled checker); SKILL.md recovered by the orchestrator; pdf-06 (PDFKit fallback visibility) after recA merges.
- R32 delivered. Recovered 42 of the 69 verified mechanisms (contract §W; every code recovery keeps R28–R30: WARN, exit 0, no refusal, clock never decides). Code: content/check prints stale-excerpt and missing-citation lines; validate_report --min-sections 3 at issue; PDF text floor 5000 for every language; every unverified live sample prints; fetch 20 s / 3 attempts; claim add warns on omitted kind/importance and unknown fields; missing review is a WARN; status shows snapshot/review state; init echoes archetype/language; evidence-ledger schema required lists and minItems (brief, coverage rows, synthesis and children) as ledger/schema WARNs; analysis-without-reasoning WARN; content-review floors 20; rewild gate: checker reads full prose, quotes visible to direction checks, intra-sentence clause cuts, ASCII mask floor 4, exemption budget in run_check; bundled checker one-hit vocabulary cap; rewild/fidelity WARN lines uncapped; PDFKit failure printed. Docs: SKILL.md August sentences (non-negotiables 6–9, argument chain, drafting requirements, content-quality pointer, post-Rewild length rule, three censuses, compatibility pointer, receipts honesty, receipts-pass criterion); content-quality §13 rubric/censuses/named-human floor, content review no longer optional/clock-gated (that sentence deleted — named here); research-protocol two-exit derived discipline; evidence-recording checklist; cover-image honesty; rewild-gate second pass; bundled Rewild SKILL.md ×3 run the checker with dispositions. Rejected 27 as user-ruled burden (refusals, clock, R13 short segments, R25 harm/people, R26/C26 Han numerals, R11 snapshot/hash), cosmetic, or present (untrusted-content rule at SKILL.md:13). Adaptations named: E3 neutralizes citation-label commas before clause cuts; F keeps the fallback chain and prints instead of refusing; coverage rows require all eight documented fields (research-protocol.md names them).
- FINAL MAIN after this docs commit: 966 pass, ruff clean, pushed; installs refreshed; fixture replay exit 0 at every step (init → merge → claim add → check --fix → snapshot → reviews → issue → render → status). Part two (fat deletion) awaits the user's go.
- USER (09-16, verbatim key lines): "Some rules are hard rules, and some rules can be just warnings or reminders … examine all the warnings … if a mechanism can impact the overall quality by more than five points, it should be blocking … one or two points drop can be just a warning … I want the quality gates that really have a bigger impact to still be blocking." → R33: severity re-examination of all 56 families + 4 flow gates; contract §X; 15 Cursor xhigh raters (5 batches × lenses Q/I/E); orchestrator arbitrates the tier table; then implementation.
- R33 delivered (contract §X, §X.2). Ratings: 18 Cursor xhigh raters × 62 gates (scratchpad/sev/ratings.txt). Three tiers now: HARD-drop (fabrication; `issue` drops or restores, as before), BLOCKED / HARD-refuse (`issue` prints `=== BLOCKED (fix, then alx issue again) ===`, writes nothing, exits 1; `render` too): unsupported Arabic figure or date (`ledger/quantity`), Rewild direction/negation/causal reversal (`fidelity/semantic`), unresolved critical review finding, no snapshot at issue, report under two thirds of the length floor, unrestorable encoding, broken PDF at render (no file or < 500 extractable chars). WARN: everything else; `ledger/direction` (8-point impact) stays WARN because the lexical detector misfires often — first candidate for a better detector. Fixed on the way: `binding/leftover-prose` is now really deleted and `fidelity/cache-detached` really drops its claims. The clock never lifts a block (SKILL.md Clock). Fixture replay: `claim add` prints the C24 date finding, `check` lists it under HARD, `issue` blocks (exit 1, no receipts), `status` says "blocked: 1", `claim drop C24 --apply` then issue → render → deliver.
- FINAL MAIN after this docs commit: 979 pass, ruff clean, pushed; installs refreshed. Part two (fat deletion) still awaits the user's go.
- USER (09-16, verbatim key lines): "Drop the blocking with: a number or date … the humanizer flipping a sentence's meaning … a critical finding from the content review … these go back to just a warning … a reminder to the agent … 'you haven't fixed this one, please fix it'. Skipping the humanizer … a report under two-thirds of the required length … file cannot be read … broken PDF: only the last four are blocking." Also: "it's not your job to delete the fat" (part two closed).
- R33 amended and delivered: BLOCKED = no snapshot at issue, report under two thirds of the length floor, unrestorable encoding, broken PDF at render. `ledger/quantity`, `fidelity/semantic`, `content/critical-finding` = WARN, repeated by `issue` under `=== REMINDERS (not fixed yet; warnings, never block) ===`; docs and pins restated; 979 pass, ruff clean; fixture replay: C24 date printed as a reminder, issue exit 0, render, deliver.
- USER (09-16, verbatim key lines): "Yes, I want you to delete the fat. Remember, everything you delete should not impact quality … Quality always first … only use [Cursor] as your sub-agents. Same X high." → R34 (part two). Contract docs/orchestration/09-16-02-fat-inspection.md: 13 Cursor xhigh scouts (181 candidates → 127 deduped) → 39 Cursor xhigh verifiers (Q quality / C cost / D dependency) → arbiter: 78 fat, 49 kept. Kill list presented in five groups; USER: "A yes … only delete 'a' for the rest unless you can convince me with three sentences per category" → three sentences each → USER: "delete B, E". C and D stay.
- R34 delivered (main 960687a): A = leftovers of user-deleted rules (living/dead flag+fields+schema+person.md trigger; BEHIND SCHEDULE; DEGRADE nudge; deadline plumbing; Class F phrase/reason; --deliver; --dry-run; review restore; show; snapshot --iter; fetch --refresh; style waivers + fidelity notes; managed-runtime ceremony in SKILL/INSTALL/status/gate-errors; check/fetch/merge exit 0; Verification-note doc wording — the strip in issue stays). B = duplicate checks (URL-membership loop, "Report location is missing", second date-line WARN, ledger re-emission default, short-field reprint, PDF size print, single WARNING prefix, one PDF text pass, per-render contrast walk, STATUS clock repeat, second offline probe pass, discarded context-changed at add, stale finish hash suffix). E = PDFKit skipped off macOS. Suite 945, ruff clean, fixture replay clean (139 WARN, check 1.0 s). Installs refreshed.
- USER (09-16 afternoon, WorkBuddy run 2026-09-16-15-20-33, 61 min, 2 PDFs delivered; bug report + session log attached; verbatim key lines): "48 hours … constant fix, test, fix, test … No more ceremony. No more useless, over-strict rules. If there's a hard rejection that doesn't impact the quality, drop it … I only care about the quality of the report … make it as light as possible … efficient and non-blocking … super resilient, works with everything, never stops … use sub-agents as much as possible, especially Cursor … prioritize Grok … Fable does orchestration only … do NOT run end-to-end acceptance tests, I test myself with WorkBuddy + GLM Flash … 30 minutes is the target, without hurting quality … judge every mechanism: does it improve quality or no? … read the log and fix the issues." → R35, contract docs/orchestration/09-16-03-final-look.md; inventory docs/orchestration/09-16-03-gate-inventory.md.
- R35 analysis (2 Cursor xhigh scouts on the log + code): 20 of 61 min were schema trial-and-error (ledger merge 6 min/17 calls chasing `required` fields nothing reads; reviews 9 min/35 tools: skeleton text "every field below, nothing else" → agent rewrote the note without metadata, minLength 20 → 28 "too short" lines, content gate re-validated the LEDGER schema as HARD and refused the receipt, hash bindings refused the receipt after a one-paragraph edit); first `alx render` 11.3 min (profiled by W4); `PDF has 9 pages; minimum is 10` caused the 7-min redo; live re-read at issue printed `fidelity/context-changed` notes every time and changed nothing. Rulings R35.1–R35.11: notes never validated; merge prints `merged:` only; remaining schema lines print every violation; review notes = attestations (metadata filled by alx, no jsonschema, no prose floors, quality lines all at once); staleness = reminder, issue stamps hashes; no ledger schema in content gate; no page-count minimum at render; live re-read opt-in `issue --live`; clock 30 min / polish cutoff 8 min. Workers W1 (ledger/schema/render/issue/budget), W2 (reviews/content_gate/rewild note), W3 (SKILL.md/READMEs/gate-errors + pins), W4 (render profile, read-only) — all Cursor grok, worktrees $S/wt/w{1,2,3}-*.
- R35 delivered (main after this docs commit). Workers W1–W6 all Cursor grok (xhigh; W3 medium), each in its own worktree, merged serially, suite green at every merge. Code: `ledger merge` prints `merged:` only and runs no evaluators; evidence-ledger schema constrains only `sources`/`claims` (notes unconstrained, readers type-guarded); `ledger/schema` + `ledger/claim-input` print every violation; review notes = attestations (start writes metadata, finish/check/issue fill it, no jsonschema, no prose floors, quality lines all at once as WARN); staleness = reminder, issue stamps hashes unconditionally, no hash/path receipt refusals; content gate no longer re-validates the ledger schema; rewild note loader fills bindings, reason-length rule gone; render has no 10-page minimum; live re-read opt-in (`issue --live`; `--offline` accepted, ignored); `--budget-minutes` 30; contact sheets PDFium first with PDFKit/Poppler fallbacks, renderer subprocesses stdin=DEVNULL + new session (R35.12); stale source-fidelity receipt = absent, one WARN (R35.13). Docs: SKILL.md 30 min / polish cutoff 8 min / notes stored as given / reviews edited in place / issue --live / no page count; READMEs ×4; gate-errors; rewild-gate.md Blind-review note; pdf-production. Tests: 945 pass (baseline 945: −17 deleted pins of deleted behaviour, +17 added/restated), ruff clean. Replay on a copy of the 09-16 workspace: merge (arbitrary shape) → `merged:`; review finish on a metadata-less note → finished + metadata; paragraph appended → check asks no re-review → `issue` 1.4 s `receipts written: content, rewild` → `render` 2.8 s, no page-count line. Render profile (W4): 3.6 s warm on this machine; the 674 s first render in the field run finished its work at +13 s (worklog) and the harness call stayed open — cause not reproduced (W5: a fresh-module-cache swift run leaves no children); PDFium-first removes swift from the default path regardless. NOT changed (judged quality-bearing or already adjudicated R28–R34): fabrication HARDs, the four BLOCKED gates, verbatim-extract check at claim add, length floors, content/rewild reviews themselves, WARN families of the ledger evaluators at `check`. User runs the acceptance test on WorkBuddy + GLM Flash himself.
- USER (09-16 evening): WorkBuddy verification pasted — Bugs 1/3/4, render chain, clock confirmed; objections: merge accepts garbage silently ("no defence"), wrap-up rule only in SKILL.md. "keep fixing." → R35.14 (merge warns, stored as given, only for fields `check` reads: unknown coverage status, claim-id lists not lists, synthesis items not objects; nothing for editorial_mode/outcome/brief/people — no consumer) + R35.15 (footer suffix "stop polishing: alx issue, then alx render" at remaining 1–8, "time is up …" at 0). W7 (Cursor xhigh) delivered; SKILL.md §2 + Clock and gate-errors ledger/coverage + ledger/synthesis restated by the orchestrator. Main 949 pass, ruff clean, pushed; installs refreshed.
- USER (09-16 night, WorkBuddy run 2026-09-16-18-27-10, GLM-5.3-Flash, .base b034256; verbatim key lines): "a clean run … major issues are gone … much faster, within 30 minutes … still a few frictions … polish … prioritize Cursor CLI, the Grok model." Friction report alexandria-issues.md: (1) fetch stores the redirected final URL, the report cites the requested one → `binding/link-not-in-ledger` HARD, three fix rounds; (2) `claim add` FAIL on an extract typed from memory, closest passage saved it, batch rhythm missing; (3) `ledger merge` accepted a wrong-shape `synthesis.adversarial_tests` silently, found at `check`; (4) review JSON filled by a 30-line throwaway script — templates carry no per-field hints. → R36; scouts on log + code first (fleet6), then W8.
- R36 delivered (W8, Cursor xhigh; contract §"Second WorkBuddy run", recon docs/orchestration/09-16-04-recon.md). Log facts: 22.6 min init→delivery; friction 1 was a hand-copied `.com.cn` variant of a fetched `.com` URL (not a redirect), three rounds = the agent's broken sed; friction 3 did not occur (merge printed the R35.14 WARN and the agent fixed it); friction 4: filler script on the wrong path crashed and `review finish` closed two blank skeletons. Code: `check --fix` rewrites same-page link variants (www./domain suffix, same path ≥ 2 segments, same registrable label) and prints `link rewritten: … (S<n>)`; `binding/link-not-in-ledger` Fix says cite the nearest ledger URL, never fetch the wrong one; `claim add` FAIL = two lines (`extract not found verbatim in S<n> (searched: …)` + `S<n> extract_or_location: "<closest>"`), summary says paste and re-add in one file; `review start` prefills section_reviews from the H2 headings and prints `Note to fill: <abs path>` first, `Report copy for the blind read:` second; `review finish` on a blank skeleton prints one `nothing filled yet` line. Docs: SKILL.md §3/§5/§6, gate-errors. 957 pass, ruff clean. Replay on a copy of the 18-27-10 workspace: all four behaviours confirmed (8 sections prefilled incl. 资料来源; FAIL block; `link rewritten:` then HARD 0; blank-finish line). Pushed; installs refreshed.

## 2026-09-16 — R37 third run (20-52-04) + R38 remedy truth

User (verbatim): "Okay, here's another run by work buddy, and there are still problems. Please read all the four attached documentations and logs, analyze all the problems, and fix them. Remember, this has been going on for fucking two days. You need to fucking answer the question: why are there still fucking problems? You can check the git history. It's been over 48 hours. How many submits? When can we have a good scale that runs smoothly without these kind of problems? What the fuck?" + "/orchestrator Prioritize cursor CLI usage".

Answer recorded: 209 commits in 48 h. Cause: fixes were reactive per run; message families were never audited for "true as worded + remedy clears the line". W10 audit: 80 families, remedy works 22 / partial 25 / never 33 — `adopt` table and `_ref` catch-all. R38 fixes the remedies.

Delivered R37 (W9a alx/validate_*, W9b rewild/fold/docs; both Cursor grok xhigh): contract §R37. Suite 972, ruff clean. Replay on the run copy: excerpt-missing 19→0, coverage 12→1 (one true line), synthesis 12→1, reference 7→1, no "incomplete"/"skipped", C16 two-record input accepted, review finish clears stale. Known: workspaces whose claims were added before this update show `fidelity/context-changed` once (hash table changed); new workspaces unaffected; R38.9 makes the family live-only. B6 ruled legitimate (figures not in sources) — message form fixed only.

Delivered R38 (W11a validate_ledger/content_gate producers, W11b alx adopt/review/wording; Cursor grok xhigh): every printed Fix is now a literal command or edit that clears its line; adopt never replaces a producer Fix; `review start` refuses to overwrite a filled note (`--iter` moves it aside); `tests/test_remedy_truth.py` bans `--iter` on quality/stale lines, flagless `source set`, nonexistent patch-file names, `--paragraph 1`. Orchestrator follow-up: "remove X from <field>" Fixes name the claim file (R20 pin kept). Suite 986, ruff clean. Replay on the run copy: every remaining WARN line true with a working remedy.
- 0917 field run (WorkBuddy, Hy4 preview; 蒋介石日记, zh-CN): DELIVERED in 20 min, 0 hard, 2 PDFs. Model's bug report triaged: BUG-1 (empty-shell HTTP-200 pages S17/S20/S23 enter the ledger) REAL; BUG-2 (last page exempt from sparse check) FALSE — `alx render` printed the page-10 sparse_page WARNING, last-page exemption = bibliography by design, no change; review JSON mojibake via heredoc + `--dir` redundancy = doc/UX items. Task W12 (Cursor grok-medium, wt $S/wt/w12 branch wip/w12, prompt $S/prompts/w12.prompt): A fetch EMPTY gate (<200 chars) · B `check` uncited-sources line · C `alx review set kind PATH=VALUE` · D SKILL.md `--dir` optional note. Baseline main f3ef7b7: 990 pass. Then: opus-medium task review → merge → suite → push → refresh ~/.workbuddy|~/.codex|~/.cursor skills/alexandria.
- 0917 W12 DONE (Cursor grok-medium sid 5cda273f…; opus-medium review ✅ spec / Approved, 6 Minor: #1 KeyError on id-less source + #2 EMPTY wording/http None FIXED by orchestrator; #3 #5 #6 FIXED in W13 (d4dba51: CJK floor 80 chars, uncited test asserts no-cache + order, example rationale real text); #4 DROPPED (fixture padding harmless: the Traditional sentence still carries the keyword)). Merged main f5d3ccc: 994 pass, ruff clean, pushed; installs refreshed (.base = f5d3ccc). Field copy: `uncited sources (13): S20 (0 chars) S17 (12 chars) S23 (21 chars) …`. wt/w12 + wip/w12 removed. USER via WorkBuddy: "parked" is not a state — fix or drop; applied.
- 0917 three parallel WorkBuddy runs (01-37-01 Hy4, 01-37-17, 01-36-37; 蒋介石日记 zh-CN, all delivered). USER: "look at the bug report and the session logs … verify them and fix them … prioritize Grok inside Cursor". Contract docs/orchestration/09-17-02-run4-frictions.md. Triage: run 01-37-01 BUG-1..9 all reproduce (BUG-1 "silent" overstated: check printed the coverage WARN ×8, no doc listed the enum); run 01-37-17: 1 of 3 real (review set JSON values undocumented; cycle-HARD and finish-feedback claims false); run 01-36-37: 4 of 9 real (TLS fallback, merge crash on list synthesis, no language-mix check, install_runtime encoding; kind-validation / finish-idempotency / quantity-on-unreachable / sparse-page claims false; guidance localization declined — no quality impact).
- W14+W15+W16 (one Cursor grok-medium session 3c4dd131, wt w14, branch wip/w14; opus-medium task review REJECTED once — fallback inert behind source_fidelity's https-only transport, fake `fidelity_checks.quotes_unchanged` key, `findings.0` example failing on an empty list — all fixed; orchestrator finished the transport thread: `allow_plaintext_http` through validate_public_http_url/default_fetcher, live probe old.counsellor.gov.cn + dangshi.people.com.cn both `fetched over plain http (https failed: tls)`). Delivered: coverage status enum in SKILL/research-protocol/gate-errors; `reported_claim` wire value in docs + `claim add` normalizes kind (WARN after acceptance); `findings[N]` 0-based; resolved/rejected semantics; fetch http fallback (http url → https fails → http; https url + tls → http) storing `plain_http`, `ledger/https` skips it; SKILL.md init Read-before-Write note, full-command rule, `review set` real examples + JSON values + `.N.` index (appends at len); `alx claim schema`; excerpt re-derive on plain check/issue (field replay: 4 false excerpt-missing → 0); `claim add` invalid-JSON one-liner; `ledger merge` type guard (WARN, not merged, no crash; readers tolerate legacy list); `integrity/language-mix` WARN (zh, ≥4 ASCII words, markers/links masked, ≤5 lines); install_runtime utf-8. Orchestrator: restored the three SKILL.md paragraphs Grok had paraphrased (standing order), word pin 3000→3200. Suite 1007, ruff clean.
- 0917 re-checks by the field models. DeepSeek (run 01-37-01): 9/9 confirmed; 3 new on the plain-http fallback, all real → W17 (Cursor grok-medium, wt w17): `plain_http` on the ledger schema; refresh (`fetch --id`) and the live sampler pass `allow_plaintext_http` for fallback sources; both-failed UNREACHABLE line names both classes. Live: `fetch --id S1` re-fetches old.counsellor.gov.cn over http, schema WARN gone. Suite 1010. GLM (01-37-17): problem 2 ✅; "check hides the cycle" false (prints on the ledger/reference line, verified by injection); empty-finish = R35 by design. Hy4 (01-36-37): 4 ✅; finish-idempotency guard refused (re-finish is the prescribed re-attest path, and a refusal); quantity B6 stands; localization declined; encoding fix present (wrapped line). Task review skipped for W17: 12 code lines, read + live-tested by the orchestrator.
- 0917 DeepSeek round 2 (bug-report.md §8): W17 3/3 confirmed; 5 new, all real → W18 (Cursor grok-medium, wt w18): `check --fix` no longer deletes prose after the Sources list (block bounded by the next heading; non-list lines kept); WARN fold = one line per distinct message, 5 per family then `+N more (--verbose)` (R29 amended, named to the user — the old fold hid the circular-support line; my earlier "false" on GLM's claim was wrong); `accessed`/`verified_at` from the LOCAL date of `fetched_at`; `binding/claim-marker` WARN for bare `[C99]`/`[S99]` not in the ledger; init strips leading `#` from the brief title. Opus-medium task review REJECTED once (Major: a paragraph between the Sources heading and the list made `--fix` emit the list twice — Grok's test only checked the paragraph survived; 8 minors) → W19 (resume) fixed all 9. Orchestrator re-ran five tail shapes: one list each, every paragraph kept; DeepSeek's D1/D2 repros pass. Suite 1021, ruff clean.
