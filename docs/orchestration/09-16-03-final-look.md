# 09-16-03 — Final look: the 2026-09-16 WorkBuddy run (R35)

Base: main fc6e896. Suite baseline: 945 passed, ruff clean.
Inputs: `/Users/wallny/WorkBuddy/2026-09-16-15-20-33/alexandria-bug-report.md` (user), `docs/orchestration/09-16-03-gate-inventory.md` (scout inventory, file:line for every mechanism named below — trust it, verify with grep before editing).

## Rulings (binding; the doc wins over any worker prompt — flag conflicts in your report)

R35.1 Notes are not gated. `brief`, `people`, `coverage`, `synthesis`, `unresolved_questions` are the agent's own planning notes. `alx` stores whatever JSON it is given and never validates their shape. The only fields any code reads are `coverage[].claim_ids`, `coverage[].status`, `coverage[].priority`, `coverage[].area`, `coverage[].gap_impact`, `people[].name/aliases/person_id`, `synthesis.central_judgment_claim_ids`, `synthesis.counterevidence_claim_ids`, `synthesis.*[].claim_ids` / `rationale_claim_ids`, `brief.report_language`; every reader tolerates a missing or differently-typed field (isinstance guard → skip, no message).
R35.2 `ledger merge` prints: `merged: <top-level keys>`, the existing ignored-keys line(s), the existing `N claim ids not in the ledger` line. Nothing else. It does not run the ledger evaluators (`check` does). Exit 0 (bad JSON stays exit 1 with the existing message).
R35.3 Every jsonschema-derived line that remains (`ledger/claim-input`; `ledger/schema` now only for `sources`/`claims` items) prints EVERY violation as `<json path>: <message>` — no first-plus-count, no cap, in compact and verbose mode alike. jsonschema's own messages already list enum candidates.
R35.4 Review notes are attestations, not exams. `review start <kind>` writes the note with all metadata filled and prints "Edit reviews/<kind>.json in place: fill the fields below, leave the rest as written." `review finish <kind>`, `check`, `issue` (content_gate.run_check / run_content_gate, rewild_gate._load_review_note / run_gate) never reject a note over metadata: each one FILLS OR OVERWRITES `schema_version`, `status` (→ `completed` at finish), `report_path`, `ledger_path`, `report_sha256`, `ledger_sha256`, `source_sha256`, `report_lang`, `profile`, `reviewed_at` (ISO date-time, now), `reviewer_mode` (default `fresh_eyes`) from the workspace before reading the judgment fields. No jsonschema validation of a review note anywhere; no prose length floors anywhere (delete the `prose_floor` machinery and every `is too short` path). The schema files stay as shape documentation: drop `additionalProperties: false`, every `minLength` > 1, every `const` on metadata (`status`, `schema_version`, `disposition`, `visual_assets[].disposition`), and the `allOf` region/fidelity→resolved rule; keep `enum`s (documentation), keep `required` lists (documentation only — nothing enforces them).
R35.5 Quality lines from a review, printed ALL AT ONCE by `review finish` as `WARN review/<kind>: …`, repeated by `check`/`issue` under the existing WARN families, never a receipt refusal: content score < 4 (one line per dimension, `<dim> scored N (< 4): revise, then alx review finish content`); checks false (one line listing the false keys); `section_reviews` empty; `completion_note` empty; critical finding with disposition ≠ fixed (existing `content/critical-finding`); major finding rejected; major accepted_limitation whose disclosure excerpt is not in the report (existing `content/disclosure`); rewild `fidelity_checks` not all true (one line listing the false keys); a rewild finding with an unknown category/disposition or a `region`/`fidelity` finding not `resolved` (one line naming the index; the finding is skipped, the gate proceeds). Delete the rewild `reason` length rule (schema minLength 5 and code ≥ 10).
R35.6 Staleness is a reminder. `issue` stamps the current hashes into both notes unconditionally (drop the `freshness_findings` guard in `_receipt_phase`). `review/content-stale` and `review/rewild` staleness lines become one WARN each: `N paragraph(s) changed since the <kind> review — re-read them if the change was substantive; alx review finish <kind> re-stamps.` Delete the receipt-refusing sha/path bindings: content_gate.run_content_gate lines "Content review belongs to a different …" / "does not match the …", and the rewild note `bindings` mismatch return in `_load_review_note` (use the workspace values instead). Receipts still record the hashes they were issued against.
R35.7 The evidence ledger is never re-validated inside the content gate: delete `_schema_errors(ledger, EVIDENCE_LEDGER_SCHEMA, …)` in content_gate.run_content_gate (and in run_check if present). `validate_references` stays.
R35.8 PDF page count is not a gate: `alx render` calls `validate_report.validate_pdf(…)` without `min_pages` (or `min_pages=0`); the message `PDF has N pages; minimum is M.` never prints from `alx render`. Text floor 5000, links ≥ 1, fonts, A4, overflow, blank pages stay.
R35.9 The live re-read at `issue` is opt-in: `alx issue --live` runs it; default is offline (today's `--offline` behaviour). `render`'s automatic issue is offline. `fidelity/context-changed`, `fidelity/unreachable`, `fidelity/undecodable` therefore print only under `--live`. Keep the `--offline` flag as a no-op alias for one release (accepted, ignored).
R35.10 Clock: `--budget-minutes` default 30. Docs: "within about 30 min"; "30 min wall-clock from `init`"; "At remaining ≤ 8 min stop polishing"; READMEs likewise (60→30, 15→8 in all four).
R35.11 Nothing else changes. No new features, no renames, no refactors, no re-wording of quality prose beyond the sentences named in §W3. Tests that pin a deleted behaviour are deleted; tests that pin a changed line are restated to the new text (repo convention: "pins restated"). New tests: one per ruling, minimal (a merge with an arbitrary-shape synthesis prints only `merged:`; claim-input violations all print; finish on a note lacking every metadata field finishes and the file gains the metadata; issue stamps hashes when the report changed after the review; render output has no `minimum is` line; `issue` default runs no network — patch/monkeypatch the live sampler and assert it is not called).

## Workers

Every worker: worktree named below; full suite `/Users/wallny/Developer/Skills/alexandria/.venv/bin/python -m pytest -q -p no:cacheprovider tests` green before reporting; `/Users/wallny/Developer/Skills/alexandria/.venv/bin/python -m ruff check scripts tests` clean if ruff is installed. Report in chat: files touched, tests deleted (names) / restated / added, final pass count, every contract conflict.

### W1 — ledger merge, schema, claim-input printing, render min_pages, issue --live, budget  (worktree `wt/w1-ledger`)
Files: `scripts/alx.py` (merge ~2403-2485; issue live default ~3953-3960 + argparse ~4591; render→issue call ~4286; render validate_pdf ~4341; `--budget-minutes` ~4493), `scripts/validate_ledger.py` (schema wrapper ~3082-3088, 3459-3474; readers of coverage/synthesis/people — tolerance per R35.1), `scripts/gate_severity.py` (compact renderer ~71-139: schema-derived families print all members), `references/evidence-ledger.schema.json` (R35.1: strip `required`/`minLength`/`minItems`/`enum`/`allOf`/`if`/`then`/`additionalProperties:false` from the `brief`, `people`, `coverage`, `synthesis`, `unresolved_questions` subtrees and from the top-level `required` — keep top-level required = `schema_version, subject, research_question, report_date, sources, claims`; `sources`/`claims` item schemas untouched), tests. Rulings: R35.1, R35.2, R35.3, R35.8, R35.9, R35.10 (code default only), R35.11. Do NOT touch content_gate.py, rewild_gate.py, review code in alx.py (3080-3180, 3660-3780, 4060-4110), or any .md.

### W2 — review notes, content gate, rewild note  (worktree `wt/w2-reviews`)
Files: `scripts/alx.py` (review completeness ~3080-3180; start/finish ~3660-3780; `_receipt_phase` ~4060-4110 stamping guard), `scripts/content_gate.py`, `scripts/rewild_gate.py` (`_load_review_note` ~265-330 and its caller), `references/content-review.schema.json`, `references/rewild-review.schema.json`, `references/rewild-gate.md` (append a "Blind-review note" section: `alx review start rewild` writes it with metadata; the agent fills `fidelity_checks` (four booleans) and `findings[]` (`category style|region|fidelity`, `finding`, `disposition resolved|rejected`, `reason`); minimal copy-paste example of the judgment fields; `alx review finish rewild`), tests. Rulings: R35.4, R35.5, R35.6, R35.7, R35.11. Do NOT touch merge code, render code, argparse defaults, validate_ledger.py, gate_severity.py, evidence-ledger.schema.json, SKILL.md, READMEs, gate-errors.md.

### W3 — docs and their pins  (worktree `wt/w3-docs`)
Files: `SKILL.md`, `README.md`, `README.en.md`, `README.zh-CN.md`, `README.zh-HK.md`, `references/gate-errors.md`, tests that pin these documents (tests/test_repository_contract.py, tests/test_skill*.py or wherever `grep -rn "60 min\|remaining ≤ 15\|never read scripts" tests` hits). Rulings: R35.10 and the exact edits below; R35.11. Do NOT touch scripts/ or schema files.
SKILL.md exact edits (find each quoted fragment; replace as stated; nothing else in the file changes):
- line 8 `within about 60 min` → `within about 30 min`.
- Depth: `**Production target:** roughly ten or more finished PDF pages, depending on language, tables, and layout.` → `**Typical result:** about ten PDF pages; the word/character range above is the only length rule.`
- §2: `(`brief`/`people`/`coverage`; the schema is `references/evidence-ledger.schema.json`, `alx` reports what is malformed and never blocks on it)` → `(`brief`/`people`/`coverage`/`synthesis`, in whatever JSON shape you find useful; `alx` stores it as given)`.
- §3 last bullet: `record the central judgment, counterevidence, adversarial tests and limitations in the ledger `synthesis` (a `ledger merge` patch).` → `record the central judgment, counterevidence, adversarial tests and limitations in the ledger `synthesis` (a `ledger merge` patch); `check` reads `central_judgment_claim_ids` and `counterevidence_claim_ids`, the rest is free-form.`
- §6 step 7: `each print a skeleton into `reviews/`; the skeleton lists every field; fill only those (the content review scores eight dimensions 1–5 with a rationale each and reviews every section's structure); never read scripts/ or references/*.schema.json; `review finish` names anything still missing and never blocks.` → `each write the note into `reviews/` with its metadata filled in; edit it in place and fill only the judgment fields it lists (the content review scores eight dimensions 1–5 with a rationale each and reviews every section's structure); `review finish` prints what is still empty or scored below 4, and finishes anyway.`
- §7 step 8: `re-reads a sample of the cited pages live (central-judgment citations first), drops whatever is still hard,` → `drops whatever is still hard,` and append to that sentence's end (after `exits 1 — fix and run it again.`): ` `issue --live` also re-reads a sample of the cited pages.`
- §7: `(text, links, fonts, page count and size, bookmarks, near-blank pages, header collisions)` → `(text, links, fonts, page size, bookmarks, near-blank pages, header collisions)`.
- Clock: `60 min wall-clock from `init`` → `30 min wall-clock from `init``; `At remaining ≤ 15 min stop polishing` → `At remaining ≤ 8 min stop polishing`.
READMEs line 13 (all four): 60→30 minutes, 15→8 minutes, same sentence otherwise.
gate-errors.md: `ledger/schema` section → applies to `sources`/`claims` items only and prints every violation; `review/content-stale` + `review/rewild` → reminder wording of R35.6; `fidelity/context-changed`, `fidelity/unreachable`, `fidelity/undecodable` → "printed only under `alx issue --live`"; "Removed families" list += "review-note metadata bindings (hash/path/profile/schema_version) — `issue` stamps them", "content-review prose floors", "evidence-ledger schema on `brief`/`people`/`coverage`/`synthesis`", "PDF page-count minimum at `alx render`", "live re-read by default at `issue`".

### W4 — render profile (read-only on the repo; scratch copy of the user's workspace)
Copy `/Users/wallny/WorkBuddy/2026-09-16-15-20-33/` to `<scratch>/ws-profile/` (never touch the original). With `ALEXANDRIA_PYTHON=$HOME/.alexandria/runtime/bin/python` and `SKILL_ROOT=/Users/wallny/Developer/Skills/alexandria`, time `alx --dir <copy> render` end to end (`/usr/bin/time -l`) and then instrument by timing each stage separately in a throwaway Python driver (import the modules from scripts/): `cmd_issue` if triggered, `md_to_pdf` per template, `render_pdf_pages.render_pages` per template (which backend was selected, whether PDFKit/swift was compiled or timed out first), `validate_report.validate_pdf` per template (pdf_quality page rendering), anything network-bound. Report a table stage | seconds | notes, the total, and the single largest cost with file:line. The 2026-09-16 run's first `alx render` took 11.3 min and the second under 2 min — explain the difference if you can reproduce it (cold caches? swift compile? font subsetting? a network fetch?). Do not edit the repo.

## W4 result → R35.12 and W5

W4 (render profile): on this machine `alx render` takes 3.6 s end to end (WeasyPrint 0.7 s per template, PDFKit contact sheet 0.5 s, pdf_quality 0.3 s). In the 2026-09-16 session the worklog shows render finished at +13 s, yet the harness Bash call (`alx render 2>&1 | tail -40`) returned after 674 s: a process spawned during the first-of-day `swift render_pdfkit_pages.swift` launch (clang module cache built at that instant) outlived alx and held the pipe open. Not reproducible warm.

R35.12 Contact sheets never depend on a subprocess by default. `render_pdf_pages.AUTO_FALLBACK` order becomes `("pdfium", "pdfkit", "poppler")` on darwin and `("pdfium", "poppler")` elsewhere (PDFium = pypdfium2 in the runtime, in-process, no compile). Every renderer subprocess (`render_with_pdfkit`, `_run_renderer`) runs with `stdin=subprocess.DEVNULL` and `start_new_session=True` so nothing it spawns can inherit the caller's terminal, session, or pipes. Explicit `--backend pdfkit` still works. The alx.py line `contact sheet: PDFKit failed (…); rendered with <backend>` is unchanged (it now fires only if PDFium failed first and PDFKit failed too).

### W5 — contact-sheet backend order and subprocess hygiene  (worktree `wt/w5-render`)
Files: `scripts/render_pdf_pages.py` only in scripts/; tests pinning the darwin chain order (tests/test_alx.py `TestF1`/pdf-06 class around line 4056 and any `AUTO_FALLBACK`/"starts at PDFium"/"PDFKit first" pin — `grep -rn "pdfkit\|PDFKit\|AUTO_FALLBACK" tests`), docs that state PDFKit-first on macOS (`grep -rn "PDFKit" references/pdf-production.md references/gate-errors.md SKILL.md INSTALL.md README*.md` — restate to "PDFium first; PDFKit, Poppler as fallbacks; `--backend pdfkit` on request"). Rulings: R35.12, R35.11. Do NOT touch alx.py, md_to_pdf.py, pdf_quality.py, validate_report.py. Add one test: darwin auto chain tries pdfium before pdfkit (patch both, assert call order) and every subprocess.run call in the module passes `stdin=subprocess.DEVNULL` and `start_new_session=True`. Optional, report only (≤ 2 min, kill after): with a fresh `-module-cache-path` under scratch, run `swift scripts/render_pdfkit_pages.swift <any pdf> <tmpdir> 96` once and list any process still alive 5 s after it exits (`ps -o pid,ppid,command`), to confirm or refute the lingering-child theory.

## Replay after the merge (main 439b2e3) → R35.13 and W6

CLI replay on a copy of the 2026-09-16 workspace: `ledger merge` with an arbitrary-shape patch prints `merged: synthesis, coverage, brief`; `review start content` prints "Edit reviews/content.json in place …"; `review finish content` on a note stripped of every metadata field finishes and the file gains the metadata; after appending a paragraph, `check --fix` asks for no re-review; `render` prints no page-count line and uses PDFium. One defect: `issue` printed `note: content receipt not issued: Source-fidelity receipt does not match the ledger` — the workspace carried a source-fidelity receipt from an earlier live run, the ledger had changed since, and the content gate treats that stale receipt as HARD. With the live re-read now opt-in, a stale fidelity receipt is ordinary. Also `=== REMINDERS (not fixed yet; warnings, never block) ===` printed with nothing under it.

R35.13 A source-fidelity receipt that does not match the current ledger or report is treated exactly like a missing one: the content gate proceeds with `fidelity_skipped`, prints one WARN `source-fidelity receipt predates the ledger; skipped (alx issue --live refreshes it)`, and never refuses the content receipt over it. Every other reader of `receipts/source-fidelity.json` in `alx issue`/`render` (the existence/validation checks near alx.py `_receipt_phase`, `cmd_issue`, and the render receipt list) applies the same tolerance: stale = absent, one WARN, exit 0. The `=== REMINDERS … ===` header prints only when at least one reminder follows.

### W6 — stale fidelity receipt tolerance, empty reminders header  (worktree `wt/w6-fidelity`)
Files: `scripts/content_gate.py` (run_content_gate around the `source_fidelity_receipt_path` / `fidelity_skipped` block, ~615-662), `scripts/alx.py` (readers of `receipts/source-fidelity.json` — `grep -n 'source-fidelity.json' scripts/alx.py`; the REMINDERS header emitter — `grep -n 'REMINDERS' scripts/alx.py`), `scripts/source_fidelity.py` only if `validate_source_fidelity_receipt` must expose a "stale vs invalid" distinction (prefer checking `ledger_sha256`/`report_sha256` in the caller). Tests: one for R35.13 (content gate with a receipt whose `ledger_sha256` is wrong → no HARD, one WARN, receipt issued), one for the header (no reminders → no header line); restate any pin that expects the HARD. Rulings: R35.13, R35.11. Do NOT touch review start/finish code, merge code, render backend code, schemas, or .md files.

## WorkBuddy verification (user, 09-16 evening) → R35.14, R35.15 and W7

WorkBuddy confirmed Bugs 1, 3, 4, the render chain and the clock. Two objections: (1) `ledger merge` now accepts garbage silently — an unknown `coverage[].status`, strings where `check` expects objects, a made-up `editorial_mode` — "no defence at all"; (2) the wrap-up rule lives only in SKILL.md's Clock section; the model sees only the `remaining N min` number.

R35.14 Merge warns only where a consumer exists. After storing the patch as given, `ledger merge` prints (all at once, exit 0, nothing rejected) one WARN per finding, and `check`/`issue` repeat them under the existing `ledger/coverage` / `ledger/synthesis` WARN families (one shared helper in validate_ledger.py, e.g. `notes_shape_findings(ledger)`, called from `collect_findings` and from merge):
- `coverage` not a list, or an item not an object → `coverage[i] is not an object — stored as given; check reads area/status/claim_ids from objects only`.
- `coverage[i].status` present and not in `unstarted|in_progress|supported|disputed|gap` → `coverage[i].status '<v>' is not one of unstarted|in_progress|supported|disputed|gap — stored as given; check tracks coverage only for supported|disputed|gap`.
- `coverage[i].claim_ids`, `synthesis.central_judgment_claim_ids`, `synthesis.counterevidence_claim_ids` present and not a list of strings → `<path> must be a list of claim ids like ["C1","C2"] — stored as given`.
- `synthesis.adversarial_tests` / `implications` / `decisions_or_takeaways` / `scenarios` present with an item that is not an object, or an object lacking a list at `claim_ids` (`rationale_claim_ids` for decisions_or_takeaways) → `<path>[i] is a <type>; check links it to claims only through an object with <claim_ids|rationale_claim_ids> — stored as given`.
- Nothing for `brief.*`, `people.*`, `unresolved_questions`, or any other synthesis sub-field (`outcome`, `hypothesis`, `result`, …): no code reads them. This is deliberate and stays.
R35.15 The clock speaks when it matters. The footer every command prints (`elapsed N min, remaining M min`, alx.py `_emit`) gets a suffix: when `1 ≤ remaining ≤ 8` → ` — stop polishing: alx issue, then alx render`; when `remaining == 0` → ` — time is up: alx issue, then alx render, deliver`. Nothing else about the clock changes (no blocking, no behaviour change in any command). Existing tests that match the footer literally are restated to a prefix/regex match.

### W7 — merge-time note warnings, clock footer  (worktree `wt/w7-notes`)
Files: `scripts/validate_ledger.py` (new helper + call from `collect_findings`), `scripts/alx.py` (`cmd_ledger_merge` prints the helper's lines after `merged:`; `_emit` footer suffix), tests (one test per ruling; restate footer pins; a merge test asserting a made-up `editorial_mode` and an unknown `outcome` produce NO line). Rulings: R35.14, R35.15, R35.11. Do NOT touch schemas, review code, render code, gate_severity.py, content_gate.py, rewild_gate.py, or .md files (the orchestrator restates gate-errors.md / SKILL.md if needed).
