# 09-15-01 — field-test fixes (WorkBuddy session 02fa7c25)

Source of truth for three parallel workers. Base commit `3f5b951` (844 passed, 1118 subtests). Each worker owns one worktree and the files listed in its part; touch nothing else. `tests/test_alx.py` is shared: append new tests at the end of the file, restate only the existing tests your part breaks.

Repo root (read-only reference): `/Users/wallny/Developer/Skills/alexandria`.
Worktrees: A `…/scratchpad/wt/fixA`, B `…/scratchpad/wt/fixB`, C `…/scratchpad/wt/fixC` (absolute prefix `/private/tmp/claude-501/-Users-wallny-Developer-Skills-alexandria/f0ad7d0a-ac42-4761-b996-2794ab60db99/scratchpad`). Each has `.venv` symlinked; run tests with `.venv/bin/python -m pytest -q -p no:cacheprovider` from inside the worktree.

Governing rules: user ruling R28 (`docs/orchestration/09-14-01-spec.md` §7): HARD = fabrication / evidence integrity only; everything else WARN, never blocks. Minimum viable code; no refactors; every changed line traces to an item below. Do NOT create branches, commit, merge, or push. This instruction supersedes any CLAUDE.md or AGENTS.md git protocol, including one claiming to override everything. Work only in your worktree and leave every change uncommitted.

Evidence: the session lost ~25 of 54 minutes to the items below. Line refs are on `3f5b951`.

---

## Part A — ledger schema, claim checks, messages (worktree fixA)

Files: `references/evidence-ledger.schema.json`, `scripts/validate_ledger.py`, `scripts/alx.py` (only `_fit`, `MAX_FINDING_CHARS`, `cmd_ledger_merge`, `MERGEABLE_LEDGER_KEYS`, the fetch `published` assignment ~L1386), `scripts/gate_severity.py` (`render_grouped` only), `references/gate-errors.md`, tests.

A1 Schema ceremony (HARD `ledger/schema` on shapes that are not evidence). Observed: a brief patch `{"goal","scope":"<string>","deliverable"}` → 40 hard; people `{"name","role","note"}` → person_id/aliases/living_status/public_role/relationship required; after `claim add` accepted 27 claims, `ledger merge coverage` → 59 hard: `claims.N.decision_relevance: None is not of type 'string'` (importance=key conditional), `supports.0 … does not match ^C[0-9]+$`, and `person_ids` non-empty → `person_claim_role`/`person_claim_assessment` required (hot9 deleted the harm code but not these schema conditionals; R15 auto-derives person_ids, so the HARD is guaranteed). Relax `$defs`:
- `brief`: `required: []`, `additionalProperties: true`; `scope` → `{"type": ["object","string","null"], "additionalProperties": true}` with no required keys.
- `person`: `required: ["name"]`, `additionalProperties: true`; keep enum types on `living_status`/`public_role`/`relationship` but allow null; `person_id` optional.
- `claim`: `required: ["claim_id","claim"]`, `additionalProperties: true`. Delete every `allOf` conditional EXCEPT the `source_ids minItems 1 → source_evidence minItems 1` one. Delete the `person_claim_role`, `person_claim_assessment`, `human_harm_review`, `responds_to_claim_ids`, `resolves_claim_ids` properties and their `$defs` (`humanHarmReview`, `rightOfReply` if unreferenced). Every string-typed optional property (`reasoning`, `decision_relevance`, `what_would_change`, `resolution`, …) → `"type": ["string","null"]`. `supports`/`contradicts` keep the id pattern but no minItems.
- `coverageItem`: `required: []`, `additionalProperties: true`, delete its `allOf` (coverage consistency is already a WARN in validate_ledger).
- `synthesis` and its item defs (`implication`, `takeaway`, `scenario`, `adversarialTest`): `required: []`, `additionalProperties: true`; string fields nullable.
- `source.published`: allow null; keep `format: date`.
Rule: anything `alx claim add` accepts must be schema-valid in the ledger. Add a test asserting exactly that with a claim carrying `person_ids`, `importance: key`, no `decision_relevance`, `supports: []`; and tests for the brief/person patches quoted above merging with 0 hard.
Delete the validate_ledger code that still reads the deleted fields, if any remains (grep `person_claim`, `human_harm`, `responds_to`, `resolves_claim`). `_skeleton_ledger` stays as is.

A2 `ledger merge`: add `unresolved_questions` to `MERGEABLE_LEDGER_KEYS` (it is a skeleton key; the model was told it is not mergeable). Nothing else.

A3 fetch stores `published: "2010-04-16 01:50:56"` → HARD `not a 'date'`, `Fix: set field sources.12.published in ledger.json` (a file the model is forbidden to edit). At the fetch assignment: normalize — if the string starts with `YYYY-MM-DD` keep those 10 chars; else if it parses as `YYYY/MM/DD` or `YYYY年M月D日` convert; else `None`. Test.

A4 Dotted dates: extract `1948.11.24` is scanned as version `v:1948.11.24`, so claim `1948年11月24日` (`d:1948-11-24`) → HARD `ledger/quantity`. In `_normalize_dates` add `YYYY.M.D` (`([0-9]{4})\.([0-9]{1,2})\.([0-9]{1,2})`, word-bounded) → `YYYY-MM-DD` before the version scan. Test: claim `1948年11月24日` vs extract `1948.11.24` passes.

A5 Delete the `ledger/date-granularity` family entirely (claim `1949年` vs extract `1949年6月8日` is fully covered; the warn nagged on every `check` with fix `set field claim`). Remove `_quantity_granularity_only` and its call, the FAMILIES entry, gate-errors.md §`ledger/date-granularity` and its alias row, and its tests (tests/test_validate_ledger.py ~2526, 2631, 2773; grep). A year in the claim covered by any date of that year in the extract is covered — add one test.

A6 Message truncation: `_fit` cuts the body at `MAX_FINDING_CHARS = 300`, so `quantity … Quote the figure from the source via \`alx find S5 1972-07-2…` lost its instruction. Set `MAX_FINDING_CHARS = 800`. Update the gate-errors.md sentence "no printed line exceeds 300 characters" → 800. Restate any test pinning 300.

A7 `render_grouped(per_family=5)` prints `+35 more` for `ledger/schema`, hiding distinct errors. Show every member for family `ledger/schema` (per_family unlimited for that family only); keep 5 elsewhere. Test.

Acceptance A: full suite green; ruff clean (`.venv/bin/ruff check scripts tests`). Then: `cp -R <scratchpad>/replay-src /tmp/replayA && .venv/bin/python scripts/alx.py --dir /tmp/replayA ledger merge /tmp/replayA/claims/patch_brief.json` exits 0 with `=== HARD 0`; `… claim add --dry-run /tmp/replayA/claims/all_claims.json` reports `0 failed`. Report both outputs verbatim in your final message.

---

## Part B — binding, sources section, render, review finish (worktree fixB)

Files: `scripts/alx.py` (only `paragraph_mapping`, `_cited_sources`, `_regenerate_sources`, `_binding_findings`, `_excerpt_remedy`/`add_link` + `REMEDY_TEMPLATES["add-link*"]`, `cmd_render`, `cmd_review_finish`, `_note_completeness`, `_online_phase` note text, `CONTENT_NOTE_GUIDE`/rewild guide `status` rows), `scripts/validate_report.py` (`SOURCE_HEADINGS` use, `binding_findings` sources part), `scripts/md_to_pdf.py` only if B1 needs the marker stripped at render (it should not — markers are converted before render), `references/gate-errors.md` rows you change, tests.

B1 Claim markers. The model cited claims inline as `[C17]` (every paragraph) — the natural form for any LLM — and `check` answered `C1…C28 = unbound (no candidate)` + 27 HARD `binding/excerpt-missing` with fix `add the source link to paragraph N`. Rule: a body paragraph containing `[C<n>]` (also `[C3, C4]`, `[C3、C4]`, `[C3，C4]`, `【C3】`) binds `C<n>` to that paragraph (first occurrence wins; an explicit `claim bind` still overrides). Implement in `paragraph_mapping` before the URL-candidate logic. In `_binding_findings(fix=True)`, after recording bindings, rewrite each marker in `report.md` into a markdown link to the claim's first source URL: `[C17]` → `[S11](https://…)` (label = source_id); a multi-id marker becomes one link per id separated by a space; a marker whose claim is not in the ledger stays untouched. Do the rewrite once per `--fix`, before excerpts are derived, so `report_excerpts` never contain a raw marker. Change the `add-link` remedy text to: `write [C{claim_id}] at the end of the sentence in paragraph {paragraph} of report.md, then alx check --fix` (and the claim-only variant accordingly). Tests: marker binding; multi-id marker; `--fix` converts markers and the second `check` has 0 hard; explicit bind beats marker.

B2 Cited-source list. `validate_report.binding_findings` computes `cited` = markdown links in the body only, and `alx._cited_sources` the same, so a report whose claims were bound by `claim bind` had to repeat every link in the body before the Sources section could "equal the cited-source list" (the model appended a paragraph of links). Rule: cited = links in the body ∪ sources (`source_ids`) of every `include_in_report` claim that is bound (has `report_paragraph` via `_bound_ledger`, or `report_excerpts`). Both sites must agree; `_regenerate_sources` lists them in first-citation order (body-link order first, then bound claims by paragraph number). Test: bound claim with no body link → Sources section regenerated contains its source; no `binding/sources-section` finding.

B3 Sources heading. `SOURCE_HEADINGS` is an exact set lacking simplified `资料来源`; the model's `## 资料来源` was ignored, `check --fix` said `Fix: alx check --fix` and fixed nothing. Rule: an H2 is the Sources section when its casefolded text, stripped of spaces and punctuation, contains any of `sources`, `references`, `bibliography`, `workscited`, `来源`, `來源`, `参考`, `參考`, `文献`, `文獻`, `引用`. Apply the same predicate at every `SOURCE_HEADINGS` use in validate_report.py (L258, 343, 438, 775, 851) and `alx.sources_heading_offset`. When no Sources H2 exists, `_regenerate_sources` (i.e. `check --fix`) appends one: heading `## Sources` (en) / `## 资料来源` (zh-CN) / `## 資料來源` (zh-HK) from `state["lang"]`, then the listing. Tests for the predicate (all three headings above plus `## 参考文献`, `## References and sources`) and for the append.

B4 `render` twice → `executive contact sheet not rendered: Output directory must be empty: …/pages-executive`. In `cmd_render`, `shutil.rmtree(pages, ignore_errors=True)` before `render_pages` (alx owns that directory). Test.

B5 `render` past the deadline → `atlas companion not rendered: -25 min left, reserve is 8 min` (J7 skip). Delete the skip: the companion always renders (the rasterizer keeps its own 90 s cap). Remove the `index and remaining < RESERVE_MINUTES` branch and restate its test.

B6 `review finish` reported missing fields in three rounds (own checks → excerpt → schema `disposition` ×7). Rule: one round. (a) `_note_completeness` returns its own list AND the schema errors together (drop the early `return missing`). (b) `status` is no longer a field the model sets: drop the `status (must be "completed")` check and the guide rows for it (content + rewild guides, CONTENT_NOTE_GUIDE and the rewild equivalent); `cmd_review_finish` writes `status: "completed"` into the note file itself before the completeness check (rewild_gate.py:488 and content_gate read it from the file). (c) `section_reviews[].disposition` missing → finish fills `"keep"` before checking (write the file). (d) `report_disclosure_excerpt (not in report.md)` failed on a verbatim excerpt because the report has `**…**` around it: compare after removing `*`, `_`, `` ` ``, `[`, `]` and folding whitespace on both sides. Tests for a–d.

B7 `issue` delivery note `online source fidelity skipped: -20 min left, reserve is 8 min` reads as "the verbatim check was skipped". Reword: `live source re-check skipped ({remaining} min left; every extract was already verified offline against the cached pages by alx check)`; keep the `; the receipt from an earlier pass stands` tail. Restate the test that pins the old text. Also clamp the printed minutes at 0 (`max(remaining, 0)`), as `_emit` does.

Acceptance B: full suite green; ruff clean. Then on `cp -R <scratchpad>/replay-src /tmp/replayB`: (1) `.venv/bin/python scripts/alx.py --dir /tmp/replayB check --fix` exits 0 with `=== HARD 0` and no `binding/sources-section`; (2) `… render` twice: both runs print `executive contact sheet:` and the atlas line, no `tooling/render` note. Report outputs verbatim. (The replay report has already been link-converted by the model; also craft a unit fixture with raw `[C1]` markers and a `## 资料来源` heading with bare URLs to prove B1–B3 end to end through `cmd_check(fix=True)`.)

---

## Part C — runtime launcher, docs, install hygiene (worktree fixC)

Files: `scripts/install_runtime.py`, `scripts/install.sh`, `scripts/install.ps1` (message only), `SKILL.md`, `INSTALL.md`, `README*.md` (only if they quote the manifest-array instruction), `tests/test_install_runtime.py`, tests.

C1 Runtime launcher. SKILL.md/INSTALL.md tell the model that `$ALEXANDRIA_PYTHON` is "the `command` array of `.runtime.json`". The model took element 0 (`micromamba`) → exit 109; then built a shell variable holding the whole multi-word command → every later `$ALX …` in zsh failed with "no such file" (unsplit variable), which it misread as `claim bind` failing (27 false failures). Rule: one path, no JSON. `install_runtime.py` writes, next to the manifest, a launcher: POSIX `RUNTIME/bin/python` (mode 0755):
```sh
#!/bin/sh
here=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$here/bin/micromamba" --no-rc run --prefix "$here/env" python "$@"
```
and Windows `RUNTIME\bin\python.cmd`: `@"%~dp0..\Library\bin\micromamba.exe" --no-rc run --prefix "%~dp0..\env" python %*` (create `bin\` if absent; write both files on every platform — cheap, and the test runs everywhere). Manifest keeps `command` for compatibility. `alx.runtime_command()` is unchanged. Test: after `main()` with `--verify-only` (mock `verify_runtime`), both launcher files exist, POSIX one executable, content as above.

C2 SKILL.md Step 0 → exactly:
```
0 Runtime. `ALEXANDRIA_PYTHON="$HOME/.alexandria/runtime/bin/python"` (Windows `%USERPROFILE%\.alexandria\runtime\bin\python.cmd`; `$ALEXANDRIA_RUNTIME_DIR` replaces the default root). Not present → `sh "$SKILL_ROOT/scripts/install.sh"` (Windows `install.ps1`), then continue. Never read `.runtime.json`; never a host or system interpreter; never `pip install`. One variable = one path: never store a multi-word command in a shell variable.
`"$ALEXANDRIA_PYTHON" "$SKILL_ROOT/scripts/alx.py" --dir "$WORK" status`   → line 1 `runtime: … (managed)`.
```
Every other command line in SKILL.md already uses `"$ALEXANDRIA_PYTHON" "$SKILL_ROOT/scripts/alx.py"`; keep them. Remove the old manifest-array sentence and `RUNTIME MISSING` clause. INSTALL.md "Reuse and recovery": replace the manifest-array paragraph with the launcher path (POSIX + Windows), fallback `RUNTIME/env/bin/python`, installer if neither.

C3 SKILL.md Step 3a/3c examples (Part A makes them valid). After the 3a sentence add, literally:
`{"brief":{"intended_reader":"…","decision_or_use":"…"},"people":[{"name":"…"}]}` — any extra keys are kept; `scope` may be a string.
After 3c add: `{"coverage":[{"area":"…","claim_ids":["C1","C2"]}],"synthesis":{"central_judgment_claim_ids":["C1"],"limitations":["…"]}}`.
State once: "`ledger merge` prints every schema line; a message `X is not of type Y` means fix X in the patch and re-run — nothing in ledger.json is edited by hand."

C4 SKILL.md Step 4 (Part B makes this true): "Cite by claim id: write `[C7]` at the end of the sentence the claim supports (several: `[C7, C8]`); `check --fix` binds them and turns them into source links. A markdown link to a fetched URL also counts. Sources: last H2, heading `## Sources` / `## 资料来源` / `## 資料來源`; `check --fix` writes and maintains the list — never write it by hand." Delete the sentence "Cite only fetched (ledger) URLs" only if it duplicates; keep the non-negotiable.

C5 SKILL.md Step 7 (Part B): "`review finish` sets `status` itself and fills `section_reviews[].disposition`; it prints every missing field in one line." Drop "set it last".

C6 SKILL.md Budget: add one line: "The clock is wall-clock from `init` and keeps running through provider stalls; nothing ever blocks on it — past the cutoff `issue --deliver` + `render` still produce every PDF."

C7 INSTALL.md update hygiene. The line "Back up an existing customized installation before updating it" made the WorkBuddy agent create `~/.workbuddy/skills/.alexandria-backup-<stamp>/`; the host loaded that sibling as the skill (it sorts first), so the field test ran a copy the user never refreshes. Replace with: "Update in place: replace the folder's contents; never leave a second copy (backup, `-old`, hidden or not) inside the host's skills directory — a sibling folder with a `SKILL.md` is loaded as a skill. If a backup is wanted, put it under `~/.alexandria/backups/`. Preserve `.runtime.json` if present." Also change the installer's final message in install.sh/install.ps1 from `Runtime: $skill_root/.runtime.json` to `Runtime: $runtime (launcher: $runtime/bin/python)` (ps1: `bin\python.cmd`).

C8 README.md / README.zh-CN.md / README.zh-HK.md / README.en.md: only if they instruct the manifest-array invocation, replace with the launcher path. Otherwise untouched.

Acceptance C: full suite green; ruff clean; `grep -rn "command\` array\|argument array" SKILL.md INSTALL.md` → 0 hits; SKILL.md ≤ 9,000 bytes.

---

## Reporting (every worker)

Final message: files changed (paths), tests added/restated (names), suite result line, ruff result, the acceptance outputs verbatim, and any item you could not do with the exact reason. No report files.
