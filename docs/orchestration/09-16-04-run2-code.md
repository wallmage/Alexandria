Using dispatching-parallel-agents for five independent recon areas. Reading that skill, then launching one Task agent per area.Dispatching five explore agents in parallel, one per area.Spot-checking the cited line ranges so the tables match the files.Read-only recon of `main` (`b034256`). Five parallel explore agents; citations spot-checked against the files.

## A. Fetch + URL storage

`cmd_fetch` (`alx.py:1685-1699`) loops URLs into `_fetch_one` (`1558`); `--id` uses `_refresh_one` (updates `accessed` only). Transport is custom `http.client` GET via `_request_pinned` (`source_fidelity.py:687+`), not httpx/requests. No HEAD.

**Redirects** (`default_fetcher` `1125-1202`, `MAX_REDIRECTS=5` at `93`):

| Rule | Behavior |
|---|---|
| Loop | `for _ in range(MAX_REDIRECTS + 1)` → max 5 hops; then `Source exceeded 5 redirects.` (`1202`) |
| Codes | 301/302/303/307/308 + `Location` (`1163-1178`); resolve `urljoin`; `validate_public_http_url` |
| Limits | HTTPS↛HTTP (`1171-1172`); same registrable domain else `cross-domain-redirect → {url}` (`1173-1176`); `visited` loop (`1138-1139`) |
| Stored hops | `FetchedDocument.redirects` (`1196`); `final_url=current.url` (`1195`) |

`fetch_document` does not follow redirects; it calls `default_fetcher` (`503`) then sets `url=raw_url`, `final_url=fetched.final_url`, `aliases=[raw_url, final_url, *redirects]` (`528-536`). `_fetch_one` HTTPS-upgrades `http://` before fetch (`1564-1576`), so `raw_url` is that target, not always the CLI string.

| Field | Ledger (`_upsert_source` `1533-1552`) | `S<n>.meta.json` (`write_cache` `354-369`) |
|---|---|---|
| `url` | **`result.final_url`** (`1536`) | **`result.url`** = requested fetch target (`355`) |
| `aliases` | requested + hops + https target **minus** `final_url` (`1537`, assembled `1563-1594`) | full `result.aliases` (`357`) |
| `final_url` | no | yes (`356`) |
| `requested_url` / `canonical_url` | **no** | **no** (`requested_url` only in fidelity observations `1365-1378`, `1492-1495`) |

**Ledger keys written at fetch:** `source_id`, `title`, `url`, `aliases`, `publisher`, `accessed`, `source_family`, `published`, `author` (`setdefault(None)` `1546`), `provenance`, `evidence_type`, `roles`, `accountability_basis`. Not written here: `family_justification`, `accountability_note`, `limitations`, `undated_reason`.

**Meta keys:** `url`, `final_url`, `aliases`, `http_status`, `charset`, `title`, `published`, `fetched_at`, `text_sha256`, `transport`, `probe_contexts` (preserved). No `redirects` / `requested_url` key.

**Per-URL prints** (`_emit` `1068-1071`):

| Outcome | f-string | Line |
|---|---|---|
| OK | `f"{source_id} OK {len(text)} chars {result.charset} \"{result.title}\" {result.final_url}"` | `1598-1600` (refresh `1631-1633`) |
| Dup | `f"{existing['source_id']} already in the ledger: {existing['url']}"` | `1569` |
| Reject | `f"{url} REJECTED (non-http scheme) — not added"` | `1561` |
| Unreachable | `f"{source_id} UNREACHABLE ({reason_class}: {reason}){suffix}"` | `1584-1586` |
| Undecodable | `f"{source_id} UNDECODABLE ({reason_class}) — not added"` | `1590` |

## B. Link binding

Match: `normalize_url(report_url) in _ledger_url_set` (`validate_report.py:568-575`). Set = `normalize_url` of every `source.url` **and** `aliases` (`432-440`). Destinations from `extract_markdown_urls` (`240-242`); http/https only (`572`).

`normalize_url` (`400-410`): lower scheme+host, drop fragment, drop `utm_*`, `path.rstrip("/")`. **No** `www.` strip, **no** http↔https.

| Helper | File:line | Notes |
|---|---|---|
| `normalize_url` | `validate_report.py:400` | binding match |
| `normalize_url` | `alx.py:1130` | `casefold`; same idea |
| `_normalized_url` | `validate_ledger.py:1342` | ledger dedup; `path or "/"`; keeps query |
| `_registrable_domain` | `source_fidelity.py:151-154` | strips `www.` (domain, not links) |
| `_same_source_host` | `source_fidelity.py:621-624` | optional `www.` strip |
| `_normalize_url` / `canonical_url` | — | **none** in `scripts/` |

**`binding/link-not-in-ledger`** (`577-585`): `f"Report URL is not present in the ledger: {url}"` + optional `f"; nearest ledger URL: {nearest}"`. Nearest: `difflib.SequenceMatcher(...).ratio()` (`451-457`); candidates = **raw** `source["url"]` only (`443-448`), not aliases, not normalized.

**`alx check --fix`:** `cmd_check` → `run_check(..., fix=args.fix)` (`3544-3548`, `3479-3492`). `mechanical_fixes` (`3411`) does **not** touch URLs. Binding fix (`2963-2980`): `_bind_markers` → `_convert_claim_markers` (`2730-2758`) rewrites `[C17]` → `[S1]({source['url']})` (ledger URL as-is). Then `_regenerate_sources` rebuilds Sources with `source.get('url')`. **Does not** rewrite/normalize existing body destinations. `link-not-in-ledger` is not auto-fixed here.

## C. Claim-add FAIL output

`cmd_claim_add` does not format the passage. It prints `item_finding.message` (`2144-2148`):

```
f"{claim_id} {'WARN' if … else 'FAIL'} [{item_finding.family}] {item_finding.message} — fix: {item_finding.fix}"
```

`fidelity/mismatch` message (`source_fidelity.py:1070-1076`) appends `_closest_passage_message` (`1004-1031`):

- miss: `f" no similar passage in {source_id}"` (`1011`, `1013`)
- hit: `f" Closest passage in {source_id}: {json.dumps(passage, ensure_ascii=False)}"` (`1031`)

**JSON-encoded**, same as find (`alx.py:1836-1838`): `f"{source_id} #{index} extract_or_location: " + json.dumps(window, ensure_ascii=False)`. `cmd_claim_add` does not `dumps` again.

| Output | Where |
|---|---|
| Summary | `lines.insert(0, f"{len(items)} submitted, {accepted} accepted, {tail}")` (`2189-2192`); `_emit` summary `f"{accepted} accepted"` (`2198-2203`) |
| Accept lines | `f"{id} {verb} (assigned id)"` / `f"{id} {verb} ({n} sources)"` (`2179-2183`) |
| Transcript | `transcript = ws.alx / "last-claim-add.txt"` (`2194`); `write_text("\n".join(lines)+"\n")` (`2196`); then `full output: .alx/last-claim-add.txt` (`2197`). `ws.alx` = `{workspace}/.alx` (`1018`) |

## D. Review skeleton

`cmd_review_start` (`3717-3746`) writes `_note_skeleton` (`3691-3714`) after `_stamp_review_metadata` (`3669-3688`). **`section_reviews` is `[]` — not prefilled from H2s.**

Shared stamp: `schema_version` 1 rewild / 2 content; `status` then set `"draft"` (`3693`); paths/hashes; `source_sha256` `""` if no snapshot; `reviewer_mode="fresh_eyes"`.

| Kind | Fields (literals) |
|---|---|
| rewild | `fidelity_checks`: each of `facts_and_figures`, `attribution_and_uncertainty`, `direction_and_negation`, `causality` → **`False`** (`3695-3697`; keys `rewild_gate.py:44-48`); `findings=[]` |
| content | `scores[k]={score: None, rationale: ""}` for `CONTENT_SCORE_KEYS` (`158-167`); `checks[k]=False` for `CONTENT_CHECK_KEYS` (`168-179`); `section_reviews=[]`, `visual_assets=[]`, `findings=[]`, `evidence_limitations=[]`, `completion_note=""`, `claim_support=[]` (`3700-3712`) |

H2 helper: `_h2_sections` (`validate_report.py:164-176`) — `^##\s+(.+?)\s*$` on unfenced text. Used at finish-gate by `content_gate._section_review_errors` (`147-179`), matching `section_heading` via `_normalized_heading` (`138-139`). Not used at start.

| Field | `review finish` → `_note_completeness` (`3095-3180`, called `3777`) | `content_gate.run_check` (`536-565`) |
|---|---|---|
| score | `not isinstance(score, int) or score < 4` (`3142`) | int **and** `< 4` only (`539-542`); `null` skipped |
| rationale | **never checked** | **never** |
| checks | `checks.get(name) is not True` (`3149`) | `passed is False` only (`554`) |
| section_reviews | `if not note.get("section_reviews")` (`3152`) | same (`562`) |
| completion_note | `if not note.get("completion_note")` (`3154`) | same (`564`) |
| fidelity_checks | `is not True` per required key (`3107-3113`) | not in `content_gate` (rewild gate) |

Finish never blocks: prints `WARN review/{kind}: …`, sets `finished=True` (`3778-3781`). Defaults missing `disposition` → `"keep"` (`3773-3775`). Heading coverage is `run_content_gate` only (`698-700`), not `run_check`.

## E. `notes_shape_findings`

Full function: `validate_ledger.py:3117-3206`. Core for `adversarial_tests` (`3183-3205`): if value is **not a list**, `continue` (silent). Per item, silent only if `isinstance(item, dict) and isinstance(item.get("claim_ids"), list)`. Else:

`f"synthesis.adversarial_tests[{i}] is a {type(item).__name__}; check links it to claims only through an object with claim_ids — stored as given"`

| Shape | Result |
|---|---|
| missing / `null` / **dict instead of list** | silent (`3191-3192`) |
| `[]` | silent |
| `{claim_ids: []}` or `{claim_ids: ["C1"]}` or extra keys | silent |
| `{claim_ids: [1,2]}` (non-strings) | **silent** (`_str_list` unused here) |
| `{claim_ids: "C1"}` (string) | **WARN** (`is a dict`) |
| `{claims: [...]}` no `claim_ids` | **WARN** |
| nested list `[[{...}]]` | **WARN** (`is a list`) |
| list of strings | **WARN** (`is a str`) |

Same loop for `implications`/`scenarios` (`claim_ids`) and `decisions_or_takeaways` (`rationale_claim_ids`). Stricter `_str_list` (`3113-3114`) only on `central_judgment_claim_ids` / `counterevidence_claim_ids` when the key is present (`3172-3181`).
