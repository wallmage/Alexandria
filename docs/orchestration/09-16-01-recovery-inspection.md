# 09-16-01 — history-only recovery inspection (R32)

Job: find every quality mechanism dropped in the September window and not present at HEAD. Recovery (arbiter-decided, from git, never rewritten) follows in a later step. README comparison EXCLUDED (done 09-15). Fat deletion EXCLUDED (part two, later). Scouts: read-only; facts with evidence; the contract wins over the dispatch prompt.

## §0 Common

Repo `/Users/wallny/Developer/Skills/alexandria`, HEAD = `b3d46e3` (main, clean; ignore uncommitted files under `docs/orchestration/`). BASELINE = `1edb929` (2026-08-30, last commit before the window). WINDOW = `1edb929..b3d46e3`, 173 commits, 2026-09-12 23:20 → 09-16 00:43. List: `git log --no-merges --reverse --format='%h %ad %s' --date=format:'%m-%d %H:%M' 1edb929..b3d46e3`.

Skill shape: the model reads `SKILL.md`, runs `scripts/alx.py` subcommands (`alx …`) which call the modules in `scripts/`; before 09-14 (no alx.py) `SKILL.md` invoked the module scripts directly. Report quality = these mechanisms: source fetch/cache integrity; verbatim-extract probing (fabrication gate); ledger structure/coverage/synthesis/triangulation/independence/freshness checks and their remedies; claim kinds/importance/confidence; report-vs-ledger consistency (citations, links, quotations, figures, numbers); Rewild humanizing + rewild fidelity gate; content review + rewild review; PDF production checks (text layer, links, fonts, pages, overflow, contact sheets, PDFKit verification, portability, template selection); editorial modes/archetypes/depth targets; research protocol (parallel agents, disjoint id ranges); severity/receipts/issue drop logic; tests pinning any of these.

User principle (verbatim): "Any mechanisms, anything related to quality gates that meaningfully control the quality, uphold the value of the report must stay … if you think they are useful … if you think they belong to the quality gate … recover them."

User-ruled BURDEN (R25–R31; never recovered; list sightings under `burden_seen`, one line each, no analysis): living/dead subject status (`--subject-status`, `living_status`) and every living-person harm rule (role/assessment/harm review/protected status); any command refusing or blocking (exit≠0 that stops the flow); clock-decided behaviour (deadline/remaining minutes changing what runs); `claim add --dry-run` as a step; `alx issue --deliver`, Class A/F, waivers; "status line 1 must say managed", reading `.runtime.json`; per-step time budgets; the "Verification note" paragraph inside the PDF; README phrase-matching test; severity downgrades HARD→WARN for ledger/quantity, CJK numerals, source classification (portfolio), status/direction/schema, person_id. A downgrade is NOT a drop when the check still runs and prints; a check that no longer runs, no longer prints, or is unreachable IS a drop.

Dropped = one of: `absent` (no equivalent code/text at HEAD); `off-path` (exists at HEAD but nothing on HEAD's `SKILL.md` path or in `alx.py` reaches it); `weakened` (threshold loosened, scope narrowed, output hidden, sample reduced, fallback that silently skips it). Renamed/moved with the same effect = not a finding (one line in `notes`).

Method: git only (`git show <c>:<path>`, `git diff <a> <b> -- <path>`, `git log -p`, `git log -S'<symbol>'`, `git show <c> -- <path> | grep -n '^-'`); read code, not prose, unless your section names a doc; every claim carries evidence (commit:path:symbol, or file:line at HEAD, or the grep that returned nothing). No edits, no branches, no commits. Use Task subagents in parallel: split your scope by file or commit range; each returns the same JSON; you merge and dedupe.

Output — the final answer is ONLY this JSON (no fences, no prose before or after):
{"scout":"<name>","covered":["<what you actually examined: commits, files, functions>"],"findings":[{"id":"<name>-NN","what":"<mechanism, one line>","kind":"code|test|skill-prose|reference|schema|flow","last_present":"<commit>:<path>:<symbol or lines>","removed_in":"<commit> <subject>","present_at_head":"absent|off-path|weakened","head_evidence":"<file:line, or 'grep -n X scripts/ → 0 hits'>","purpose":"<quality property it protects, concrete>","failure_if_absent":"<concrete bad-report outcome possible now>","burden_ruling":"none|<ruling id + why it matches>","recovery":"<git show <commit>:<path> symbol/lines; HEAD conflicts to respect>","confidence":"high|medium|low"}],"burden_seen":["<one line each>"],"notes":"<equivalents, blind spots, conflicts with this contract>"}

Zero findings is a valid answer when the evidence supports it; `covered` must then show what was examined.

## §A skill — SKILL.md
Scope: `git log --oneline 1edb929..b3d46e3 -- SKILL.md` (21 commits). `1edb929:SKILL.md` = 403 lines (July text + ten August commits: "Raise P0/P1 quality bar", "Harden delivery gates", "Simplify validation and retain delivery hardening", "three-language golden gates, severity tiers, CJK quantity scanning", "Verify PDFs with macOS PDFKit", "Harden PDF portability", "Require explicit long-form requests", security fixes; `git diff 41aca26 1edb929 -- SKILL.md` = +215/−59). HEAD = 243 lines (the July `41aca26` prose re-inserted at `ab7226b` around alx command lines). Deliverable: every instruction, step, rule, checklist item, threshold, command flag, reference pointer and acceptance criterion present in `1edb929:SKILL.md` OR in any intermediate version and absent at HEAD. For each: is it re-expressed elsewhere at HEAD (a reference file; an alx check that enforces it automatically — then `equivalent`, in notes)? Pay special attention to the August additions — the R31 recovery used the July text.

## §B flowmap — July/August execution path vs HEAD alx path
Enumerate every script invocation, flag and check the model was told to run at `1edb929` (`1edb929:SKILL.md` and `1edb929:references/*.md`: validate_ledger.py modes/flags, source_fidelity.py commands, content_gate.py, rewild_gate.py, md_to_pdf.py flags, validate_report.py flags, render_pdf_pages.py, pdf_quality.py, contact sheets, PDFKit verify, golden gates). For each: the HEAD equivalent — which alx subcommand, which function, which call site (`scripts/alx.py:line`), with which flags/thresholds — or NONE. Findings = checks with no HEAD call site, or run with narrower coverage / looser thresholds / smaller sample than at baseline, or whose failure blocked delivery at baseline and at HEAD is not even printed. Put the full mapping table in `notes` (compact: check | baseline invocation | HEAD call site or NONE).

## §C alx-early — scripts/alx.py from creation to d85add1
Range: `git log --oneline 1edb929..d85add1 -- scripts/alx.py`. Diff consecutive versions; for every removed `def`, argparse option, branch, family in a set/dict/list, remedy, restore set, receipt field, printed line: is it at `b3d46e3:scripts/alx.py`? Findings per §0. Also: the T7 series (`d9e9828` … `002701d`) added many gates; list every T7 gate absent at HEAD.

## §D alx-late — scripts/alx.py from d85add1 to HEAD
Range: `git log --oneline d85add1..b3d46e3 -- scripts/alx.py` (R30 Part B `2e41772`, Part A `3040c9e`, seams `039b99e` `fb987e6` `9ded9fb` `21ca914` `446f9e0`, R31 recovery `ca7db23` `8838158`, `ab7226b`, `1962778`). Same deliverable as §C for this range. Additionally verify each R31 recovery: for every function the contract `docs/orchestration/09-15-05-restore.md` Part E names, compare the HEAD body with the `d85add1` body; report every remaining difference not listed under "Keep from HEAD" in that contract.

## §E ledger — validate_ledger.py, gate_severity.py, schemas
Files: `scripts/validate_ledger.py`, `scripts/gate_severity.py`, `references/evidence-ledger.schema.json`, `references/claim-input.schema.json`, `references/content-review.schema.json`, `references/rewild-review.schema.json`, `references/rewild-fidelity-notes.schema.json`. Compare `1edb929` → peaks (`f0ee2f6` post-T2, `4a1825c` post-T7, `d85add1`) → HEAD. Deliverable: every finding family, check function, threshold, required schema field / enum / pattern / min-length present at baseline or a peak and absent/weakened at HEAD (schemas "relaxed" at `e7e1258`: list each dropped constraint and what fabrication or incoherence it used to catch). Tier changes alone are not findings (§0).

## §F fidelity — source_fidelity.py
Files: `scripts/source_fidelity.py`, `tests/source_fidelity_transport.py`, `tests/test_source_fidelity.py`. Compare `1edb929` (pre-window `787f713` "Fix source fidelity security boundaries", `1edb929` Clash fake-IP) → `838fbcb` (T1) → `4a1825c` → `18520f4` (quote-span/ellipsis splitting removed) → `2cbbe58` (R13 length) → `d85add1` → HEAD. Deliverable: every probe mode, transport/security boundary, charset/cache/receipt rule, context digest, sample size / threshold present earlier and absent/weakened at HEAD. Deadline handling at fetch = ruling (`burden_seen`).

## §G content — content_gate.py, validate_report.py, report_contract.py, report_blocks.py, pdf_quality.py
Compare `1edb929` (August golden gates `3c3f44e`, severity tiers, CJK quantity scanning) → `750c28c` (T4) → `4a1825c` → `3c4050c` ("content per-claim bookkeeping deleted", per-probe digests) → `d85add1` → HEAD. Deliverable: every check, structure family, length/floor rule, link/alias/citation check, golden-report gate, per-claim bookkeeping, `--force` rejection, `include_ledger_checks` path present earlier and absent/off-path/weakened at HEAD. Also `tests/test_golden_report.py`, `tests/test_content_gate.py`, `tests/test_validate_report.py`: deleted tests → behaviour at HEAD.

## §H rewild — rewild_gate.py, bundled Rewild, rewild reviews
Files: `scripts/rewild_gate.py`; `references/rewild/**` (the bundled humanizer: `rewild/SKILL.md`, `rewild-zh/SKILL.md`, `rewild-hk/SKILL.md`, three `naturalness-check.py` — all MODIFIED in the window: report exactly what changed in each and whether it weakens the humanizer or its checks); `references/rewild-gate.md`; `tests/test_rewild_gate.py`; `tests/test_rewild_receipt.py`. Compare `1edb929` → `2f97537` (T3: integrity tiers, quote-masked style/semantic, monotonic alignment, `--check`) → `564aca3` → `4a1825c` → `aaaac97` (R28) → `d85add1` → HEAD. Deliverable: every tier/check/threshold/receipt rule present earlier and absent/off-path/weakened at HEAD; and whether HEAD's path (`alx snapshot` → humanize → `alx check` → `alx review start/finish rewild` → `alx issue` fidelity/rewild restore) exercises the rewild gate with the same coverage as the direct `rewild_gate.py` invocation in `1edb929:SKILL.md` (flags, tiers, what blocks or drops).

## §I pdf — md_to_pdf.py, render_pdf_pages.py, pdf_templates.py, pdf_compatibility.py, html_policy.py, artifact_safety.py, render_pdfkit_pages.swift
Also `references/pdf-production.md`, `references/pdf-templates.md`, `tests/test_md_to_pdf.py`, `tests/test_render_pdf_pages.py`, `tests/test_render_security.py`, `tests/test_validate_report.py` (PDF parts). Compare `1edb929` (August "Harden delivery gates", "Verify PDFs with macOS PDFKit", "Harden PDF portability") → `3827bb4` `fbcb648` (09-12: "Continue PDF delivery when automated review tooling is unavailable", "Select report templates automatically and keep delivery recovery internal") → `750c28c` (T4 fallback chain, `--issue-receipt`) → `002701d` (T7k render guard) → `d85add1` → `ab7226b` (validate_pdf at render) → HEAD. Deliverable: every verification, guard, flag, threshold (text layer, links, fonts embedded, page count, overflow, contact sheets, PDFKit verification, portability, template selection, HTML policy, artifact safety) present earlier and absent/off-path/weakened at HEAD — including fallbacks that now continue silently where baseline stopped or reported; and whether `alx render` reaches each with baseline thresholds.

## §J refs — references/*.md
Files: every `references/*.md` (not `references/rewild/**`, not schemas). Compare `1edb929` → `8efa698` (T6) → `0323cd0` (R28 docs) → `b19e85e` ("reference ceremony cut") → `d85add1` → `3ec5640` (ceremony passages removed) → `2248a1b` (R31 recovery from d85add1) → HEAD. Deliverable: every passage (rule, checklist item, threshold, procedure, acceptance criterion) present at `1edb929` or an intermediate and absent at HEAD; classify quality-vs-burden per §0; name the HEAD file/section that would receive it. `references/evidence-recording.md` (new): which baseline text did it replace and what did it drop.

## §K tests — tests/**
Every test function present at `1edb929` or any intermediate commit and absent at HEAD (`git log -p --reverse 1edb929..b3d46e3 -- tests/ | grep -n '^-def test_\|^-    def test_\|^-async def test_'`, plus files deleted via `--diff-filter=D`). For each: the behaviour it pinned (read the test body) and whether that behaviour exists at HEAD (code evidence). Deleted README phrase test `daa5a86` = the author's own, skip. Restated tests (same behaviour, new expectation by ruling) = one line in notes.

## §L records — docs/orchestration/**
Files: `ledger.md`, `HANDOFF.md`, `RESUME-PROMPT.md`, `09-14-01-spec.md`, `09-14-01-plan.md`, `09-15-01-fix.md` … `09-15-05-restore.md`. Deliverable: (1) every recorded removal / parking / off-path / optional / silenced / dead-code decision (grep: Parked, PARKED, parked, deleted, removed, dropped, off the path, optional, silent, dead, "kept but", "Keep from HEAD", "never blocks") → state at HEAD (code evidence) → finding when a quality mechanism is gone, off-path or weakened; (2) every check/gate the resilience spec `09-14-01-spec.md` (v3) specifies → implemented and reachable at HEAD? (function + call site) → finding when not.

## §M commits-1, §N commits-2, §O commits-3 — per-commit deletion scan
Ranges (non-merge): M `1edb929..4a1825c`; N `4a1825c..d85add1`; O `d85add1..b3d46e3`. Skip paths: `docs/orchestration/**`, `README*`, `.github/**`, `scripts/install*`, `INSTALL.md`, `docs/plans/**`, `pyproject.toml`, `requirements.txt`. For each commit: `git show --stat <c>`; removed hunks `git show <c> -- <paths> | grep -n '^-'`; list behaviour-level deletions (functions, checks, families, thresholds, flags, printed lines, doc sentences that instruct a check or set a threshold, tests); for each: present at HEAD? (grep). Merge commits: inspect only when the subject mentions conflict/keep/drop/restatement (`git diff <m>^1 <m>`). This slice is the cross-check for §A–§L: report everything, even when you suspect another scout has it.

## §V verification round — three lenses per finding, adversarial
Input: a batch of candidate findings (JSON objects from §0's schema, merged from the 15 scouts) is embedded in the dispatch prompt. Same repo, HEAD, baseline, window, burden list and method as §0. Read-only. Use Task subagents in parallel, one per finding. Default to REFUTING: a finding survives your lens only on evidence you have personally checked in git or at HEAD.

Lens P — presence. Try to prove the mechanism IS at HEAD: renamed, moved into `scripts/alx.py`, enforced by a schema, by a test, by a reference document on the HEAD `SKILL.md` path, or subsumed by a stronger check. Verdict `present_at_head`: `present-equivalent` (refuted) | `absent` | `off-path` | `weakened`; evidence: file:line at HEAD or the grep that returned nothing.

Lens Q — quality. Argue both sides, then decide: does this mechanism meaningfully control report quality (fabrication, source integrity, coverage, argument, prose naturalness, PDF integrity, reader value), or is it ceremony / a user-ruled burden (R25–R31 list in §0) / cosmetic? Name the concrete bad report that ships without it, or state that none does. Verdict `value`: `quality` | `burden` (cite the ruling) | `cosmetic`; `confidence`.

Lens R — recovery. Exact recipe: `git show <commit>:<path>` symbol/lines (or `git checkout <commit> -- <path>` when the whole file is right); does it apply to HEAD cleanly; which HEAD invariants it must respect (only fabrication is HARD; no command refuses; the clock never decides; lenient claim input; reviews and merges never block); which tests to bring back from which commit; regression risk; approximate diff size. Verdict `recoverable`: `clean` | `with-adaptation` (say exactly what) | `not-recoverable` (why).

Output — the final answer is ONLY this JSON: {"verifier":"<name>","lens":"P|Q|R","verdicts":[{"id":"<finding id>","verdict":"<per lens>","evidence":"<file:line / commit:path / grep>","reasoning":"<≤3 sentences>","confidence":"high|medium|low"}],"notes":"<anything the batch as a whole reveals>"}

### §V.1 User rulings verbatim (from docs/orchestration/ledger.md; lens Q cites these, never a paraphrase)
- R28 (09-15 11:xx). USER: "get rid of all these useless, impractical fucking ceremony! … do a full inspection and get rid of all fucking useless but harmful over-strict rules! make it practical for my friends … try to parallelize!" → severity table: HARD = fabrication/evidence integrity only (fidelity mismatch/cache, fidelity/rewild figures, quantity digits+dates, status, direction, derived-hard, claim-input minimal, ledger schema, unfetched source id, link-not-in-ledger, leftover-prose, integrity bytes, quotation altered, runtime). Everything else WARN and never blocks issue/render/claim add; Class A abolished; reviews optional; snapshot optional; conditional claim fields optional.
- R29 (09-15 evening). USER: "many obsolete, crazy, fucking useless, over-strict rules that only do damage … make the skill always resilient and flexible, to accommodate many clients … many different models like GLM 5.3 Flash or DeepSeek … make sure it's really RESILIENT! NO FUCKING STUPID RULES!"
- R30 (09-15 night). USER: "No more ceremony. No more useless, over-strict rules … do I have to care if a stranger is alive or not? I only care about the quality of the report … make it as light as possible … efficient and non-blocking … super resilient, works with everything".
- R31 (09-16). USER: "I want all the useful mechanisms recovered … do only recovery, not reinventing … get rid of the laughable rules like living versus dead … you will not change anything that will impact the quality of the report. You are not to trade convenience with quality."
- R32 (09-16 00:53, this job). USER: "Any mechanisms, anything related to quality gates that meaningfully control the quality, uphold the value of the report must stay … if you think they are useful … recover them."
Reading rule for lens Q: R28–R30 remove BLOCKING and CEREMONY (a check may not stop the flow; a rule with no quality effect goes). They do not remove CHECKS THAT INFORM (a WARN that names a missing field, a loosened threshold, a skipped verification, a stripped field). R31/R32 forbid trading quality for convenience. A mechanism whose only cost is that a weak model sees a warning it may ignore is quality, not burden. A mechanism whose cost is a refusal, a wait, or a rule about the subject's life status is burden.

## §W recovery — wave 1 (arbiter decisions on §V verdicts for batches 1–5)
Rules for every item: recover from the named commit (`git show <commit>:<path>` — copy the hunk, do not paraphrase); adapt ONLY where the verdict names a HEAD invariant: severity WARN (only fabrication is HARD), no command refuses or exits ≠ 0 on the finding, the clock never decides, `claim add` accepts lenient input, reviews/merges never block. Each recovered WARN prints with a fix line. Restate the tests that pinned the removed behaviour (the R28–R30 pins); add one test per recovered behaviour (exit 0 + the printed line). `references/gate-errors.md` gets a section for every family/message you add (tests/test_gate_errors_doc.py derives from the modules). Suite `.venv/bin/python -m pytest -q -p no:cacheprovider` green, `.venv/bin/python -m ruff check scripts tests` clean. Report per item: what changed (file:lines), the exact printed line, tests touched. Rejected by the arbiter (do NOT touch): content-01 per-claim claim_support skeleton/dispositions (cosmetic), content-03/04 receipt refusals (burden), content-05 zh counter (R12), fidelity-02 second live pass (burden), pdf-01 render-time replay (burden), fidelity-03 registrable domain (cosmetic), fidelity-05 short-segment (R13), ledger-03 CJK floor (cosmetic), ledger-04 drift (R24), ledger-12 claim 20-field list (R28), ledger-14/16 (cosmetic), commits-2-15, ledger-01 date-granularity (A5), records-02 Han numerals (user ruling R26/C26), records-05 humanization family (never emitted), records-01 sample unit.

### §W.A — worktree `wt/recA`, files: `scripts/alx.py`, `tests/test_alx.py`, `tests/test_gate_errors_doc.py`, `references/gate-errors.md` (review section only)
- A1 (flowmap-04) `_verify_receipts_in_process` argv (HEAD ~3917–3945): add `--min-sections 3` (baseline 1edb929:SKILL.md §8 flags). Print-only: the resulting `[FAIL]` line lands in delivery notes as today; `issue` still exits 0 and writes receipts.
- A2 (content-07) `cmd_render` validate_pdf call (HEAD ~4192): `min_text_chars=5000` for every language (baseline `--min-text-chars 5000`); print-only as today.
- A3 (ledger-05) `cmd_claim_add`: when an input claim omits `kind` or `importance`, print a WARN (family `ledger/claim-input`, severity warn, Class A) `C3: kind not given (recorded as fact); importance not given (recorded as supporting)` (adapt the wording to the actual CLAIM_DEFAULTS); the claim is still accepted, exit 0. Source of the rule: 4a1825c:references/claim-input.schema.json `required` (adapted from schema HARD to WARN).
- A4 (commits-3-01) `_normalize_claim_input` (HEAD 1939–1961): before stripping unknown keys, collect them; `cmd_claim_add` prints WARN `C3: unknown fields ignored: importnace, caveats` (family `ledger/claim-input`, warn); still strips and accepts. Rewrite `test_claim_add_drops_unknown_keys` to expect the line + acceptance. Source: d85add1 raw path + claim-input `additionalProperties:false` (adapted from HARD to WARN).
- A5 (records-04) `freshness_findings` (HEAD ~3177–3180 `return []` when not finished): restore the missing branch from ca47493:scripts/alx.py:2949–2951 / 8ddee2e — `finding(_review_family(kind, "missing"), f"the {kind} review is missing.", fix=f"alx review start {kind}")` for each REVIEW_KIND not finished; register `review/content-missing` in OWN_FAMILIES and WARN_FAMILIES (severity warn); `review/rewild` keeps its stale clause. Restore the `review/content-missing` section and the missing clause of `review/rewild` in gate-errors.md from 0323cd0:references/gate-errors.md (lines ~333–342), marked W. Invert tests/test_alx.py:4998–5013 and tests/test_gate_errors_doc.py:85. `check`/`issue` still exit 0.
- A6 (records-06) `cmd_status`: reinsert d85add1:scripts/alx.py:4072–4077 (snapshot yes/no; per-kind review finished|pending) after the claims line (HEAD ~4236), using `latest_snapshot()` and `state["reviews"][kind]["finished"]`; `_next_command` untouched. Flip `test_status_prints_the_phase_checklist`.
- A7 (alx-late-02) `cmd_init`: reinsert d85add1:scripts/alx.py:1301–1302 (origin given|inferred) and 1361–1369 (archetype/language echo + how to change) after "Workspace ready" (HEAD ~1379), without the `--subject-status` suffix and without any refuse. Flip InitEchoTests (tests/test_alx.py ~4103–4138).
- A8 (flowmap-07) `_online_findings` (HEAD 3805–3839): every sampled check with status unreachable/undecodable becomes a `fidelity/{status}` finding (Class A, warn) — drop the `and beyond_quorum` condition; append the "past the 25% quorum" sentence only when beyond. Receipt policy, `force=True`, delivery note untouched; `issue` exits 0. Source: 1edb929 printed every unverified sampled check (adapted from refuse to WARN).
- A9 (fidelity-04, alx half) `FETCH_TIMEOUT_SECONDS = 20` (HEAD alx.py:362; baseline 1edb929 source_fidelity default 20).

### §W.B — worktree `wt/recB`, files: `scripts/validate_ledger.py`, `references/evidence-ledger.schema.json`, `references/content-review.schema.json`, `references/gate-errors.md` (ledger sections), `tests/test_validate_ledger.py`, `tests/test_repository_contract.py`, `tests/test_resilience_rules.py`, `tests/test_gate_errors_doc.py`
- B1 (ledger-06) `_reference_findings` (HEAD ~2663–2671 keeps estimate→assumptions): add the analysis twin — `kind == "analysis"` with empty `reasoning` → WARN in the same family and message style as the estimate check, fix `set field reasoning in claims/*.json, then alx claim add claims/*.json`. Source: 4a1825c:references/claim-input.schema.json allOf[0] (adapted HARD→WARN at check). Restate 01eb2db tests (analysis without reasoning: accepted + WARN). Do not restore allOf[2–3] (R25).
- B2 (ledger-08) `references/evidence-ledger.schema.json`: restore `minItems: 1` on top-level `coverage`, `sources`, `claims` from 4a1825c (verbatim). Effect at HEAD: `ledger/schema` WARN with schema_remedy. Restate tests/test_repository_contract.py:256–257.
- B3 (ledger-09) `$defs.synthesis`: restore 4a1825c `required` (8 keys) and `minItems: 1` on central_judgment_claim_ids / adversarial_tests / implications / decisions_or_takeaways; KEEP HEAD union types (limitations `[array,string,null]`) and `additionalProperties: true`. Restate test_repository_contract.py:255 and any pin on `required: []`.
- B4 (ledger-10) `$defs.coverageItem`: restore 4a1825c `required` (area, question, priority, decision_relevance, completion_criteria, status, claim_ids, gap_impact) and its `allOf` (supported|disputed ⇒ claim_ids minItems 1; gap ⇒ claim_ids maxItems 0 + gap_impact minLength 1); KEEP `additionalProperties: true`.
- B5 (ledger-11) `$defs.brief`: restore 4a1825c `required` (intended_reader, decision_or_use, archetype, report_language, editorial_mode, scope); KEEP HEAD scope type `[object,string,null]` (no structured-scope subfields required) and `additionalProperties: true`.
- B6 (ledger-13) `$defs.implication`, `takeaway`, `scenario`, `adversarialTest`: restore 4a1825c `required` lists and non-null string types for statement/hypothesis/test/result (verbatim), KEEP `additionalProperties: true`.
- B7 (ledger-15) `references/content-review.schema.json`: restore `minLength: 20` (4a1825c) on score.rationale, sectionReview.purpose/new_value/evidence_or_reasoning/limitation_or_tradeoff/contribution_to_governing_question, visualAsset.visible_text_and_claims_review. If validate_ledger's prose-floor doubling (HEAD ~3381–3399) would then make Latin 40, exempt content-review fields from doubling there so Latin stays 20 and CJK 20. Report whether scripts/alx.py:3083–3089 (finish path; NOT your file) needs the same exemption.
- B8 After B2–B6, run `alx check` on a copy of `scratchpad/fixtures/glm-run` (copy it first; run with `"$HOME/.alexandria/runtime/bin/python" <worktree>/scripts/alx.py --dir <copy> check`) and paste the WARN lines the schema now produces; every one must carry a `set field … via alx ledger merge` / `in ledger.json` fix and none may be HARD. Fresh `init` + `check` must exit 0.

### §W.C — worktree `wt/recC`, files: `scripts/content_gate.py`, `scripts/source_fidelity.py`, `tests/test_content_gate.py`, `tests/test_source_fidelity.py`, `tests/source_fidelity_transport.py`, `references/gate-errors.md` (content/check section only)
- C1 (content-02) `scripts/content_gate.py`: delete the `_BINDING_EXCEPTIONS` filter (HEAD 100–104, 516–518) so `run_check` re-emits validate_report_against_ledger's "cannot be located" and "has no nearby citation" strings as `content/check` WARN (as at 4a1825c:scripts/content_gate.py run_check). Keep the WARNING_PREFIX strip; do not restore `_per_claim_binding_findings`. Rewrite the two tests that pin the silence; fix the R29 sentence at gate-errors.md ~381. `alx check`/`issue` still exit 0.
- C2 (fidelity-04) `scripts/source_fidelity.py`: `DEFAULT_TIMEOUT_SECONDS = 20`, `DEFAULT_FETCH_ATTEMPTS = 3`, `fetch_document` default timeout 20 (baseline 1edb929:scripts/source_fidelity.py:52–53); retarget the two tests that assert 2 attempts; rewrite the T1 comments that encode 10s/2. No deadline abort, no batch stop.

## §W (continued) — wave 2 (arbiter decisions on §V verdicts for batches 6–10)
Same rules as §W above. Rejected (do NOT touch): refs-01 must-record list (R28), refs-10 people inventory line (cosmetic), refs-12 late rerender (burden), refs-06 receipt cascade (burden), rewild-10 effective snapshot (R11), rewild-15 source-hash restamp (R11), skill-10 renderer switch (burden), commits-1-03 render refusal (burden), orch-01 (already at SKILL.md:13), flowmap-01 as an alx call (prose pointer only, orchestrator-owned). SKILL.md is orchestrator-owned: no worker edits it.

### §W.D — worktree `wt/recD`, files: `references/content-quality.md`, `references/research-protocol.md`, `references/evidence-recording.md`, `references/pdf-templates.md`, `references/rewild-gate.md`, `references/rewild/rewild/SKILL.md`, `references/rewild/rewild-zh/SKILL.md`, `references/rewild/rewild-hk/SKILL.md`, `tests/test_repository_contract.py` (reference-file pins only; never the SKILL.md pins)
- D1 (refs-03) `content-quality.md` §13: insert the block `git show 0323cd0:references/content-quality.md` lines ~315–342 — the 1–5 rubric table (what a 4 means per dimension), "Every dimension must reach 4 before production" (keep as the review standard; HEAD's "never blocks issue" line stays right after it), the substance-not-conformity paragraph, and the three writing-clarity censuses (closing-sentence share, section-shape count, section-length spread) with their score caps. Do not restore Class F, the visual_assets fails-production sentence, or any clock.
- D2 (refs-02) `content-quality.md` ~248: replace HEAD's "wherever the evidence supplies it" sentence with the 0323cd0:248 floor sentence (one named human specific per roughly 2,000 English words / 1,500 non-whitespace Chinese characters, never fewer than three), keeping the four item types and the do-not-invent / record-a-gap sentences.
- D3 (commits-2-19) `content-quality.md` ~321: delete the sentence "The content review is optional; run it only when more than 25 minutes remain." so the runbook matches SKILL.md ("Every report passes Rewild and the content review"); keep the "never blocks issue" sentence. Report this deletion explicitly (it is a clock rule).
- D4 (refs-07) `research-protocol.md` ~155–161: restore the 8efa698 two-exit derived/estimate passage (a derivation must excuse something; more than two derived_assertions ⇒ split the claim; `kind: estimate` carries `assumptions`, which the check names) over HEAD's one-exit/optional wording. Nothing else in that file.
- D5 (refs-09) `evidence-recording.md`: after HEAD's four-field claim-add contract, splice from `git show 0323cd0:references/evidence-recording.md` lines 3–13 the recording checklist — public HTTPS URL, publication and access dates, `undated_reason` for a living undated source, evidence type / source family / role / independence, and `kind` — as what a complete entry records (the gate still asks only for the four fields; say so). Do not restore harm-review bullets (8efa698) or the people paragraph.
- D6 (pdf-08) `pdf-templates.md` after HEAD ~82: insert, scoped to a user-supplied cover image, the 1edb929:94–96 clause "verify source, resolution, relevance and rights" and the sentence "Never use … an image that implies evidence the report does not contain" (verbatim wording). No Google/Bing hunt, no generator requirement.
- D7 (refs-08) `rewild-gate.md` ~40: append the 0323cd0 clause "; never skip the second pass".
- D8 (rewild-05, rewild-06) the three bundled Rewild `SKILL.md`: restore the invoke-and-disposition step from 1edb929 (en :104–108, zh :103–106, hk :172–175, hk derived-figure region :264) in place of HEAD's "do not run the checker; alx check will print" step, adapted: the command runs `"$ALEXANDRIA_PYTHON"` (fall back to `python3` only when unset) and writes the rewrite/source files under the workspace `$WORK`, not `$SKILL_ROOT`; style findings = suggestions; fidelity findings = defects (change the rewrite, never justify them); Hong Kong region findings = defects for the writer; derived figures: annotate and continue; retarget the dead hk `步驟 9` pointer (~:261). Omit the "rerun until exit 0" loop. Keep one sentence saying `alx check` prints the same families afterwards.
- D9 `tests/test_repository_contract.py`: restate only reference-file pins your edits break; do not restore the 40-character excerpt pin (commits-1-09 rejected); do not touch SKILL.md pins.
Acceptance: suite green, ruff clean, each inserted passage quoted with file:line in the report.

### §W.E — worktree `wt/recE`, files: `scripts/rewild_gate.py`, `scripts/gate_severity.py`, `references/rewild/rewild/scripts/naturalness-check.py`, `references/rewild/rewild-zh/scripts/naturalness-check.py`, `references/rewild/rewild-hk/scripts/naturalness-check.py`, `tests/test_rewild_gate.py`, `tests/test_rewild_receipt.py`, `tests/test_gate_severity.py`, `references/gate-errors.md` (rewild/fidelity sections only)
- E1 (rewild-01) `_run_rewild_checker` (HEAD ~1466–1478): feed the bundled checker `_checker_prose(text)` (as 1edb929 `run_gate` wrote `_checker_prose`) instead of `_style_prose`; the empty-input skip fires only when `_checker_prose` is blank. Check `git diff 1edb929 HEAD -- references/rewild/rewild/scripts/naturalness-check.py` for a `strip_markdown` change that drops quoted spans; if 3ed1525 introduced it, restore the 1edb929 form in all three copies. Keep `_quotation_findings`/`_integrity_findings`; fidelity/rewild stays the only HARD family. Invert StyleMaskingTests.
- E2 (rewild-02) `_fidelity_prose` (HEAD ~930–933): `return _checker_prose(text)` (1edb929:897 / 3ed1525:921); invert the mask pin test; update the docstring and gate-errors.md ~278; fidelity/semantic stays WARN.
- E3 (rewild-03) `_clauses` (HEAD ~579–592): restore the 1edb929:569–576 intra-sentence splits (EN `[.!?;,\n]+|while|whereas|but`; CJK `[。！？；，、,\n]+`) while keeping HEAD's terminator handling; neutralize commas/semicolons inside citation/link labels before splitting so `test_citation_with_a_comma` stays green; revert `test_identical_clauses` pairing to the 1edb929 expectation; semantic stays `warning()`.
- E4 (rewild-04) `_mask_quoted_spans` (HEAD ~936–948): mask an ASCII "…" span only when its inner length ≥ 4 (the aaaac97 floor) by post-filtering the matches; keep the regex, the pairing comment and `_quotation_findings` unchanged.
- E5 (rewild-07) the three `naturalness-check.py` (byte-identical): restore the 1edb929 `check_vocabulary` pass condition `len(hits) <= 1 and hits[0][1] <= 1` and delete `VOCAB_BUDGET_SPAN` / `vocabulary_budget`; the family stays WARN; drop or rewrite `test_ai_vocabulary_budget_scales_with_length`.
- E6 (rewild-11) `gate_severity.py` `emit_compact` (HEAD ~71–123): for families `fidelity/semantic`, `rewild/region`, `rewild/ai-vocabulary`, `rewild/length` print every message in full (as `ledger/schema` is printed today); other WARN families keep the compact form; exit codes unchanged; test.
- E7 (rewild-13) `run_check`: pass `exemptions=[]` into `_semantic_fidelity_errors`; when `len(exemptions) > MAX_HEURISTIC_EXEMPTIONS` append `_finding("fidelity/semantic", <run_gate's exceed-limit string>)` as WARN — no early return. Reuse the 9-split fixture against `run_check`.
- E8 gate-errors.md: keep the rewild/fidelity sections truthful for E2/E7; tests/test_gate_errors_doc.py green.
Acceptance: suite green, ruff clean; on a copy of `scratchpad/fixtures/glm-run` run `alx check` (managed runtime, `--dir <copy>`) before and after and paste the rewild/fidelity WARN lines that changed; exit code must stay 0.

### §W.F — worktree `wt/recF` (base: main after recA/recB/recC), files: `scripts/alx.py` (`cmd_render` and its helpers only), `scripts/render_pdf_pages.py`, `tests/test_render_pdf_pages.py`, `tests/test_alx.py` (render tests only), `references/pdf-production.md` (the PDFKit / backend paragraph only)
- F1 (pdf-06) Baseline 1edb929 `render_pages` auto = PDFKit on macOS with no fallback (a failure was visible). HEAD `AUTO_FALLBACK` tries pdfkit → pdfium → poppler and discards `failures[]` when a later backend succeeds, and `alx render` never asks for pdfkit. Recover the visibility, not the refusal: on darwin `cmd_render` requests the PDFKit backend first for each contact sheet; when PDFKit fails it prints `<template> contact sheet: PDFKit failed (<reason>); rendered with <backend>` (reuse the existing note path near alx.py ~4207–4217), renders the sheet with the next backend, keeps the PDF, returns 0. Off darwin: unchanged. `render_pages` (or a thin wrapper) must expose which backend rendered and the per-backend failure reasons instead of dropping them; keep the CLI `--backend auto` fallback tests green. Fix the pdf-production.md paragraph that still describes PDFKit-mandatory auto so it states this behaviour (CLI auto fallback ≠ the alx Preview path). Test: darwin path with a stubbed failing pdfkit prints the line and still writes the sheet via the next backend; a stubbed succeeding pdfkit prints nothing extra.

## §X severity re-examination (R33)
USER (09-16, verbatim key lines): "Some rules are hard rules, and some rules can be just warnings or reminders. I want you to examine all the warnings, all the non-blocking warnings. Are they okay as warnings, or are they the standard, the principles that cannot be violated? Should we recover anything to be blocking? If a gate is blocking, it means it has a super big impact on the quality … If the quality impact is small, for instance the quality score goes from 93 to 92 to 91 to 90, a one or two points drop can be just a warning. But if a mechanism can impact the overall quality by more than five points, it should be blocking because the impact is big enough … maybe a mechanism can bring down the score from 93 to 83. That's 10 points. By definition that is a big difference. Then it should be blocking, and we should insist … I want the quality gates that really have a bigger impact to still be blocking. I want the minor quality gates to be just a warning."
Still standing: the clock never decides; no living/dead rule; the Han-numeral 三 rule (C26) and R13 short-segment stay non-blocking; `init`, `fetch`, `claim add` accept lenient input (an unfetched source or a missing extract stays HARD at claim add as today).

Tiers (the only three): HARD-drop = at `issue` the offending claim, paragraph or edit is removed or restored so it never ships; delivery continues; the removal is printed and noted. HARD-refuse = `issue` (or `render`) stops with exit 1 and the fix list; nothing is written until fixed; used only for whole-report defects that no drop can cure. WARN = printed with a fix; never changes the deliverable.

Inventory: `scratchpad/sev/inventory.json` (56 finding families from `references/gate-errors.md`, which defines what each catches, its message and fix; plus 4 flow gates G1–G4). Read the code for behaviour: `scripts/alx.py` (`adopt`, `_drop_hard`, `cmd_issue`, `cmd_render`, `_verify_receipts_in_process`), `scripts/validate_ledger.py`, `scripts/content_gate.py`, `scripts/rewild_gate.py`, `scripts/source_fidelity.py`, `scripts/validate_report.py`, `scripts/gate_severity.py`; SKILL.md for the path.

### §X.1 rater task (read-only; one batch of gates in the prompt; three lenses run independently)
For EVERY gate in your batch answer: (a) `defect`: the concrete defect the gate catches, one sentence, with an example line from a real report; (b) `impact_points`: how many points a report loses on the user's 100-point quality scale when that defect ships uncaught — a number, with two sentences of reasoning about what the reader loses (fabricated fact = the report's credibility; wrong direction = wrong decision; a missing bookmark = a reader inconvenience); (c) `false_positive_risk`: low|medium|high with evidence from the code or the GLM field-test history in `docs/orchestration/ledger.md` (a gate that fires wrongly and would then drop good content or refuse a good report is dangerous as HARD); (d) `tier`: HARD-drop | HARD-refuse | WARN under the rule (≥5 points ⇒ HARD unless false positives are frequent; ≤2 ⇒ WARN; 3–4 ⇒ judge and say why), with the drop remedy named for HARD-drop (what exactly is removed/restored) or the refusal condition for HARD-refuse; (e) `user_ruling`: none | which earlier ruling touches it and whether the new rule overrides it. Lens Q (reader and decision value): rate from the reader's seat. Lens I (integrity and fabrication): rate from the evidence chain. Lens E (engineering): rate feasibility and false-positive risk from the code and the field-test history; for each HARD you propose, name the code site that would implement it.
Output — the final answer is ONLY this JSON: {"rater":"<name>","lens":"Q|I|E","ratings":[{"id":"<gate id>","defect":"…","impact_points":<number>,"false_positive_risk":"low|medium|high","fp_evidence":"…","tier":"HARD-drop|HARD-refuse|WARN","tier_reason":"…","remedy":"…","user_ruling":"…"}],"notes":"…"}

### §X.2 tier table (arbiter, from 18 ratings; `scratchpad/sev/ratings.txt`) and implementation contract
Rule applied: ≥5 points ⇒ HARD unless the detector misfires often (a misfiring HARD deletes or blocks good work, a quality loss of its own); ≤2 ⇒ WARN; 3–4 ⇒ judged. Three tiers as in §X.

HARD-refuse (new — `issue` prints `=== BLOCKED (fix, then alx issue again) ===` with the fix line for each item, writes no receipts and no issue.json, returns 1; `render` runs issue first and returns 1 without writing a PDF while issue is blocked; the clock never lifts a block):
- B1 `ledger/quantity` — an Arabic-digit figure, percentage, currency or date in a claim that no extract or cached page of its sources carries (8/6/8 points). Han numerals stay silent (R26/C26). Fix: `alx find S<n> <figure>` then correct the claim or extract, or `alx claim drop C<n>`.
- B2 `fidelity/semantic` — Rewild reversed a direction, negation or causal link between the snapshot and the report (8/8/7). Fix line names both clauses. (Split-remnant over-cap message stays WARN.)
- B3 `content/critical-finding` — the content review lists a critical finding with no fix recorded (8/6/8). Fix: fix the report and record the disposition in reviews/content.json, then `alx review finish content`.
- B4 no snapshot at `issue` — the Rewild step never started (4/1/6; false positives none). Fix: `alx snapshot`, humanize per the bundled Rewild skill, `alx check`, then `alx issue`. A snapshot that exists with an unchanged report stays a WARN.
- B5 report far below the length floor — `integrity/length` below two thirds of the floor (en < 5,000 words; zh-CN/zh-HK < 3,333 non-whitespace chars) (reader gets half the promised depth). Between two thirds and the floor stays WARN. Fix: deepen (research, counterevidence, implications), never pad.
- B6 broken PDF at `render` — no PDF file produced by any template, or a produced PDF whose extractable text is under 500 characters (image-only or empty file) (12 points; false positives none). Below 5,000 but ≥500 stays the WARN line. `render` returns 1 and says which template failed and why.
- B7 `integrity/encoding` — the report is not valid UTF-8 and no snapshot restores a valid one (already HARD; the restore stays the first remedy, the refuse is the fallback).

HARD-drop (unchanged set; three defects fixed so HARD really drops): `runtime/missing` (refuse at launch as today), `ledger/claim-input`, `ledger/derived` (fabrication half), `ledger/reference` (unfetched source id half), `integrity/control-chars`, `integrity/replacement-char`, `integrity|fidelity/quotation-lost`, `binding/link-not-in-ledger`, `binding/leftover-prose` (D1: today the finding has no claim ids and `_drop_hard` never removes the block — `issue` must delete the leftover block through the mechanical-deletion path and say so), `fidelity/mismatch`, `fidelity/cache-missing`, `fidelity/cache-detached` (D2: today the finding carries only the source id, so nothing drops — attach the citing claim ids so the drop applies), `fidelity/rewild`.

WARN (everything else). Named decisions: `ledger/direction` (8/8/8 points) stays WARN only because the lexical detector misfires often (higher|rose|growth); it is the first candidate for a better detector. `ledger/freshness`, `ledger/status`, `content/score`, `review/*`, `rewild/*`, `content/check`, `fidelity/context-changed`, `binding/*` (non-HARD), `integrity/structure|date-line`, `ledger/*` bookkeeping, `tooling/*`, G2 sections, G4 receipts: WARN.

### §X.A — worktree `wt/sevA`, files: `scripts/alx.py`, `scripts/validate_report.py` (only if the length numbers are not reachable from alx), `tests/test_alx.py`, `tests/test_resilience_rules.py`, `tests/test_gate_errors_doc.py` (if the doc test derives tiers from modules)
- Implement HARD-refuse: a `REFUSE_FAMILIES` set (`ledger/quantity` for Arabic-digit/date findings, `fidelity/semantic` direction/negation/causality findings, `content/critical-finding`) plus the flow conditions B4 (no snapshot), B5 (length under two thirds of the floor, from the `integrity/length` numbers), B7 (encoding after a failed restore). In `cmd_issue`, after `_drop_hard` and the receipt phase, collect the refusals; if any: print the BLOCKED block (grouped, with the fix lines), write no receipts and no issue.json (leave existing receipts untouched), return 1. `cmd_render`: when its automatic issue is blocked, print the same block and return 1 without writing a PDF. B6: `render` returns 1 when no template produced a PDF or the produced PDF's extractable text is under 500 characters; the `<template> PDF check:` lines stay for the rest.
- `status`: `Next:` names `alx issue` with "blocked: <n> items" when the last issue was blocked.
- D1/D2 above: make `binding/leftover-prose` and `fidelity/cache-detached` drops real.
- No clock condition anywhere in these paths. `claim add`, `fetch`, `init`, `check`, `ledger merge`, `review finish` keep exit 0 as today (check prints the would-be BLOCKED items under its HARD header so the model sees them early).
- Tests: one per refuse family/condition (issue returns 1, prints the BLOCKED block, writes nothing; after the fix issue returns 0), render blocked, render broken-PDF, leftover-prose actually deleted, cache-detached actually dropped, length two-thirds boundary. Restate the R28/R29 pins that assert "issue never refuses" for exactly these cases and nothing else. Suite green, ruff clean.

### §X.B — worktree `wt/sevB`, files: `references/gate-errors.md`, `references/content-quality.md`, `references/rewild-gate.md`, `references/research-protocol.md`, `references/evidence-recording.md`, `tests/test_gate_errors_doc.py` (doc side), `tests/test_repository_contract.py` (reference pins only; never SKILL.md pins)
- `gate-errors.md`: tier letters become F (HARD-drop), B (HARD-refuse), W; the header/legend explains the three tiers in the §X wording; each B family/condition gets its section with message and fix as in §X.2 (B4–B6 as flow sections under `issue`/`render`); the `ledger/direction` section says it warns because the detector misfires and names the candidate improvement; the R28/R29 sentences "never blocks issue/render" are corrected wherever they now contradict the table (quote each change in the report).
- `content-quality.md` §13: "A missing or stale content review is a warning and never blocks `alx issue`" stays; add that an unresolved critical finding blocks `alx issue` until its fix is recorded.
- `rewild-gate.md`: semantic reversals (direction, negation, causality) block `alx issue`; fabricated figures restore the snapshot (as today).
- Keep every other sentence. Suite green, ruff clean.
