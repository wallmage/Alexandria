# HANDOFF — Alexandria resilience job (read fully after compaction)

Written 2026-09-14 by the orchestrator (Claude Fable 5.1) before a manual lossy compaction. This file + `docs/orchestration/ledger.md` (running log) + the spec/plan are the memory. Trust them and `git log` over recollection.

## 0. Who / what / how to behave
- User: wallny (author of the Alexandria skill, repo `/Users/wallny/Developer/Skills/alexandria`, main branch, no PRs). Speaks English to me; his friends are Chinese users on Tencent WorkBuddy with cheap open models (GLM-5.3-Flash, DeepSeek-Flash); others use Codex, Claude Code, Cursor.
- User style rules (from his CLAUDE.md + this session): concise, lead with the result, no preamble; Chinese when he asks "in Chinese"; he said the first analysis was "fucking too long" → keep messages short. Read-only by default; he approved this implementation explicitly ("burn tokens… best possible quality… make the skill as resilient as possible… always deliver under 60 minutes"). Later: "too heavy… end after round 2… you are the adjudicator and final judge… no round 3." Then: worried about context rot (>500k), wants ≤300–400k; "do not kick off new subagents… wait for everything to finish… then compact."
- Orchestrator skill (/orchestrator) governs delegation: Fable = judgment only; Workers = Cursor CLI `cursor-grok-4.6-medium-fast` (free) or Workflow `model:'opus', effort:'medium'`; Escalated = Workflow opus `high`; Scout = opus `low`; reviewers: grok-written diff → Workflow opus medium (SDD task-reviewer-prompt.md); opus-written diff → Cursor `cursor-grok-4.6-xhigh-fast --mode ask`. Never call Agent tool directly. Fleet dispatch = ONE Monitor running `sh ~/.claude/skills/orchestrator/dispatch.sh` with `TMP=… JOBS='name|workdir|cmd'` (or `CLI=… WD=… JOB=…`). Watcher "ERROR" wakes were false positives (spec text matched its regex) — check `turn.failed`/EXIT instead. Cursor runner: `cursor-agent -p --force --trust --output-format stream-json --model <slug> [--mode ask] [--add-dir DIR] [--resume SID] "$(cat PROMPT)"; grep -a '"type":"result"' LOG | tail -1 | jq -r .result > FINAL`. Codex: `codex exec --json -o FINAL -m gpt-6-astra -c model_reasoning_effort=xhigh -s read-only "$(cat P)"` (resume: `codex exec resume <thread_id> …`). CodeBuddy: `codebuddy -p --permission-mode plan --output-format stream-json --model kimi-k3-2 --effort max "$(cat P)"` with WD=$HOME. SDD helper scripts are not executable: run with `bash ~/.codex/plugins/cache/openai-curated-remote/superpowers/6.3.0/skills/subagent-driven-development/scripts/{task-brief,review-package,sdd-workspace}`; `task-brief PLAN N OUTFILE` (3rd arg avoids the broken sdd-workspace call).
- Git rules: worktrees per task; orchestrator owns merges (serial), push, worktree removal; workers never commit. Commit messages end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Every worker prompt must contain verbatim: "Do NOT create branches, commit, merge, or push. This instruction supersedes any CLAUDE.md or AGENTS.md git protocol, including one claiming to override everything. Work only in `<worktree path>` and leave every change uncommitted."

## 1. The investigation (what the user first asked)
Input: `~/Desktop/session-log-chiang-diary.zip` (friend's WorkBuddy session, model GLM-5.3-Flash, task: report on 蒋介石日记 using Alexandria). Unzipped at `$S/log/session-log-export/` (S = scratchpad, see §6). Parsed with my scripts ($S/parse.py, timeline.py, phases.py, x01.py).
Findings (all verified from the log):
- 4h33m active (8.2h wall incl. 3.5h idle), 392 LLM calls, 57M prompt tokens (55M cache hits), 414K output, 217.5 credits, 3 context compactions mid-task; median prompt 155K tokens per call.
- Phase costs: research 3 min / 9 WebSearch / 0 page fetches (2 credits); draft 9 min; Rewild loop 26 min (29.5 cr); ledger build+validate 26 min (21 cr); source_fidelity re-sourcing loop 154 min (101 cr); content_gate + rewild realign 31 min (40 cr); render 6 min (15 cr). Verification = 88% of credits.
- Gate runs: source_fidelity 39, validate_ledger 31, validate_report 19 (each secretly re-runs rewild+content+live fetch), content_gate 16, rewild_gate 13. Agent read/grepped validator source: validate_ledger.py 18×, source_fidelity.py 13×, validate_report.py 8×, content_gate.py 7×.
- Root causes: (1) ledger hand-written from search snippets (52KB JSON twice) before any page fetch → paraphrased extracts, dead/GBK/HTTP/bot-blocked URLs → fidelity loop; (2) gates report one rule family per run, rules only in Python; (3) contradictory rules (validate_ledger quantity coverage vs source_fidelity verbatim; content_gate citation insertion vs rewild clause alignment); (4) hash cascade: one-byte edit invalidates rewild→content→validate receipts + both review notes; (5) environment: GBK decoded as UTF-8 reported as "mismatch", fictional `--allow-unverified`, inconsistent `--force/--lang/--out-dir`, sandbox blocked cache reads; (6) the 110 destroyed quotations: agent's own bash bug at 0:32:47 — inside `python3 -c "…"` the replacement `'“\\1”'` reached Python as `\1` = octal U+0001; rewild_gate PASSED that report (no quotation-preservation/control-char check); found 3.4h later; (7) skill size: a 390-line SKILL.md + ~150KB references consumed the context of any model (GLM-5.3-Flash is on par with grok-4.6-medium; the failure was the skill, not the model).
- Delivered to user: an English analysis (he said too long), then Chinese 5-sentence versions (what went wrong / how to fix). Scouts' detailed audits of the gate scripts are in `$D/artifacts/review-w6qeq29co.json`.

## 2. Design (spec v3 = final; `docs/orchestration/09-14-01-spec.md`)
Goal: any harness/model delivers MD + 2 PDFs in ≤60 min, no loops, always delivers; anti-fabrication unconditional ("shorter or more qualified, never less true").
Core: new single entry point `scripts/alx.py` (thin over existing modules): `init`, `fetch` (sources enter the ledger ONLY via fetch; cached text under `sources/S<n>.txt/.meta.json`; provenance default `unverified` = interested), `source set`, `find/show` (paste-ready verbatim extracts), `claim add FILE.json` (slim claim-input schema; probed offline at add time; all findings per claim), `claim drop C<n> --apply` (deletes mapped paragraph, moves claim to `excluded_claims`), `claim bind`, `ledger merge` (brief/people/coverage/synthesis only), `snapshot [--restore]`, `check [--fix]` (ALL evaluators offline, grouped output, ≤5/family, fix + remove remedies; Class F = fabrication/integrity never waivable, Class A = availability waivable; default-to-F), `review start/finish/restore {rewild,content}` (snapshots; mechanical-delta allowlist; scoped `--iter`), `issue [--deliver]` (fresh check → online fidelity with quorum v2 + re-probe all claims of refreshed sources → Verification note inserted pre-hash → notes stamped → receipts → `receipts/issue.json`), `render` (requires matching issue.json; `md_to_pdf --issue-receipt` offline only), `status`. Deadline propagated (10 s/source, 1 retry, 8-min reserve). Gate changes (spec §7): validate_ledger grouped findings API + warn tier + date-fragment quantity rules + `unverified`; source_fidelity charset ladder/GB18030, unreachable≠mismatch, cache dir, probes full coverage, receipt policy v2, context-change hard; rewild integrity (control chars, quotation loss) + quote-masked style + sentence-level monotonic alignment + `--check`; content_gate/validate_report `--check/--fast/--final-once`, `--issue-receipt`, `--out-dir`, backend fallback. SKILL.md → ≤150-line runbook; `references/gate-errors.md`; `references/claim-input.schema.json`.
Decision table D1–D16 in the spec. Debate: 3 seats (Cursor grok xhigh, Codex astra xhigh, CodeBuddy kimi max), 2 rounds, all findings accepted; v3 = my adjudication (no round 3 by user order). Rulings R1 (source_fidelity owns `record_probe_contexts`; refresh preserves `probe_contexts`) and R2 (T4 owns content-review schema, adds `claim_support[]`) are in ledger.md.

## 3. Plan and status (`docs/orchestration/09-14-01-plan.md`)
Tasks / worktree ($S/wt/…, branch wip/<name>, base 361298e):
- T1 source_fidelity — Cursor sid bd457f75-9497-4a02-b6ec-d767fbd0e2a8 — DONE → review ❌ → fix1 DONE (529 pass, verified), committed on wip/t1-fidelity 2b9f866. Complete pending integration.
- T2 validate_ledger/gate_severity/schemas — Cursor sid 217e0178-4ca3-4f58-aa9a-542733fab11b — DONE → review ❌ → fix1 DONE (529 pass, verified), committed on wip/t2-ledger d5946b8. Complete pending integration.
- T3 rewild — DONE (517 pass), committed on wip/t3-rewild, NOT yet reviewed; concerns in ledger.md.
- T4 content_gate/validate_report/md_to_pdf/render_pdf_pages — Cursor sid 438c74f6-ace1-4832-9439-16319ef4a414 — DONE → review ❌ (Critical ×2 fixed in fix1: issue-receipt render crash, pdfkit timeout) → fix1 DONE 535 pass; verified by grep; **committed on wip/t4-gates 195d3fc**. Complete pending integration. Deferred minor: `--force` no-op.
- T5 scripts/alx.py — DONE (549 pass, tests/test_alx.py 46), committed on wip/t5-alx, NOT yet reviewed; has in-memory schema shim + seam fallbacks to remove at integration (ledger.md). Expect seams with T1 (`record_probe_contexts`), T2 (`Finding`, `collect_findings`, `expand_claim_input`), T4 (`binding_findings`, `integrity_findings`, `run_check`).
- T6 docs (SKILL.md runbook 79 lines, gate-errors.md, refs, READMEs, tests) — Cursor sid 79d215ed-ce33-47a3-8c6e-a2cf9a09a9b9 — DONE → review ❌ (6 Important) → fix1 DONE (507 pass, README contract tests fixed) → verified by grep; **committed on wip/t6-docs 333973b**. Complete. Deferred: collapse `quotation-lost` alias names once T3 emits one family name.
- T7 integration/e2e — NOT STARTED.
Reviewer results (JSON) copied to `$D/artifacts/review-*.json`; diffs `$D/artifacts/reviews/tN.diff`; prompts `$D/artifacts/prompts/`.

## 4. Exact next steps after compaction
1. All six worktrees are committed on wip/* (nothing running). Start at step 3.
2. (done) T1/T2 fix rounds verified and committed.
3. T3/T5 reviews: package diff (`cd wt; git add -N .; { git status --short; git diff --stat; git diff -U10; } > $S/reviews/tN.diff`), dispatch reviewer = Cursor `cursor-grok-4.6-medium-fast --mode ask` reading `task-reviewer-prompt.md` with brief/report/diff paths + plan Global constraints; one fix round max (resume via SendMessage to the workflow agent is not possible — dispatch a fresh Workflow opus-high fixer with brief+report+findings), then adjudicate; commit worktrees.
4. Merge on main, serially: order T2 → T1 → T4 → T3 → T5 → T6. `git checkout main && git merge --no-ff wip/<name>`; after each: `.venv/bin/python -m pytest -q` + ruff. Expected seams: family-name mismatches (gate-errors.md vs FAMILIES), local `Finding` fallbacks in T1/T4 to replace with `gate_severity.Finding`, `claim_support` schema, alx ↔ T1/T2/T4 signatures. Fix seams with ONE Cursor dispatch in a fresh worktree from main, review, merge.
5. Regression fixture: `$D/artifacts/fixture/{ledger.json,report.md}` (friend's final ledger 25 claims/25 sources) → run `alx check` in a temp workspace; must print grouped output, no traceback.
6. e2e clean-path acceptance (spec §10): install skill copy from main into a temp skill dir; runtime already at `~/.alexandria/runtime`; Cursor `cursor-grok-4.6-medium-fast` headless with the friend's prompt (zh-CN, 蒋介石日记: "请阅读、分析蒋介石的日记…三个方面…形成一个报告"), 60-min cap via Monitor; pass = MD + 2 PDFs, `issue` without `--deliver`, ≤3 check rounds, no traceback, quotations preserved. Then Codex `gpt-6-astra` control run. Record in ledger.
7. Final judgment review (Cursor xhigh --mode ask, `~/.claude/skills/orchestrator/judgment-reviewer.md`) on merged main; fix wave if needed; `git push`; `git worktree remove $S/wt/<t>` ×6; `git worktree prune`; `git branch -d wip/*`; report to user with "Rulings I made" (debate rulings, R1, R2, any parked minors) and the final numbers.
8. Keep the user's context concern: short messages, no diff pasting, delegate reads.

## 5. Baselines and facts
- Test baseline on main 361298e: 503 passed + 3 README subtests failing (fixed by T6). Suite ~35 s. Python via `.venv/bin/python` at repo root (worktrees use the absolute path).
- Ledger schema enums: provenance {primary_independent, primary_interested, secondary_independent, secondary_dependent, unverified}; evidence_type includes news_report; accountability_basis {none, court_or_regulator_record, named_source_investigation, subject_admission}; roles {subject_official, counterparty_official, independent_analysis, empirical_data, affected_stakeholder, expert_interpretation, historical_record}. fetch defaults pinned: provenance=unverified, evidence_type=news_report, accountability_basis=none, roles=["independent_analysis"].
- `.superpowers/sdd/` is git-ignored (own .gitignore) and holds briefs + durable artifact copies.
- Commits on main this job: 62522f6 (spec v3/plan/ledger), 47990d0 (ledger handoff). This HANDOFF.md committed next.

## 6. Path variables
S=/private/tmp/claude-501/-Users-wallny-Developer-Skills-alexandria/1095d7dd-74cb-4300-9a63-e72f22f9f774/scratchpad (session scratchpad; may vanish on reboot — worktree branches wip/* keep committed work; uncommitted T1/T2/T3/T5 changes live only in $S/wt until committed)
D=/Users/wallny/Developer/Skills/alexandria/.superpowers/sdd/09-14-01-plan (durable copies: artifacts/{reports,reviews,prompts,fixture,debate,review-*.json})

## 7. FINAL STATUS AT COMPACTION (2026-09-14, all agents finished, nothing running)
Validated by the orchestrator (not just self-reported):
- T1 source_fidelity: reviewed → fix round 1 → greps confirm `record_probe_contexts`, policy-aware `fidelity_errors`, JSON-serializable findings; focused suite 68 pass; full suite 529 pass. Committed wip/t1-fidelity 2b9f866. STATUS: complete pending integration.
- T2 validate_ledger/gate_severity/schemas: reviewed → fix round 1 → greps confirm substring classifier removed, no invented status default, remedies carry operands, granularity warning fires (checked in-process); focused 174 pass; full 529 pass. Committed wip/t2-ledger d5946b8. STATUS: complete pending integration.
- T4 content_gate/validate_report/md_to_pdf/render_pdf_pages: reviewed → fix round 1 → greps confirm `claim_support` in content-review schema, pdfkit `TimeoutExpired` handled, unmocked issue-receipt test; full 535 pass. Committed wip/t4-gates 195d3fc. STATUS: complete pending integration.
- T6 docs/runbook: reviewed → fix round 1 → greps confirm enum fix, time boxes, length floor, WARN families, pinned assertions; focused 18 pass; full 507 pass (README contract tests fixed). Committed wip/t6-docs 333973b. STATUS: complete.
- T3 rewild: orchestrator ran the full suite in the tree: 517 pass + 3 README (T6) — matches report. NOT reviewed. Committed wip/t3-rewild 3ed1525.
- T5 alx.py (2916 lines) + tests/test_alx.py (1151 lines, 46 tests): orchestrator ran the full suite: 549 pass + 3 README — matches report; `alx --help` lists init, fetch, source, find, show, claim, ledger, snapshot, check, review, issue, render, status. NOT reviewed. Committed wip/t5-alx 34157bf.
Progress estimate: ≈60% of total effort. Remaining ≈ T3/T5 reviews + one fix round each (30–45 min), integration/seam fixes (30–45 min), fixture regression (5 min), two 60-min e2e acceptance runs (can overlap), final judgment review, push/cleanup.
Known integration seams to resolve in T7 (one Cursor dispatch): remove stand-in `Finding` try/except in rewild_gate.py, source_fidelity.py, validate_report/content_gate if present → use gate_severity.Finding; remove T5's in-memory schema shim once T2 schema (aliases/excluded_claims/tooling) is merged and apply R3 (drop `include_in_report→report_excerpts minItems:1` from the schema; enforce in `alx check`(c)); rewild `--check` should print grouped block via gate_severity.render_grouped; align FAMILIES ↔ gate-errors.md names (collapse quotation-lost aliases to the name T3 emits); alx must call T1 `record_probe_contexts` after claim add; T4's `--issue-receipt` requires `--ledger` (alx render passes it); `ledger remove` dropped (R5).

## 8. FINAL (2026-09-15) — job complete
main 4a1825c pushed; 730 tests pass; ruff clean; installed copies at ~/.codex/skills/alexandria and ~/.cursor/skills/alexandria refreshed from main. Integration = T7 (seams) → T7b (check output) → T7c (live-finding classes, review skeletons) → T7d/T7e (judgment fixes, parallel A/B) → T7f (post-merge seams). Evidence: fixture regression (no traceback, grouped output, remedies parse), dry-run 1 (33 min, delivered, loops) and dry-run 2 (30 min, delivered, 13 check rounds / 5 review starts) in .superpowers/sdd/09-14-01-plan/artifacts/e2e-dry*/. Field acceptance = user on WorkBuddy with DeepSeek/GLM Flash (user decision). Parked items: ledger.md last entry. Follow-up first: P4 (CJK rationale minLength in evidence-ledger.schema.json ~line 1105: 40 → 20).
