# Orchestration ledger — alexandria

## User decisions (verbatim)
- 2026-09-14: "Make our scale as resilient as possible... always provide the best quality Deep Researched report under 60 minutes... Whatever happens, we will always deliver... Burn tokens if you need to. I just want the best possible quality." (skill = the Alexandria skill)
- Delegation rule: orchestrator skill (/orchestrator).

## Standing orders
- Git: worktrees for edits; merge to main; no PRs; orchestrator owns git.
- Baseline: 503 passed, 3 pre-existing README subtest failures in tests/test_repository_contract.py (from commit 361298e). PARKED unless fixed by the README task.

## Task log
- 09-14 spec v1 written: docs/orchestration/09-14-01-spec.md. Debate: 3 seats (>4h job).
- 09-14 debate r1: 3 seats all REVISE (cursor 8, codex 12, codebuddy 10 findings; all accepted) → spec v2. r2 launched (resumed sessions: cursor 15efc6ac…, codex thread 01a0a034-8e82…, codebuddy 01a0a034-8c37…).
- Pending v3 edit: schema already has provenance enum `unverified` → D13 uses it (no new enum). Pinned fetch defaults: provenance=unverified, evidence_type=news_report, accountability_basis=none, roles=["independent_analysis"]; validate_ledger must treat `unverified` as non-independent in every rule (test).
- Runtime installed at ~/.alexandria/runtime (install.sh exit 0, smoke PDFs OK). Fixture (friend's final ledger 25/25 + report) at scratchpad/fixture/.
- 09-14 debate r2: all seats REVISE (cursor 4 new, codex 3 new + 4 partial, codebuddy 5 new). USER: "end after round 2… you be the adjudicator… no round 3." Spec v3 = final adjudication.
  Rulings (v3): accepted — render requires passed issue (cursor#9/codex#1); claim drop --apply deletes paragraph + excluded_claims (codex#13); Verification note by issue pre-hash (codex#15/cb#11); refresh → re-probe all claims (codex#14); changed context never self-authorizing (codex#9); fetch keeps transport lock, same-domain redirects (cursor#12); https scope url-only (cursor#11); person status at init (cursor#10); claim-input conditional fields + schema additions (codex#2); content review stale/missing = Class F, rewild review = Class A (codex#5 partial); default-to-F + explicit A list (cb#13); issue step semantics (cb#12); D14 scoped (cb#14); missing snapshot handled (cb#15). Rejected — none. Cost if wrong: drop cascade may over-delete prose under time pressure (accepted: shorter, never less true).
  Plan: no plan debate (user). Orchestrator = judge.
- 09-14 execution: docs committed on main (62522f6). Worktrees (branch wip/<name>) under scratchpad/wt/: t1-fidelity t2-ledger t3-rewild t4-gates t5-alx t6-docs, all from 361298e. Briefs: .superpowers/sdd/09-14-01-plan/task-N-brief.md (git-ignored). Prompts: scratchpad/prompts/task-N.prompt.txt. Reports expected: scratchpad/reports/task-N-report.md.
  Dispatched: T1,T2,T4,T6 → Cursor grok-4.6-medium-fast (fleet monitor bf7my8fo7, logs scratchpad/fleet/tN.log); T3,T5 → Workflow opus high (run wf_e7197c21-43d). Task reviewers: grok-written → Workflow opus medium; opus-written → Cursor grok-medium --mode ask.
  Merge order plan: T2 (gate_severity.Finding) → T1 → T4 → T3 → T5 → T6; full suite after each; seams fixed by one Cursor dispatch (T7).
- T6: DONE (507 pass). Review (opus medium): spec ❌ 6 Important (invalid enum, time boxes, length floor, WARN families, weak assertion, README grammar). Fix round 1 (cursor resume): all fixed; scoped re-review done by orchestrator via grep + focused tests (18 pass). Task 6: complete (uncommitted in wt/t6-docs; deferred minor: quotation-lost alias collapse at T7).
- T1 DONE 524 pass; T2 DONE 526 pass; T4 DONE 527 pass — reviews running (wf_5fc86d92, wf_0c82cb2d, wf_6d6263fd). T3/T5 still implementing (wf_e7197c21).
- Reviews r1: T1 ❌ (Critical: policy v2 dead; Important ×7) → fix1 running (blu7n0f47). T2 ❌ (Important ×6: substring family classifier, dead granularity warn, remedies w/o operands, invented status/prose) → fix1 running (bu4398i0x). T4 ❌ (Critical ×2: --issue-receipt render crash, pdfkit timeout escapes; Important ×5) → fix1 running (bwhck52ja).
  Rulings: (R1) source_fidelity owns `record_probe_contexts(cache_dir, source_id, claim_id, probes)`; refresh preserves `probe_contexts`; alx claim add calls it. (R2) T4 owns references/content-review.schema.json and adds optional `claim_support[]`. Cost if wrong: seam rework at T7.

## HANDOFF (written 09-14 before manual compaction) — read this first on resume
Goal: implement spec v3 (docs/orchestration/09-14-01-spec.md) per plan (docs/orchestration/09-14-01-plan.md); orchestrator = judge (no more debate rounds, user decision). User wants context ≤300–400k: keep turns lean, delegate reads, do not paste diffs.
Progress ≈ 55% by effort. Remaining: finish T1/T2/T4 fix rounds → scoped re-checks; T3/T5 reports → reviews → fixes; T7 integration; e2e ×2; final review; merge/push/cleanup.

### Paths
- Repo main: /Users/wallny/Developer/Skills/alexandria (docs committed at 62522f6; ledger.md edits uncommitted).
- Scratchpad S=/private/tmp/claude-501/-Users-wallny-Developer-Skills-alexandria/1095d7dd-74cb-4300-9a63-e72f22f9f774/scratchpad
  - worktrees: $S/wt/{t1-fidelity,t2-ledger,t3-rewild,t4-gates,t5-alx,t6-docs} (branches wip/<name>, base 361298e, all changes UNCOMMITTED)
  - prompts: $S/prompts/task-N.prompt.txt, task-N-fix1.prompt.txt · reports: $S/reports/task-N-report.md · diffs: $S/reviews/tN.diff · fleet logs: $S/fleet/*.log, *.final.txt
  - fixture (friend's final ledger 25/25 + report): $S/fixture/ · session log: $S/log/
- Briefs: .superpowers/sdd/09-14-01-plan/task-N-brief.md (git-ignored).
- Runtime installed: ~/.alexandria/runtime (install.sh OK). Test cmd: .venv/bin/python -m pytest -q (503 pass baseline + 3 README subtests fixed by T6). Ruff: .venv/bin/python -m ruff check scripts tests.

### Agents (as of handoff)
- Cursor sessions (resume with `cursor-agent … --resume <id>`): T1 bd457f75-9497-4a02-b6ec-d767fbd0e2a8 · T2 217e0178-4ca3-4f58-aa9a-542733fab11b · T4 438c74f6-ace1-4832-9439-16319ef4a414 · T6 79d215ed-ce33-47a3-8c6e-a2cf9a09a9b9.
- Running at handoff: t1-fix1 (Monitor blu7n0f47), t2-fix1 (bu4398i0x), t4-fix1 (bwhck52ja), T3+T5 Workflow wf_e7197c21-43d (opus high; results → journal.jsonl under ~/.claude/projects/-Users-wallny-Developer-Skills-alexandria/1095d7dd-74cb-4300-9a63-e72f22f9f774/subagents/workflows/wf_e7197c21-43d/). Their final replies land in $S/fleet/tN-fix1.final.txt and $S/reports/task-3-report.md / task-5-report.md.
- Review script (reusable): Workflow scriptPath ~/.claude/projects/…/workflows/scripts/alexandria-task-review-wf_b38dedec-e33.js with args {task, brief, report, diff, worktree, model:"opus", effort:"medium"}; T3/T5 (opus-written) reviewers should instead be Cursor `cursor-grok-4.6-medium-fast --mode ask` per roster.

### Next steps (in order)
1. When fix rounds land: verify each with focused tests + greps of the named findings (cheap) instead of full re-reviews; mark Task N complete in Task log.
2. T3/T5: package diff (`cd wt; git add -N .; git status --short; git diff --stat; git diff -U10 > $S/reviews/tN.diff`), dispatch reviewer, one fix round max, then adjudicate.
3. T7 merge order on main: T2 → T1 → T4 → T3 → T5 → T6. For each: `git -C main checkout main; git merge wip/<name>` is NOT possible (uncommitted) → commit inside each worktree first (`git add -A && git commit -m "<task>"` with Co-Authored-By line), then merge serially, run full suite after each; seam fixes by ONE Cursor dispatch (families naming, Finding import fallbacks, claim_support schema, quotation-lost alias collapse in gate-errors.md, alx ↔ T1 record_probe_contexts).
4. Regression: run `alx check` on $S/fixture (copy into a temp workspace via alx init + place files) → grouped output, no traceback.
5. e2e clean path (spec §10): fresh dir; SKILL.md from main; Cursor `cursor-grok-4.6-medium-fast` with the friend's prompt (蒋介石日记, zh-CN); 60-min cap; then Codex gpt-6-astra control. Record timings/gate rounds here.
6. Final judgment review (Cursor xhigh --mode ask, judgment-reviewer.md) on the merged branch; push main; `git worktree remove` each wt; `git branch -d wip/*`; report "Rulings I made" (R1, R2 + debate rulings above) to user.
- T4 fix1 DONE: 535 pass (+3 README = T6). Critical ×2 fixed, Important fixed, minor 14 deferred. Task 4: complete pending integration.
- T1 fix1 DONE: 529 pass; record_probe_contexts present; committed on wip/t1-fidelity 2b9f866. Task 1: complete pending integration (deferred: registrable-domain suffix list).
- T2 fix1 DONE: 529 pass; committed on wip/t2-ledger d5946b8. Task 2: complete pending integration (note: leftover strings coerce to ledger/reference).
- T3 DONE_WITH_CONCERNS (517 pass; 14 new tests): sentence-level clauses lose intra-sentence swap detection (spec-mandated; direction reversals still caught); `--check` prints flat block (render_grouped is T2's); guarded stand-in `Finding` in rewild_gate.py → delete at integration. UNREVIEWED. Committed on wip/t3-rewild.
- T5 DONE_WITH_CONCERNS (549 pass; tests/test_alx.py 46): `ledger remove` not implemented (spec §6.5 has none → fine); in-memory schema shim drops `include_in_report→report_excerpts minItems` and injects §9 additions → remove shim at integration; `claim bind`/`report_paragraph` kept in .alx/state.json (claims are additionalProperties:false); local fallbacks for T1/T2/T3/T4 seams. UNREVIEWED. Committed on wip/t5-alx.
  Rulings: (R3) at integration, evidence-ledger.schema.json drops the `include_in_report → report_excerpts minItems:1` clause; `alx check`(c) enforces excerpt presence for included claims as Class F instead. (R4) `report_paragraph` bindings may live in .alx/state.json (accepted). (R5) `ledger remove` dropped from the plan.
- ALL AGENTS FINISHED at handoff. Nothing running. Every worktree committed on its wip/* branch (t1 2b9f866, t2 d5946b8, t3, t4 195d3fc, t5, t6 333973b). Resume at HANDOFF.md §4 step 3 (T3/T5 reviews) then step 4 (merge).
- 09-14 post-compaction: user GO ("continue until 100% finished"). Reviews T3/T5 dispatched (Cursor grok-medium --mode ask, fleet bzgh0zahi, logs $S/fleet/rev-t3|rev-t5). Merged on main serially: T2 (529 pass), T1 (555), T4 (587); ruff clean after each; 3 README subtests still red until T6.
- Reviews r1 (Cursor grok-medium ask): T3 ❌ (Important ×2: Class A findings carry snapshot-restore fix; semantic fidelity not quote-masked; Minor ×3) sid a11a2a2f-f810-4fc7-828c-af798bb74d63. T5 ❌ (Important ×8: claim add append-only + short-circuit; drop deletions not mechanical → --deliver can't finish; D14 remedy strings; fetch deadline/cache_dir not passed; claim_support disposition not required; --fix date-line; co-mapped drop test; Minor ×5) sid 33c7a0a7-8da9-4f42-b640-261e55699596. All Important accepted; T3 Minor 1+3 FIX, Minor 2 PARKED; T5 minors PARKED.
  Rulings: (R6) D14 closed imperative list gains "extend the report body in report.md" and "delete paragraph <n> of report.md"; remedies never print literal C<n>. Seam work folded into the fix rounds: main (T2+T1+T4) merged into wt/t3-rewild and wt/t5-alx (and t6-docs); T5 removes all T1/T2/T4 stand-ins, keeps one guarded rewild import, applies R3 (schema clause removed; alx check(c) enforces excerpts as Class F); T3 removes stand-in Finding, --check via render_grouped, exports FAMILIES.
  Fix1 dispatched: Workflow wf_57042c42-4c8 (opus high ×2). Prompts $S/prompts/task-{3,5}-fix1.prompt.txt (copies in $D/artifacts/prompts).
- USER (09-14, post-compaction): "do not use codex astra for this. it's only for dabate reviewer" → e2e control run = Claude worker (Workflow opus medium), not Codex astra. Spec §10 amended.
- Fix1 T3/T5 DONE (603/643 pass), verified by greps + focused tests, committed (t3 564aca3, t5 538fe32); merged on main: T3 (603 pass) → T5 (655 pass + 4 alx failures = T3↔T5 seam: alx ignores producer klass; rewild/length + review/rewild are Class A in rewild_gate but default-to-F in alx; note hash checked before stamping). main e458b49.
- T7 seams dispatched: Workflow wf_51c673d0-d45 (opus high) in wt/t6-docs (main merged in, evidence-recording.md conflict resolved keeping T6 text); contract $S/prompts/task-7-seams.prompt.txt (10 items incl. gate-errors.md family alignment, SKILL.md parse test). Parallel: fixture scout wf_46af80c7-d0a (opus low); Cursor dry-run e2e (fleet bbwr5ldoc, snapshot $S/e2e/skill-dry of wt/t6-docs pre-seam, ws $S/e2e/ws-dry-cursor, 65-min kill cap). USER: "let's not refactor for now."
- Fixture regression (scout, main e458b49): PASS on the formal bar (no traceback, grouped output, all 13 printed commands parse) — 74 HARD/3 WARN on the friend's old-workflow ledger (25/25 cache-missing as expected). Defects found in alx check output (would loop a weak model): "Fix: alx check --fix" printed for unfixable findings (date-line placement, circular supports); generic per-family remedies appended on top of producer remedies → duplicates/contradictions (ledger/https, host-conflict), `alx find S1 C1` (claim id as keyword); Class A not visible inside HARD block; claim table incomplete; 7 lines >400 chars. → Task 7b contract $S/prompts/task-7b-check-output.prompt.txt (same worktree, after T7 lands, before the T7 review). Ruling R7: `source set` gains `--family-justification FILE`; family headers print (F)/(A, waivable by --deliver).
- USER (09-15): GLM-5.3-Flash is not a weak model (~42, on par with grok-4.6-medium 43 / opus-low 40); "the problem was in the skill itself" → root cause 7 reworded: skill size vs context, not model intelligence; stop saying "weak model". Acceptance control run → CodeBuddy CLI `--model glm-5.3-flash` (friend's exact model, Tencent harness), optional third run `deepseek-v4.1-flash`; Claude control dropped. (Roster BANNED list applies to workers, not to the system under test.)
- USER (09-15): "finish everything on your part, commit and push, and I can install the latest version of Alexandria into my WorkBuddy… run it directly… export session logs for you to analyze. If it's running okay within 60 minutes, that means we are fine." → CodeBuddy control run DROPPED; field test = user on WorkBuddy (DeepSeek/GLM Flash). Bar before push: T7+T7b reviewed & merged, suite green, judgment review, ONE Cursor grok-medium clean-path run on merged main passing spec §10.
- T7 DONE (673 pass, 0 fail, ruff clean), committed in wt/t6-docs d9e9828; concerns: rewild/* Class A still get snapshot-restore remove (→ T7b addendum, ruling R8: Class A never gets Remove; closed list gains "(edit prose; waivable by alx issue --deliver)"); validate_report report_sha256 exemption per receipt kind (accepted); claim-input named_subjects bare array (accepted); SPEC_FAMILIES aliases live in doc Aliases table (accepted). T7 review dispatched (fleet byffktcxm, Cursor grok-medium ask, diff $S/reviews/t7.diff) IN PARALLEL with T7b (Workflow wf_a85e1c88-d91, opus high, same worktree). Acceptance runner ready: $S/e2e/run-cursor.sh <skill> <ws> <prompt>.
- T7 review (Cursor grok-medium, sid 173eaca9-2b64-4187-a6a5-5bc51be3ef56): ✅ spec on all ten items; Important ×1 (alx check(d)/issue step 2 still isinstance-filter T1 findings → live mismatch never classified); Minor ×5 (accept 1–3). Dry-run (snapshot, 16 min in): 20 sources fetched, ~50 claims, reviews finished, then [content/check] 24 with wrong remedy "review start content --iter"; agent read alx.py 48× + review schemas 24× to learn the note format. → Task 7c contract $S/prompts/task-7c.prompt.txt (after T7b, same worktree; then ONE review of T7b+T7c). Rulings: (R9) closed list gains "add the source link to paragraph <n> of report.md" (mechanical delta per §6.8).
- T7b DONE (686 pass, 13 new tests, fixture confirms all six items), committed wt/t6-docs d85b723; concerns 1 and 5 folded into T7c (items 6–7), 2–4 accepted. T7c dispatched (Workflow wf_0d22d978-f92, opus high). T7b review dispatched in parallel (Cursor grok-medium ask, diff $S/reviews/t7b.diff).
- DRY-RUN e2e (Cursor grok-medium, pre-seam snapshot of wt/t6-docs, sid d43edd36-9661-4c93-ba22-a940ed025372): DELIVERED report.md (8,440 non-ws chars) + report-executive.pdf + report-atlas.pdf in 33 min wall-clock, exit 0, zero tracebacks; 20 sources fetched (several UNREACHABLE: plaintext-http/tls — reported cleanly), ~50 claims via claim add, 30 claim drops. Clean-path bar NOT met: 24 check rounds / 21 review starts (content/check remedy loop → T7c) and `issue` needed --deliver (Class A: "validate_report --fast --final-once failed: Rewild gate receipt does not record ledger_sha256" — rewild receipts have no ledger input; validate_report.py:1274 must exempt the rewild receipt → queued for the T7b/T7c fix round as item 8). Evidence: $D/artifacts/e2e-dry/.
- T7b review (Cursor, sid 6447de14-af5f-40fb-a310-c005a18496c4): ❌ Important ×3 (producer `alx check --fix`/`alx check` remedies leak for non-mechanical families: ledger/coverage, rewild/length; gate-errors mislabels ledger/source-ids and ledger/reference), Minor ×4. Ruling R10: the §6.7 mechanical list is the whitelist for printing `alx check --fix` regardless of producer; bare `alx check` never a remedy. → T7d contract $S/prompts/task-7d.prompt.txt (after T7c; includes the rewild-receipt ledger_sha256 seam as item 8).
- Parallel while T7c runs: dry-run 2 (Cursor grok-medium on `git archive d85b723` = T7+T7b, ws $S/e2e/ws-dry2-cursor) + judgment review pass 1 (Cursor grok-xhigh ask, judgment-reviewer.md, scope 361298e..d85b723; delta pass planned on the final commits). Fleet logs $S/fleet/e2e-dry2.*, judg-1.*.
- JUDGMENT REVIEW pass 1 (Cursor grok-xhigh, sid 0dee68ab-5991-4890-bf20-36454ef5225a, scope 361298e..d85b723): DO NOT SHIP. Critical ×2 (live MISMATCH → exception → Class A → --deliver ships it; bound drops delete the wrong paragraph after sequential drops), Important ×4 (two paragraph splitters → wrong bind N; context-changed invisible in check + refresh loop; --deliver no re-check after live drop; quotation-lost fights claim drop), Minor ×2. ALL ACCEPTED. Split by file ownership: worker A = T7d expanded (alx.py + docs; $S/prompts/task-7d.prompt.txt Part J), worker B = T7e (validate_report split_body_paragraphs + klass A families + rewild-receipt ledger_sha256 exemption; validate_ledger collect_findings cache_meta; source_fidelity record_probe_contexts overwrite; $S/prompts/task-7e.prompt.txt) in a new worktree wt/t7e from the T7c commit. Both launch when T7c lands, in parallel with the T7c review.
- T7c DONE (700 pass), committed wt/t6-docs a310c39; concerns 1–2 → J9/J10 (A) + B6/B7 (B); 3–4 accepted. Worktree wt/t7e (wip/t7e from a310c39) for worker B. Dispatched: Workflow A+B (opus high ×2) + T7c review (Cursor, diff $S/reviews/t7c.diff). Dry-run 2 still running (claims phase at 17:16Z).
- T7c review (Cursor, sid 91e6434f-4ac1-42e8-8add-0ffb7d7dd1f3): ❌ Important ×1 (finish misses empty claim_support note paths), Minor ×3 → $S/prompts/pending-alx-items.txt for A's fix round (A is mid-edit on alx.py).
- USER (09-15): "This seems never-ending… Why can't we just get it done already?" → CUTOFF RULE: after A+B land → ONE combined review → fix ONLY findings that let a fabrication ship or cause a loop; everything else PARKED (list in ledger) → merge → suite → PUSH. Judgment delta pass + official Cursor run AFTER the push, in parallel; findings become follow-up commits. No further review rounds. Pending list $S/prompts/pending-alx-items.txt: P1 fix in the combined round (finish can pass a schema-invalid note = loop risk), P2/P3 PARKED.
- DRY-RUN 2 (Cursor grok-medium on d85b723 = T7+T7b, sid e0ed5752-18de-4dc3-bb22-79e9370a0e65): DELIVERED in 30.5 min (report 8,857 non-ws chars + 2 PDFs), zero alx tracebacks (3 in log = agent's own ad-hoc python); 13 check rounds (was 24), 5 review starts (was 21), 3 drops (was 30). Only Class A abort = rewild-receipt ledger_sha256 (B3) → would have passed clean-path without --deliver. New loop-class item P4 (person_claim_assessment.rationale CJK minimum) queued. Evidence $D/artifacts/e2e-dry2/.
- USER (09-15): "Why the fuck we still need 60 to 90 minutes?" → NO review before push. When A+B land: merge B into t6-docs, merge to main, full suite, PUSH, update installed copies. Delta judgment review + official Cursor run AFTER push in parallel; findings = follow-up commits. Pending P1–P4 = follow-up commit.
- USER (09-15): "No more acceptance run… I would do that myself with WorkBuddy" → official Cursor run CANCELLED. Two dry-runs stand as the evidence (run 1: loop exposed; run 2: fixes confirmed, 2 last items found). Sequence: A+B land → merge → suite → push → refresh installed copies. Judgment delta pass after push only if user wants it.
- A (T7d) DONE 718 pass → committed 4171386; B (T7e) DONE 712 pass → committed 0223d19; B merged into t6-docs; merged into main 8efa698 and PUSHED (origin/main); installed copies refreshed (~/.codex/skills/alexandria, ~/.cursor/skills/alexandria). Merged tree: 727 pass, 3 seam failures (content/check findings filtered by A's obsolete fallback; claim add re-emits context-changed and never rebinds hashes) → T7f fixer (Workflow wf_5edb8d60-ef8, wt/t7f from main). Worktrees t1–t6, t7e removed; wip branches deleted.
- A/B final replies: A concerns confirm the 3 seam causes (CLAIM_ADD_FAMILIES lists context-changed; dead J9 fallback; B's include_ledger_checks=False also dropped binding_findings — T7f keeps the per-claim binding checks per spec §7.4). PARKED: rewild receipt still sees the raw snapshot so a quote inside a dropped paragraph can refuse the rewild receipt (Class A → --deliver still delivers; needs a ruling on source_sha256 vs reviewed hash); --deliver spends a second deadline-capped live pass. B: B5 was already correct (tests added); validate_report._body_paragraphs removed (content_gate updated); alx keeps an offset-aware splitter that agrees with split_body_paragraphs except a standalone date-line paragraph.
- T7f DONE (730 pass) → merged main 4a1825c, PUSHED, installed copies refreshed, all worktrees removed, no wip branches. JOB COMPLETE per plan (acceptance = user field test on WorkBuddy). Parked list: P2 (unbound prefill), P3 (rewild guide test), P4 (person_claim_assessment.rationale schema minLength 40 vs CJK 20 — loop-class, fix in follow-up), A-concern (rewild receipt vs dropped-paragraph quotes → Class A), T5 minors, validate_report --force no-op.
- USER (09-15): "If there are still issues, why don't you fix them already?" → T7g dispatched (Workflow wf_cab25361-0cd, opus high, wt/t7g from main): G1 P4 schema minLength 40→20 (5 sites) + validator CJK/non-CJK thresholds; G2 ruling R11 effective snapshot (.alx/snapshot.effective.md = snapshot minus recorded drop deletions) as rewild source, receipt records snapshot_sha256, note source hash stamped as mechanical delta; G3 P1; G4 P2; G5 P3. Then merge, suite, push, refresh installed copies.
- T7g DONE (738 pass) → merged main 7393706, PUSHED, installs refreshed. USER (09-15): "fix all the remaining issues… third run allowed (confirmation)… always multitask." → in parallel: confirmation run 3 (Cursor grok-medium on `git archive 7393706`, ws $S/e2e/ws-run3-cursor; bar: issue without --deliver, zero rationale findings) + T7h (Workflow, opus medium, wt/t7h: H1–H7 minors incl. accountability_note rule).
- T7h DONE (741 pass) → merged main, PUSHED, installs refreshed, worktree removed. Zero open code items. Confirmation run 3 in progress.
- USER (09-15): README phrase test is nonsense → deleted tests/test_repository_contract.py::test_readmes_explain_the_name_unlimited_scope_and_pdf_output (pushed). READMEs keep only the one added alx/60-min paragraph; user free to reword. CI root cause before the job = that test vs the 361298e README rewrite.
- T7k DONE (all seven P1: K1–K7) → merged main d4569d7, PUSHED, installs refreshed, no worktrees. 773 pass (README phrase test removed). CODE WORK COMPLETE — hard stop. Remaining: CI green on GitHub (Ubuntu unit tests + macOS/Windows PDF jobs), then user field test on WorkBuddy.
- CI: run on ec84a11 → Ubuntu ×3 green, macOS PDFKit green, Windows red at `choco install ghostscript` (Ghostscript 10.8.0 installer access violation; same version installed fine on 09-12 → flaky/runner image). Fix f8580c1: retry ×2 then fall back to 10.5.1 / 10.4.0. Watching run 34887244666.
- CI GREEN on d4569d7 (final code): test 3.10/3.12/3.14, macos-pdfkit, windows-pdf all success (Ghostscript crash was transient). f8580c1 (retry step) run in progress. Repo state: main pushed, installs refreshed, no worktrees, nothing running. User proceeding to WorkBuddy field test.
- USER FIELD TEST (WorkBuddy + GLM-5.3-Flash, ws ~/WorkBuddy/2026-09-15-03-49-54): 11 sources fetched in 2 min; 33 claims written; `claim add` 0 accepted then 1 accepted; model wrote fix_ext.py / check_segs.py (old pattern). Peek on a copy: 64 findings — 37 fidelity/short-segment on VERBATIM extracts (probe splits on quote spans → orphan fragments <8 chars) = BUG → hot1 (literal-whole-first probe; wt/hot1); wrong remedies for person_ids link + month-day quantity → hot2 (wt/hot2). Installed copies to refresh incl. ~/.workbuddy/skills/alexandria.
- USER (09-15): "the rules are too strict… make it resilient… loosen the format rules… your tests are useless (they tested the rule; the rule was wrong)." → Ruling R13: extract LENGTH is not a fabrication check. Only rule: whole extract verbatim in cache, else every author-ellipsis piece verbatim, any length. fidelity/short-segment DELETED; ledger/extract-length → WARN (Class A, whole extract only); warns never block claim add. hot3 (threshold 8/4) STOPPED and discarded; hot4 dispatched (wf, wt/hot4) with spec §7.2.3/§6.4 + gate-errors.md updates. Why tests missed it: tests verified the spec's rule; the rule was the bug; Cursor runs never wrote quote-laden extracts.
- Rulings R14 (month-day extract covers the claim's full date when the year occurs anywhere in that source's cached page/title) and R15 (person_ids auto-derived from names in the claim + WARN; harm rules still hard after linking) → hot5 (wf_614bcfbd-ebd, wt/hot5, tests in NEW tests/test_resilience_rules.py). hot4 (R13) running (wf_b9ef7987-63e, wt/hot4). After both: merge hot4 then hot5 on main (expect test-file overlap only), suite, push, refresh installs incl. ~/.workbuddy, re-run claim add on a fresh copy of $S/peek and record remaining findings.
- hot4 (R13) merged 78bd70a; hot5 (R14/R15) merged b08abe4 (conflict: kept R13 side, dropped dead `_is_verbatim`, spec §6.4 combined). 794 pass, ruff clean, pushed, 3 installs refreshed (incl. ~/.workbuddy). Worktrees removed, wip branches deleted.
- ROOT CAUSE of "Grok runs passed, GLM failed" (same Chinese prompt in run 3 and WorkBuddy): extract STYLE. Grok: 16 extracts, median 105 chars, 2 with quote marks → 27 split pieces, 2 short. GLM: 44 extracts, median 280 (max 1050), 40 with quotes → 297 pieces, 99 under the 8-char floor. Splitter punished the more faithful style; runs 1–3 passed by style luck. No acceptance run had exercised GLM-style extracts.
- USER (09-15 05:0x, verbatim key lines): "WHY you had no issues with your acceptance run, and why the work body run had a ton of issues? … What's the root cause?" / "I'm going to sleep. I want you to keep pushing, keep fixing, do whatever you need to do. Do not ask me for any approval. Burn whatever tokens you need to burn. You are the orchestrator. You have other models to use, just fix it. Just let me and my friends be able to use this tomorrow. Just get it done, fix it, do whatever acceptance test." → STANDING ORDER overnight: no approvals; acceptance runs allowed again; goal = WorkBuddy/GLM run delivers tomorrow. Codex astra still debate-only; no Claude control run of the skill itself.
- Replay of GLM's 33 claims on b08abe4: 21 hard (8 person, 8 reference, 4 quantity, 1 direction) + 1 WARN. Judged: real = C2 '1931年' (extract cut short), C26 '三'; over-strict/buggy = rest. Rulings → hot6 (wf_52c719ce-109, wt/hot6, contract $S/prompts/hot6.prompt.txt, Opus high):
  R20 ledger/person messages self-sufficient: name person + living_status, list allowed roles (neutral|harmful|sensitive_private_fact|response|resolution), got-value, assessment shape {classification, rationale ≥N}; stays HARD; remedy names real claim file.
  R21 year in claim (d:1917) satisfied by bare 4-digit n:1917 in an extract.
  R14b year-month claim (d:1945-08) satisfied by month-day fragment of that month + year on page/extract.
  R22 multiple source_evidence entries per source allowed; duplicate finding deleted.
  R17 ledger/direction C17 false positive (extract contains 增加) → root-cause (suspect window truncation); if lexicon only → WARN when no numeric quantity.
  R23 "counterevidence not tested by adversarial hypothesis" → WARN, correct fix text (was "set field supports").
  R24 verified_at precedes as_of: ≤1 day tolerance (UTC vs local date); beyond stays HARD, fix "set field as_of".
- Scout on WorkBuddy session log (wf_f419524a-eb9, Opus medium, read-only): every non-checker friction point (harness injection, misread doc wording, unactionable messages, time per phase).
- Run 4 (Cursor grok medium-fast, skill snapshot = main b08abe4, friend's prompt + wrapper instruction forcing GLM-style long quoted extracts 200–600 chars): tests report/check/review/render on that style. Run 5 after hot6 = proof run on final code.
- SCOUT RESULT (WorkBuddy log, 133 tool calls, 36 min, 0 delivered): P0 = model ran alx with WorkBuddy's bare python (no jsonschema/weasyprint/pypdf) because `.runtime.json` is written into the SKILL folder (install_runtime.py ROOT) and every install refresh (rm + git archive) wipes it → no manifest anywhere; INSTALL.md never says where it is. P0 = `alx find` single source/keyword → model hand-built extracts via grep/heredocs, then wrote its own checker scripts (17m50s). P1 = Step 3 order (merge coverage before claims → 30 reference errors), no claim-add summary (model `| tail -15`), no behind-schedule escalation, portfolio vocab hidden in forbidden schema file, pip install into shared env. P2 = ledger path wording, fetch singular + && chain, classify at fetch, reads never scheduled, archetype inferred silently, harness memory/skill-edit reminder, compaction drops tool results.
- Dispatched in parallel from main: hot7a (runtime self-relocation + manifest to RUNTIME + status line + jsonschema message; Opus high, wt/hot7a), hot7b (find batch + claim add --dry-run/summary/array + init echo + portfolio vocab + BEHIND SCHEDULE; Opus medium, wt/hot7b), hot8 (SKILL.md/INSTALL.md/gate-errors wording; Opus medium, wt/hot8). hot6 still running. Install-refresh procedure must PRESERVE `.runtime.json` from now on.
- hot7a merged 5bfc42d (relocation moved BEFORE skill imports by orchestrator: macOS /usr/bin/python3 = 3.9 cannot import report_blocks; verified relocation from WorkBuddy python and from 3.9). hot8 merged ca18848 (INSTALL.md conflict → hot8 paragraph). hot7b merged 7ba3382 (array claim files were already accepted; `_fit` per-line cap). SKILL.md find/dry-run promoted to command lines 3eb1bc2. hot6 merged 9db50ca clean: R17 root cause = CJK negation window ignored ASCII `.`/`,` stops (未 32 chars earlier denied 增加) + CJK carrier overlap threshold 0.75 unreachable (13/24 bigrams) → shared 4-char phrase binds; direction stays HARD. R20–R24 as ruled. Residual (parked, P2): online receipt sample still keyed by source_id (one passage per source sampled online; offline check catches a bad second passage).
- FINAL MAIN 9db50ca: 838 pass, ruff clean, pushed. Installs refreshed via $S/refresh-installs.sh (preserves/seeds .runtime.json; manifest generated at ~/.alexandria/runtime/.runtime.json by install_runtime.py --verify-only). Worktrees removed, wip branches deleted.
- Replay of GLM claims on the INSTALLED ~/.workbuddy copy using WorkBuddy's bare python: relocates to managed runtime; 30 submitted, 24 accepted, 6 failed: C1(2) C22(2) C23(2) C25(2) person vocabulary (message now lists roles + shape), C2 1931年 and C26 三 (real). 21 → 10 hard lines.
- Run 4 (b08abe4 snapshot) in progress at ~25 min: 30 claims, report drafted, reviews done, binding. Run 5 launched (Monitor, skill snapshot 9db50ca + manifest, same GLM-style prompt) = proof run.
- RUN 4 DELIVERED (Cursor grok, snapshot b08abe4, friend's prompt + forced GLM-style extracts): 28 min (20:55→21:23Z), 15 sources, 29 claims, extracts median 400 chars, 24/29 with quote marks; check 0 hard → `issue` (no --deliver) → 2 PDFs (220 KB each), report 9,977 body chars, 63 quoted passages. Grok noted the snapshot lacked `.runtime.json` and found the runtime itself (fixed since by hot7a/manifest). Conclusion: the R13–R15 code holds on GLM-style evidence end to end.
- RUN 5 DELIVERED (proof run, snapshot 9db50ca + manifest): 22 min (21:22→21:44Z), 16 sources, 25 claims, extracts median 300 chars, 21/26 quoted; followed new Step 3 order (merge brief+people → find → claims → dry-run → claim add → coverage/synthesis); check 0 hard; `issue --deliver` because two Class A notes: rewild style (15 Chinese semicolons) and content C9 quantity 1936-12-11 flagged at the content gate although the extract itself says 1936年12月 … 12月11日 (content path had no page cache → R14 haystack empty). Fix 3b5371f: source_year_text starts with the claim's own extracts (test fails without it). Re-check of run-5 workspace on fixed code: 0 hard, 26 warn (style only). 839 pass, pushed, installs refreshed (manifest-ok).
- CLOSE-OUT 09-15 ~05:50 local: main 3b5371f; no worktrees; no running jobs. Parked P2: online receipt sample keyed by source_id (one passage per source); `alx claim patch`; history.log after compaction; install.sh message names skill-root manifest only. Next: user restarts WorkBuddy fresh session with the friend's exact prompt; export session log if anything blocks.
- USER (09-15 morning, resumed OLD WorkBuddy chat, verbatim): "what the fuck are these rules? what's the utility of them? C25 … role 字段; C26 … 三 … I fucking told you to get rid of the stupid strict useless formality rules, make it resilient!" → R25: protected-person role/assessment = WARN, default neutral, HARD only when the model itself declares harmful/sensitive_private_fact and omits the review. R26: CJK-numeral quantities (三位…) = WARN; digit figures, percentages, currency, dates stay HARD (fabrication). hot9 dispatched (Opus high, wt/hot9, contract $S/prompts/hot9.prompt.txt). Old chat facts: no .alx/ledger.json ever existed; root ledger has 28/30 claims after the update; its clock started 19:50Z 09-14 → alx says remaining 0 → old chat cannot be a fair run; user told to start a fresh session.
- USER (verbatim): "i want my report done, do i care fucking defamation protection? do i have to care?" → R25 amended: living-person harm rules DELETED entirely (role/assessment/harm review/protected status); unregistered person_id → WARN. R27: ledger/portfolio (source classification) → WARN. R26 unchanged (CJK numerals WARN; digits/dates HARD). hot9 restarted with the amended contract (worktree reset).
- USER (verbatim): "get rid of all these useless, impractical fucking ceremony! … do a full inspection and get rid of all fucking useless but harmful over-strict rules! make it practical for my friends … try to parallelize!" → R28 SEVERITY TABLE ($S/prompts/severity-table.md, binding): HARD = fabrication/evidence integrity only (fidelity mismatch/cache, fidelity/rewild figures, quantity digits+dates, status, direction, derived-hard, claim-input minimal, ledger schema, unfetched source id, link-not-in-ledger, leftover-prose, integrity bytes, quotation altered, runtime). Everything else WARN and never blocks issue/render/claim add; Class A abolished (`--deliver` = alias); reviews optional (>25 min left); snapshot optional; conditional claim fields optional. Workers: sevB (validate_report/content_gate, Opus high, wt/sevB), sevC (rewild_gate/source_fidelity/alx issue-render flow, Opus high, wt/sevC), sevD (spec §7 / gate-errors / SKILL.md 5–9 / references, Opus medium, wt/sevD) dispatched in parallel; sevA (validate_ledger ceremonies + schemas, contract $S/prompts/sevA.prompt.txt) dispatched after hot9 merges (same file).
- USER (verbatim, leaving 2 h): "please fix everything for me, get rid of all fucking stupidity! make it resilient, and non-blocking! … Keep running all audit, fix all issues, test when needed, fix everything!" → autonomous until done; agent cap exceeded with this authorization.
- sevB merged 003018a (report/content gates WARN; quotation altered=HARD/removed=WARN). sevD merged dae193b (spec §7 R28 table, gate-errors F/W, SKILL.md steps 5–9, harm docs deleted; 844 pass).
- BLOCKER AUDIT (2 lenses, read-only) P0s: SKILL.md never defines $WORK/--dir (every command after init fails, error invites re-init); claim-input schema errors print "set field claim in ledger.json"; claim shape/enums undocumented; synthesis/coverage schema demands ~25 hand keys (6 HARD ledger/schema on a fresh workspace); init skeleton writes limitations as string vs array (guaranteed HARD at start); binding/link-not-in-ledger has no Remove remedy → blocks issue AND --deliver forever; Deep-reads section licenses ~17k words of reading after init; Non-negotiables still say Rewild/content gates required. P1s: find prints each hit twice; _next_command ignores deadline; cmd_render unwrapped (traceback, zero PDFs); missing-file message misdiagnoses paths; content note ~115 keys; claim bind one per call; length floor as non-negotiable; zero-slack minute budget; content-quality/rewild-gate/person/rewild SKILL ceremony paragraphs.
- Dispatched: docsE (wt/docsE, Opus medium) for all doc items. Queued: sevA (+addendum: claim-input remedy field, schema relaxation, skeleton validates) after hot9 merges; flowF (skeleton [], --deliver strips foreign links, find single line, deadline-aware next command, render try/except, file-arg message, fixed-form review note, multi claim bind, extend-quote file, gate_severity labels) after sevC merges.
- hot9 merged 520302a (harm rules deleted: ~550 lines + 34 tests; CJK numerals WARN; portfolio/provenance/key-claim WARN). sevC merged a35da21 (rewild/fidelity/tooling WARN; issue never aborts on warnings; --deliver = HARD remedies; class labels gone; render continues past a failing template). docsE merged 35b0c2a (SKILL.md: $WORK + --dir on every command, literal claim example, Deep-reads section deleted, non-negotiables = fabrication only, length a target, reads removed from steps, date-line forms, 10-min slack; references: harm/sourcing-floor/censuses/checker-loop/image-search passages deleted). 3 cross-worker tests restated. 831 pass, pushed; installs refreshed at 35b0c2a (interim).
- Verified on a copy of run-5 workspace with reviews and receipts deleted: `issue` exits 0 (receipts none, warnings recorded) and `render` produces the PDF → reviews are optional in practice.
- Running: sevA (validate_ledger ceremonies WARN, claim-input minimal + remedy field, schema relaxation, skeleton validates), flowF (skeleton [], --deliver strips foreign links, find single line, deadline-aware next command, file-arg message, fixed-form review note, multi claim bind, extend-quote file).
- sevA merged 08966d2; flowF merged 21d8a56 (test conflict → flowF restatement). FINAL MAIN 21d8a56: 840 pass, ruff clean, pushed; installs refreshed (manifest-ok); worktrees/branches removed.
- Acceptance on the installed ~/.workbuddy copy at 21d8a56: GLM's 30 real claims → 29 accepted, 1 failed (C2 1931年 genuinely absent from its extract); fresh `init` → `check` = 0 hard, 4 warn; run-5 workspace with reviews+receipts deleted → `issue` exit 0, `render` produces the PDF. Run 6 (Cursor grok, snapshot 21d8a56 + manifest, GLM-style prompt) launched as proof (Monitor be4n3o9t0).
- Parked (P2, never blocking): render exits 0 even with zero PDFs (a new refusal would violate R28 — instead it prints tooling/render notes); online receipt sample keyed by source_id; history.log after compaction.
- RUN 6 DELIVERED (proof, snapshot 21d8a56): 14 min (04:28→04:42Z), 24 sources, 28 claims, extracts median 300 (19/28 quoted), report 10,916 body chars, 60 quoted passages, check 0 hard, reviews done, `issue` (no --deliver), 2 PDFs. Its one delivery note exposed two content-gate seams, both fixed: 8d496a7 content gate re-checks the ledger WITH the sources cache (R14 sees the page); next commit: content-gate link check uses aliases + normalize_url like the binding check. Re-issue of the run-6 workspace copy: both receipts written, delivery notes empty. Installs refreshed.
- 9f7476f: rendered-report link check also honours aliases + normalize_url. Re-issue of the run-6 workspace copy on the installed 9f7476f: receipts content + rewild written, delivery notes EMPTY. FINAL: main 9f7476f, 844 pass, ruff clean, pushed, installs refreshed (manifest-ok), no worktrees, no running jobs.
- USER (09-15 evening, WorkBuddy field test 02fa7c25, GLM, 蒋介石日记, 54 min, delivered 8-page PDF): "This skill has been updated since yesterday, about 24 hours straight. this really sucks. can you fix it, once and for all?" Log analysed by orchestrator (timeline in scratchpad). Findings: (1) `$ALEXANDRIA_PYTHON` documented as the manifest `command` ARRAY → model used element 0 (micromamba, exit 109), then a multi-word shell variable → every later `$ALX` call failed in zsh; the "claim bind exits non-zero" report is that bug, not alx. (2) evidence-ledger.schema.json still requires person_claim_role/assessment (hot9 deleted the code, not the schema), importance=key → decision_relevance strings, brief/person strict shapes → 40+37+59 HARD ledger/schema over ~15 min. (3) fetch stored `published: '2010-04-16 01:50:56'` → HARD. (4) extract `1948.11.24` scanned as a version → HARD quantity on a true date. (5) `[C17]` inline markers ignored by the binder → 27 HARD excerpt-missing; cited-source list = body links only → model had to paste a link paragraph; `SOURCE_HEADINGS` lacks 资料来源. (6) review finish staged its errors over 3 rounds; excerpt check markdown-sensitive. (7) render #2 failed on non-empty pages dir; companion skipped past deadline. (8) INSTALL.md "back up the existing installation" → `.alexandria-backup-*` sibling loaded by WorkBuddy as the skill. Contract docs/orchestration/09-15-01-fix.md Parts A/B/C → worktrees wt/fixA fixB fixC (wip/fixA…), Workflow opus (A high, B high, C medium). Acceptance: suite green + replay on a copy of the real workspace (scratchpad/replay-src).
- 09-15-01 DELIVERED: fixA/fixB/fixC merged → main 5a9144b, pushed; 870 pass, ruff clean; installs refreshed (~/.codex, ~/.cursor, ~/.workbuddy at 5a9144b, manifest-ok, launcher written to ~/.alexandria/runtime/bin/python by install_runtime --verify-only); stray ~/.workbuddy/skills/.alexandria-backup-* moved to ~/.alexandria/backups/; worktrees and wip branches removed. Orchestrator additions after merge: `_bind_markers` shared by check --fix and snapshot (snapshot never carries a marker the report loses — B's flagged quotation-lost seam); mixed `[C4, C22]` links the known id and keeps `[C22]`; sources-heading predicate tightened to start/end tokens (`参考`/`引用`/`文献` alone matched body sections). Replay on a copy of the real workspace (scratchpad/accept.sh): brief patch 0 hard (was 39); 27 marker citations bound + linked, `## 资料来源` regenerated, check 0 hard / 3 warn (length, two missing reviews); `issue` without --deliver; `render` ×2 both PDFs + contact sheets. Not a skill bug: "claim bind exits non-zero" was the model's unsplit multi-word zsh variable (root cause = the array-manifest doc, now one path). Next: user's own WorkBuddy/GLM-5.3-Flash acceptance run.
- 09-15 WorkBuddy review of the fix (5 residuals) verified: ① find already prints a JSON literal (doc clause added), ② SKILL.md reviews threshold 25→15 (matches alx), ③ wrong (note is in the report body/PDF), ④ P3 skipped, ⑤ real → CI now runs the launcher on both OSes. Windows CI then exposed a real bug: micromamba.exe 2.9.0 aborts (0xC0000409) on every start after its first run without MAMBA_ROOT_PREFIX (creates ~\.mamba, then crashes). Fix: both launchers and alx relocation pin MAMBA_ROOT_PREFIX=RUNTIME/mamba (the installer already did). Also python.cmd written as bytes (text mode made \r\r\n) with `..` normalized. Install CI green on macos-15 + windows-2025 (launcher imports weasyprint). 870 pass. Installs refreshed; local launcher regenerated.
- USER (09-15 evening, WorkBuddy run 2026-09-15-20-23-53, GLM-5.3-Flash, 29 min, delivered 2 PDFs): six friction reports written by the model itself; "verify and fix". Orchestrator read-only analysis of the session log. REAL: (6) `integrity/quotation-lost` HARD on the model's own humanize edits — `validate_report._QUOTE_SPAN_RE` ASCII branch `"[^"]{4,}"`: open == close, so a quoted term under 4 chars (`"雪耻"`) fails the branch and the scanner restarts on its CLOSING quote; 55 of 76 ASCII spans on the real snapshot were the writer's prose. (1) `claim add --dry-run` printed `C2 added` + the live summary line and overwrote `.alx/last-claim-add.txt` → model took 19 rehearsed claims as committed, found out at `check --fix` 8 min later (`ledger merge` had said `references unknown claim` 23× as WARN). (4) `find` literal: no Simplified↔Traditional folding (OpenCC map already vendored). COSMETIC: `[C1, C2]` on one source → `[S3](u) [S3](u)` (×3 in the shipped report). DOCUMENTED, model skimmed: (2) `check --fix`/`snapshot` rewrite report.md (step 4); (5) length floor (step 4). NOT A BUG: (3) fetch enums (argparse lists them; SKILL.md step 2 did not). Extra: malformed patch JSON → 15-line traceback.
- Contract `docs/orchestration/09-15-02-fix.md` Parts A/B/C → worktrees wt/quote (Opus high), wt/claims, wt/find (Opus medium), Workflow wf_ec49fe82-366, 5.4 min, all three green. Orchestrator read every diff itself (small job); no SDD/judgment reviewer run.
- INCIDENT: worker worktrees had `.venv` symlinked to the root venv; `.gitignore` `.venv/` matches directories only → orchestrator's `git add -A` committed the symlink and merging it clobbered the ignored root venv (self-loop). main reset to 0aaf75d, branches amended, venv rebuilt (uv, Python 3.14.6, requirements + ruff 0.14.14 + pytest), `.gitignore` → `.venv` (d52010f).
- Replay on a copy of the real workspace with the model's three humanize edits re-applied exposed the same ASCII parity bug in `rewild_gate._QUOTED_SPAN` (`fidelity/quotation-lost`) → same fix + test (e297532). After it: `check` 0 hard (was 3); `find all 中山舰` hits S7; dry-run prints `DRY RUN, nothing written:` and leaves the transcript untouched; `init --lang zh-CN` prints `target length: 5000–10000`.
- FINAL MAIN e297532 (+ this docs commit): 886 pass, ruff clean, pushed; installs refreshed (~/.workbuddy, ~/.codex, ~/.cursor; `.runtime.json`/`.base` preserved); worktrees/wip branches removed. Next: user's own WorkBuddy/GLM acceptance run.
- USER (09-15 evening, verbatim key lines): "it's been over 24 fucking hours … many obsolete, crazy, fucking useless, over-strict rules that only do damage … make the skill always resilient and flexible, to accommodate many clients … many different models like GLM 5.3 Flash or DeepSeek … fix this for the last time and make sure it's really RESILIENT! NO FUCKING STUPID RULES!" Then: "I don't want you to run any acceptance tests … I will run the real work body test, not you." → R29. STANDING ORDER: no orchestrator acceptance runs (Cursor grok would not find GLM's problems); prove with the suite + replay of the real GLM workspace only.
- Inventory (orchestrator, read-only, whole system): every HARD family vs R28; last run's 133 replay warnings = 91 content-review per-claim bookkeeping + 17 synthesis + 6 reference + 4 triangulation + 9 false context-changed; 13/32 first-batch claim failures, none a fabrication (whitespace, 9月18–20日 → 0918-20, dates on the page outside the pasted window, 第十三年 vs 13周年); refusal paths (issue, render without receipt, snapshot write-once, argparse choices, file paths relative to cwd); SKILL.md ceremony (brief/people/coverage/synthesis merges, two reviews, humanize, per-step minute budgets).
- Contract docs/orchestration/09-15-03-resilience.md Parts A/B/C → wt/gates (Opus high), wt/checkers (Opus high), wt/flow (Opus medium); Workflow wf_dcf779e2-87c, 28 min, all green. Orchestrator wrote SKILL.md (core path: init, fetch, claims, draft, check --fix, issue, render; everything else optional and silent when absent; literal command lines kept for weak models) and restated the doc-contract tests. Merge conflicts resolved by hand: source_fidelity `_document` (A per-probe digests × B whitespace-free normalize → helper normalizes on both branches); validate_ledger quantity block (A Han skip + B page-level coverage); appended tests.
- Orchestrator additions after merge: Han ordinal / year count on the page is evidence (第十三年, 十三周年 cover 13); `edit-prose` remedy reads "(edit prose; warning, never blocks)"; `init` reads --subject/--reader through `_input_path` (C3's pre-check accepted a --dir path that init then opened from cwd → traceback); gate-errors quantity rule/message text.
- Measurements on a copy of the real GLM workspace: `check` 95 → 18 lines, 0 hard, warn 131 → 30 (all from the model's own coverage/synthesis merge, absent in new runs); first-batch dry-run 19 → 31 of 32 accepted (C24 stays: the October date is nowhere on S5); `issue` writes receipts with no refusal; `render` without receipts issues itself and produces both PDFs; `status` never suggests snapshot/review; `source set --type commentary` warns and stores the default; fresh `init` from another cwd with a relative --subject works; untouched skeleton `check` = 0 hard, 1 warn.
- FINAL MAIN 30fc8cd (+ this docs commit): 919 pass, ruff clean, pushed; installs refreshed (~/.workbuddy, ~/.codex, ~/.cursor; .runtime.json preserved, .base = HEAD); worktrees/wip branches removed. Next: the user's own WorkBuddy / GLM-5.3-Flash run.
- USER (09-15 night, verbatim key lines): "24 hours … constant fix, test, fix, test … No more ceremony. No more useless, over-strict rules … do I have to care if a stranger is alive or not? I only care about the quality of the report … make it as light as possible … efficient and non-blocking … super resilient, works with everything … use sub-agents as much as possible, especially Cursor … do not run end-to-end acceptance tests." → R30 (contract docs/orchestration/09-15-04-final.md). Recon: 4 Cursor scouts (read-only). Workers: 3 Cursor grok-medium in worktrees (gates/flow/docs) + 1 seams worker; SDD reviews: Workflow opus medium ×3; orchestrator wrote SKILL.md and the contract.
- R30 delivered (main 9ded9fb, 851 pass, ruff clean): 19 ledger finding families deleted (status, direction, schema, key-claim, provenance, portfolio, triangulation, host-conflict, source-family, source-ids, https, freshness, undated-reason, person, excluded-supports, coverage, synthesis, derived, extract-length + the WARN half of reference); `ledger/quantity` → WARN (numeric scan is a heuristic; the verbatim extract is the fabrication gate); review staleness never a finding; verbatim miss prints the closest cached passage as a paste-able JSON literal + `paste the closest passage … or alx find S<n> KEYWORD`; probe folds Simplified/Traditional; `fetch_document` ignores the deadline and `cmd_fetch` never stops on the clock (a resumed session can fetch); `issue` live re-check opt-in (`--online`), no verification paragraph in report/PDF; `init` never refuses (existing workspace kept, broken one re-initialized, `--subject-status` never required); `ledger merge`/`review finish`/restores exit 0; lenient claim input (aliases extract/quote/text, source/sid, evidence; ints/"s3" ids; auto claim_id; unknown keys dropped; `{claims:[…]}` unwrapped); one status line per `check`; remedies never name `--deliver`; SKILL.md = core path (status, init, fetch, find/claims, draft, check --fix, render; 7 command lines, no archetype/classification/dry-run/snapshot/review/merge); references/READMEs/INSTALL stripped of ceremony, `rewild-gate.md` deleted. Fixture (real GLM workspace): `check` 30 warn → 3, 0 hard; 32/32 claims accepted; wrong extract → closest passage shown; expired clock fetches; `issue` exit 0 no note; both PDFs.
- Parked (cosmetic/dead code, never blocks): closest passage printed in folded form (ASCII punctuation, Simplified) — pastes and matches, but is not the page's exact punctuation; dead remedy branches in alx.py for deleted families; `snapshot --restore` still the Remove for integrity/* (prints "No snapshot", exit 0); rewild/content review code kept but off the path; `gate_severity` dead ledger/schema cap branch.
- USER (09-16, verbatim key lines): "Those removed parts … did you remove them or someone else? … I want all the useful mechanisms recovered … do only recovery, not reinventing … get rid of the laughable rules like living versus dead … you will not change anything that will impact the quality of the report." → R31. Answer given: the 09-15-04 removals were the orchestrator's (this session); yesterday's Deep-reads cut (b19e85e) was also the orchestrator's. Recovery source = d85add1 (+ the Deep reads section from b19e85e^ in SKILL.md). Contract docs/orchestration/09-15-05-restore.md; workers D (docs) and E (code), Cursor grok-medium; a first pair of workers with a reinventing contract was stopped and their worktrees reset.
- R31 delivered (main after d37022b, 938 pass, ruff clean). Recovered verbatim from d85add1: all validate_ledger finding families and their tests, gate-errors sections, review staleness findings, the default live spot-check at issue/render, review-start protocol pointers, references/*.md (incl. rewild-gate.md), READMEs ×4, INSTALL.md, SKILL.md (with the optional editorial block and Deep reads). Kept from 09-15-04 as burden removals (the only differences from d85add1): no refusals (init keeps/re-inits a workspace; --subject-status never required; fetch --id implies refresh; ledger merge skips forbidden keys; review finish/restores exit 0); the clock never decides (fetch never stops, fetch_document ignores the deadline, no RESERVE skip; the 4-min online cap stays); ledger/quantity WARN; closest-passage hint + Simplified/Traditional folding in the probe; lenient claim input; no dry-run step in SKILL.md; one status line per check and the HARD header "fix, or alx issue drops them"; remedies never name --deliver; fidelity/rewild auto-restores at issue; set-field remedies for ledger fields complete to "…then alx ledger merge ledger-patch.json" (bug fix). Deliberately NOT recovered, user informed: the "Verification note" paragraph alx used to insert into report.md/PDF (reader-facing tool text) — restorable on request. SKILL.md edits vs d85add1: `--archetype person` without status; claim add without dry-run and with the closest-passage sentence; editorial block "With ≥ 20 min remaining … in this order" / "Their absence never blocks delivery."; Deep reads section back.
- STANDING ORDER (R31): any step, command, check or document that leaves the prescribed path is named to the user before it goes; recovery from git history, never rewriting, when something has to come back.
- USER (09-16, verbatim key lines): "two parts … the real quality gates … described in the README in both Chinese and English … must have … recover them from git history, not rewrite, not reinvent … second, delete all the fat … useless burdens like the living versus dead verification … update the skill to WorkBuddy so I can do a final acceptance test." → FINAL INSPECTION (Workflow wf_6ddfada2-ce5: 4 auditors — README-EN map, README-ZH map, fat finder, live run — then 11 adversarial verifiers; 15 agents). 78 mechanisms confirmed PRESENT; 59 findings, 29 confirmed, 30 refuted.
- Confirmed and acted on. (1) SKILL.md had lost, in the September compression to alx command lines, the mechanism prose the README promises; recovered verbatim from 41aca26 (2026-07-28, "Harden Alexandria research and PDF pipeline"): non-negotiable standard, framing, archetype choice, depth/deepening rule, research design with parallel sub-agents and disjoint id ranges, freshness rule, ledger-entry field list (kind/confidence/limitations/supports/contradicts + importance: key), pre-draft checks, argument structure, drafting requirements with editorial modes, evidence-safe edit lock list and separate-reviewer rule, manual verification list, page-inspection checklist and fix-and-rerender, delivery criteria; Rewild + both reviews now required steps (333973b non-negotiable), never blocking. (2) README "PDF reopened to check text, links, fonts, pagination, overflow": `alx render` now calls validate_report.validate_pdf per PDF and prints `<template> PDF check:` lines (the July flow ran validate_report --pdf right after md_to_pdf; the standalone command needs a consistent receipt set and is brittle by hand). (3) Five defects fixed (wip/defects): issue prints delivery notes (stdout half of d85add1 recovered, no report paragraph); review finish lists each missing path once; ledger merge remedies name the real claims file; find prints each window once; status ends at "deliver …" after render.
- Refuted (kept as is): 30 findings, incl. every FAT proposal that touched a README mechanism (Deep reads, Rewild profiles, W-tier research rules, classification flags) and unreachable-string defects. Rejected by orchestrator against the verifier: FAT-01 (make fetch_document refuse past the deadline) — contradicts R29/R30 "the clock never decides" and the online phase is already bounded (sample_size × 10 s). Recorded as a ruling, not a bug.
- FINAL MAIN 3443489 + this docs commit: 943 pass, ruff clean, pushed; installs refreshed (~/.workbuddy, ~/.codex, ~/.cursor); full-path replay on the GLM fixture: init (kept) → ledger merge → check --fix → snapshot → review start/finish rewild + content (empty skeletons: warnings only) → issue (notes printed, receipts) → render (2 PDFs, PDF checks printed) → status "Next: deliver …". Burden removed and still removed (the only differences from history): living/deceased requirement, dry-run step, every refusal, clock-decided behaviour, quantity as HARD, `--deliver`/Class A wording, the PDF verification paragraph, 09-15-01/02/03 message fixes. Next: the user's own WorkBuddy / GLM-5.3-Flash acceptance run.
- USER (09-16 00:53, verbatim key lines): "I worry that some useful quality gate, some useful mechanisms were dropped … Any mechanisms, anything related to quality gates that meaningfully control the quality, uphold the value of the report must stay … not compare the README this time, you've already done that … only inspect git history in past three days … if you think they are useful … recover them … massive Cursor CLI … extra high … Grok 4.6 only, let them do the legwork, the cross examinations … you are the arbiter … after this run, let's have a pause … this is job part one … [part two, later] inspect the fat and delete the ridiculous useless rules, for example the living versus dead rules." → R32: history-only recovery pass (baseline 1edb929 = last commit before the September window, 173 commits); README comparison excluded; fat deletion deferred to part two.
- R32 dispatch: contract docs/orchestration/09-16-01-recovery-inspection.md; 15 read-only scouts (Cursor `cursor-grok-4.6-xhigh-fast --mode ask`, one per §A–§O: SKILL.md history, flow map, alx early/late, ledger+schemas, fidelity, content/report, rewild, pdf, references, tests, orchestration records, three per-commit slices); fleet state scratchpad/insp2; cross-verification round follows on the merged findings.
- R32 scouts delivered (15/15 exit 0; 141 raw findings, 219 burden sightings; merged scratchpad/insp3/merged.json). Orchestrator dedupe → 69 mechanisms (+3 orchestrator-found: untrusted-content rule, below-minimum-length rule, August non-negotiables 6–8). Verification: contract §V/§V.1, 10 batches × 3 lenses (presence / quality / recovery), Cursor xhigh ask-mode, two waves of 15 (scratchpad/verify).
- R32 verification delivered (30/30 verifiers exit 0; verdicts scratchpad/verify/verdicts.json). Arbiter: 36 of 69 mechanisms recovered (contract §W, waves 1–2), 33 rejected as user-ruled burden, cosmetic, or already present (orch-01 untrusted-content rule lives at SKILL.md:13 reworded). Workers: recA (alx.py), recB (validate_ledger + schemas), recC (content_gate + source_fidelity), recD (references + bundled Rewild SKILL.md), recE (rewild_gate + gate_severity + bundled checker); SKILL.md recovered by the orchestrator; pdf-06 (PDFKit fallback visibility) after recA merges.
- R32 delivered. Recovered 42 of the 69 verified mechanisms (contract §W; every code recovery keeps R28–R30: WARN, exit 0, no refusal, clock never decides). Code: content/check prints stale-excerpt and missing-citation lines; validate_report --min-sections 3 at issue; PDF text floor 5000 for every language; every unverified live sample prints; fetch 20 s / 3 attempts; claim add warns on omitted kind/importance and unknown fields; missing review is a WARN; status shows snapshot/review state; init echoes archetype/language; evidence-ledger schema required lists and minItems (brief, coverage rows, synthesis and children) as ledger/schema WARNs; analysis-without-reasoning WARN; content-review floors 20; rewild gate: checker reads full prose, quotes visible to direction checks, intra-sentence clause cuts, ASCII mask floor 4, exemption budget in run_check; bundled checker one-hit vocabulary cap; rewild/fidelity WARN lines uncapped; PDFKit failure printed. Docs: SKILL.md August sentences (non-negotiables 6–9, argument chain, drafting requirements, content-quality pointer, post-Rewild length rule, three censuses, compatibility pointer, receipts honesty, receipts-pass criterion); content-quality §13 rubric/censuses/named-human floor, content review no longer optional/clock-gated (that sentence deleted — named here); research-protocol two-exit derived discipline; evidence-recording checklist; cover-image honesty; rewild-gate second pass; bundled Rewild SKILL.md ×3 run the checker with dispositions. Rejected 27 as user-ruled burden (refusals, clock, R13 short segments, R25 harm/people, R26/C26 Han numerals, R11 snapshot/hash), cosmetic, or present (untrusted-content rule at SKILL.md:13). Adaptations named: E3 neutralizes citation-label commas before clause cuts; F keeps the fallback chain and prints instead of refusing; coverage rows require all eight documented fields (research-protocol.md names them).
- FINAL MAIN after this docs commit: 966 pass, ruff clean, pushed; installs refreshed; fixture replay exit 0 at every step (init → merge → claim add → check --fix → snapshot → reviews → issue → render → status). Part two (fat deletion) awaits the user's go.
- USER (09-16, verbatim key lines): "Some rules are hard rules, and some rules can be just warnings or reminders … examine all the warnings … if a mechanism can impact the overall quality by more than five points, it should be blocking … one or two points drop can be just a warning … I want the quality gates that really have a bigger impact to still be blocking." → R33: severity re-examination of all 56 families + 4 flow gates; contract §X; 15 Cursor xhigh raters (5 batches × lenses Q/I/E); orchestrator arbitrates the tier table; then implementation.
- R33 delivered (contract §X, §X.2). Ratings: 18 Cursor xhigh raters × 62 gates (scratchpad/sev/ratings.txt). Three tiers now: HARD-drop (fabrication; `issue` drops or restores, as before), BLOCKED / HARD-refuse (`issue` prints `=== BLOCKED (fix, then alx issue again) ===`, writes nothing, exits 1; `render` too): unsupported Arabic figure or date (`ledger/quantity`), Rewild direction/negation/causal reversal (`fidelity/semantic`), unresolved critical review finding, no snapshot at issue, report under two thirds of the length floor, unrestorable encoding, broken PDF at render (no file or < 500 extractable chars). WARN: everything else; `ledger/direction` (8-point impact) stays WARN because the lexical detector misfires often — first candidate for a better detector. Fixed on the way: `binding/leftover-prose` is now really deleted and `fidelity/cache-detached` really drops its claims. The clock never lifts a block (SKILL.md Clock). Fixture replay: `claim add` prints the C24 date finding, `check` lists it under HARD, `issue` blocks (exit 1, no receipts), `status` says "blocked: 1", `claim drop C24 --apply` then issue → render → deliver.
- FINAL MAIN after this docs commit: 979 pass, ruff clean, pushed; installs refreshed. Part two (fat deletion) still awaits the user's go.
- USER (09-16, verbatim key lines): "Drop the blocking with: a number or date … the humanizer flipping a sentence's meaning … a critical finding from the content review … these go back to just a warning … a reminder to the agent … 'you haven't fixed this one, please fix it'. Skipping the humanizer … a report under two-thirds of the required length … file cannot be read … broken PDF: only the last four are blocking." Also: "it's not your job to delete the fat" (part two closed).
- R33 amended and delivered: BLOCKED = no snapshot at issue, report under two thirds of the length floor, unrestorable encoding, broken PDF at render. `ledger/quantity`, `fidelity/semantic`, `content/critical-finding` = WARN, repeated by `issue` under `=== REMINDERS (not fixed yet; warnings, never block) ===`; docs and pins restated; 979 pass, ruff clean; fixture replay: C24 date printed as a reminder, issue exit 0, render, deliver.
