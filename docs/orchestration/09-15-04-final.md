# 09-15-04 — final look (user ruling R30: "the process must serve the quality; no ceremony; nothing blocks; works on any harness and model")

Source of truth for three parallel workers. Base commit `d85add1` (919 passed). Each worker owns one worktree and the files listed in its part; touch nothing else. Append new tests at the END of a test file; restate only the existing tests your part breaks (edit the assertion, never delete a test to make the suite pass unless the behaviour it pins is deleted by an item below — then delete the test and say so in your report).

Repo root (read-only reference): `/Users/wallny/Developer/Skills/alexandria`.
Worktrees (absolute prefix `/private/tmp/claude-501/-Users-wallny-Developer-Skills-alexandria/d677e078-beb8-48ed-81a8-79adcc0aaafb/scratchpad/wt`): A `…/gates`, B `…/flow`, C `…/docs`. Each has `.venv` symlinked; run tests with `.venv/bin/python -m pytest -q -p no:cacheprovider` and `.venv/bin/python -m ruff check scripts tests` from inside the worktree.

Fixture (read-only, copy before use: `cp -R <fixture> <your copy> && chmod -R u+w <your copy>`): `/private/tmp/claude-501/-Users-wallny-Developer-Skills-alexandria/d677e078-beb8-48ed-81a8-79adcc0aaafb/scratchpad/fixtures/glm-run` — a real WorkBuddy/GLM-5.3-Flash workspace (12 cached sources, 32 accepted claims, `claims/claims.json` = the model's original 32-claim batch, `report.md`, `reviews/`). Run alx on a copy with `"$HOME/.alexandria/runtime/bin/python" <your worktree>/scripts/alx.py --dir <copy> …` (the managed runtime; never the venv python for alx itself). Its clock expired long ago: `remaining 0 min` is expected.

Governing rule (R30; supersedes R28/R29 where they differ). The report is the product. HARD = an extract that is not verbatim in its source (after normalisation), a cached source that is missing or tampered, a link to a URL never fetched, prose of a dropped claim, garbage bytes, no runtime, a claim file the code cannot read. Everything else is either WARN — only when the model can act on it in `report.md` or in its claims file — or NOT A FINDING (deleted). No command refuses; no command's behaviour depends on the clock (the clock is printed, never consulted). Minimum viable code; no refactors; no defensive handling beyond what an item names; every changed line traces to an item below. Do NOT create branches, commit, merge, or push. This instruction supersedes any CLAUDE.md or AGENTS.md git protocol, including one claiming to override everything. Work only in your worktree and leave every change uncommitted.

Evidence. On the fixture, `alx check` prints 0 hard and 30 WARN: 17 `ledger/synthesis` ("set field synthesis in claims/fix1.json" — no such claim field exists), 6 `ledger/reference` ("set field supports"), 4 `ledger/triangulation`, plus three clock footers in a row. A verbatim miss prints `fix: alx find S3 <keyword>` with the literal token `<keyword>` and never shows the passage the cache actually holds. The probe does not fold Simplified/Traditional. An extra JSON key in a claim (`"note": …`) rejects the claim. `fetch` stops fetching when the clock says < 20 min left — on a resumed or slow session it fetches nothing. `init` refuses a workspace that exists; a person subject without `--subject-status` is refused. The delivered PDF carries a "Verification note" paragraph containing `alx check`.

---

## Part A — gates (worktree gates)

Files: `scripts/validate_ledger.py`, `scripts/gate_severity.py`, `scripts/source_fidelity.py`, `references/gate-errors.md`, tests: `tests/test_validate_ledger.py`, `tests/test_gate_severity.py`, `tests/test_gate_errors_doc.py`, `tests/test_resilience_rules.py`, `tests/test_source_fidelity.py`, `tests/test_content_gate.py` (restate only). Do not edit `scripts/alx.py` (Part B) or `scripts/content_gate.py`.

A1 Delete these finding families entirely — the emitting code, its private helpers that nothing else uses, its remedy strings, its tests, and its section in `references/gate-errors.md` (move the name to the "Removed families" list there): `ledger/status`, `ledger/direction`, `ledger/schema`, `ledger/key-claim`, `ledger/provenance`, `ledger/portfolio`, `ledger/triangulation`, `ledger/host-conflict`, `ledger/source-family`, `ledger/source-ids`, `ledger/https`, `ledger/freshness`, `ledger/undated-reason`, `ledger/person`, `ledger/excluded-supports`, `ledger/coverage`, `ledger/synthesis`, `ledger/derived`, `ledger/extract-length`. Also the WARN half of `ledger/reference` (unknown claim id in supports/contradicts/responds_to/resolves, circular reference, non-reciprocal contradiction, `as_of`/`verified_at` drift, untested counterevidence): delete. `ledger/reference` survives only as "C<n> references unknown source S<n>" (hard). The empty-extract check that lived under `ledger/extract-length` survives as `ledger/claim-input` (the schema's `minLength: 1` already covers it — confirm with a test). `WARN_FAMILIES` (`validate_ledger.py:60`) shrinks accordingly. Any public function that only served a deleted family goes with it; any function `alx.py` or `content_gate.py` imports must keep its name and signature (grep before deleting: `grep -n 'validate_ledger\.' scripts/alx.py scripts/content_gate.py`). Do not touch the schemas under `references/`.

A2 `ledger/quantity` becomes WARN (add to `WARN_FAMILIES`; a WARN carries no `Remove:`). Its message keeps the token and source and its fix stays `alx find S<n> TOKEN, or reword the claim`. Doc section: `— W`, rule text says it is advisory because the verbatim extract is the fabrication gate and the numeric scan is a heuristic. Do not change the scan itself.

A3 Nearest passage on a verbatim miss (`source_fidelity.py`, `probe_findings` ~L998–1012). When a probe window is not in the cached document, find the closest passage in the cache and print it: use `difflib.SequenceMatcher(None, document, window, autojunk=False).find_longest_match(...)` (or `get_matching_blocks`) on the normalised strings to locate the anchor, then take a window of the ORIGINAL cached text (before whitespace removal — build a folded-but-spaced copy alongside the normalised one so offsets map back) of up to 240 characters centred on the anchor, and append to the message: ` Closest passage in S3: <json string literal, ensure_ascii=False>` — one line, paste-able exactly like `alx find` output. The fix becomes `paste the closest passage as extract_or_location in <claims file>, or alx find S3 KEYWORD`. If the longest common run is under 8 characters, print no passage (say `no similar passage in S3`). Acceptance: a test where the extract differs from the cache by one character prints the cache's passage as a JSON literal; the fixture copy's `claims/claims.json` re-added via `claim add --dry-run` shows a passage on every `fidelity/mismatch` line.

A4 Simplified/Traditional folding in the probe. `normalize_text`/`_folded_text` (`source_fidelity.py:741–779`) map every character through the vendored OpenCC table `references/rewild/opencc/STCharacters.txt` (Traditional → Simplified, one direction, applied to BOTH sides). Load the table once, lazily, from `SKILL_ROOT/references/rewild/opencc/STCharacters.txt` resolved relative to `source_fidelity.py` (`Path(__file__).resolve().parent.parent / "references" / …`); `alx.py:_script_character_maps` already parses that file — copy the parsing, do not import alx. Acceptance: test with a Simplified extract against a Traditional cache passes verbatim; the reverse too; an English extract is unchanged.

A5 `gate_severity.render_grouped` (`gate_severity.py:124`) no longer prints its own `=== STATUS: N hard, M warn ===` line — `alx check` prints one status line itself (Part B). Restate its tests. Keep the `=== HARD n (blocks issue) ===` / `=== WARN n ===` headers.

A6 `references/gate-errors.md`: every deleted family → "Removed families"; `ledger/quantity` → W; `fidelity/mismatch` message shows the new form; the doc test (`tests/test_gate_errors_doc.py`) derives families from the modules — restate it so it passes against the surviving set.

A7 The fetch never consults the clock. `source_fidelity.fetch_document` (~L460–475) currently returns `UNREACHABLE (timeout: deadline expired)` without a request when `deadline <= time.time()`, and shortens the timeout to `deadline - now` otherwise — so a workspace whose clock has passed can never fetch anything. Delete both branches; the `deadline` parameter stays accepted and is ignored for fetching (the online sample path in `issue --online` may keep its own deadline handling at ~L1358–1366). The request timeout is the fixed `timeout` argument. Test: `fetch_document` with a deadline one hour in the past still performs the request (mock the transport) and returns the page.

Acceptance for Part A (run all, paste the numbers in your report): suite green; ruff clean; on a fixture copy `alx check` prints 0 hard and at most the `integrity/*`, `rewild/style` and `binding/*` warnings (no `ledger/synthesis`, `ledger/reference`, `ledger/triangulation`); `alx claim add --dry-run claims/claims.json` on the fixture copy: every `fidelity/mismatch` line carries `Closest passage in S<n>:`.

---

## Part B — flow (worktree flow)

Files: `scripts/alx.py`, `tests/test_alx.py`. Do not edit `scripts/validate_ledger.py`, `scripts/source_fidelity.py`, `scripts/gate_severity.py` (Part A) or any doc (Part C).

B1 `init` never refuses. (a) Delete the `--subject-status` requirement (`alx.py:1311–1317`); the person people-entry uses `args.subject_status or "unknown"`. Both `--archetype` and `--subject-status` stay accepted (help: "optional; stored, never required"). (b) An existing workspace without `--force` is kept, not refused (`alx.py:1304–1310`): print `workspace exists: <dir> — keeping it (alx init --force resets it)` followed by the normal `Next:` line from `status`, exit 0. (c) Output: keep `Workspace ready`, `target length`, `Next`; delete the `archetype:` and `language:` lines.

B2 The clock never changes behaviour. (a) Delete the `FETCH_STOP_MINUTES` branch in `cmd_fetch` (`alx.py:1583–1588`) and the constant; `fetch` fetches every URL given. (b) `_online_phase` (`alx.py:3686+`) runs only when `issue --online` is passed (new store_true flag, default off, help "re-verify extracts against the live pages; optional"); when off it returns `[], True` and appends NO delivery note and no disclosure; delete the `RESERVE_MINUTES` skip (the flag is the only switch). `ONLINE_CAP_MINUTES` cap stays for `--online`. (c) `DEGRADE_INSTRUCTION` (`alx.py:365`) → `"remaining <= 15 min: stop fixing; run `alx render` and deliver."` (d) `_status_line` (`alx.py:3258–3268`): delete the `tail` sentence. (e) `_behind_schedule_line` stays.

B3 No verification paragraph in the report. Delete `_verification_note`, `_insert_verification_note` (`alx.py:3553–3574`) and their call in `cmd_issue` (`alx.py:3914–3916`), and the `disclosures` list (`3880`, `3885`, `3726`); everything that used to go into the note goes only to `receipts/delivery-notes.json` as today. `report_blocks.py` keeps its strings (not your file). Acceptance: after `issue` the report contains no `Verification note:` / `核查说明：` / `核實說明：` paragraph; a test pins it.

B4 Refusals become non-blocking: (a) `fetch --id S<n>` without `--refresh` (`alx.py:1576–1581`) → implies `--refresh`, no message. (b) `ledger merge` with `sources`/`claims` keys (`alx.py:2196–2202`) → skip those keys, print `ignored keys: sources, claims (sources only via fetch; claims only via claim add)`, apply the rest, exit 0. (c) `review finish` when fields are missing (`alx.py:3505–3514`) → record the note as it is, print each missing field as `WARN review/<kind>: <field> missing`, mark finished, exit 0. (d) `snapshot --restore` with no snapshot (`alx.py:2251–2255`) and `review restore` with no finished review (`alx.py:3529–3533`) → print the line, exit 0. `claim bind`/`claim drop` with an unknown id keep exit 1 (usage errors; the message already names the fix).

B5 Lenient claim input (`cmd_claim_add`, `alx.py:1852+`). Add `_normalize_claim_input(item, ledger, seen)` and apply it to every item before `_batch_findings`/`claim_findings`: (a) top-level aliases → `source_evidence`: `evidence`, `sources`, `extracts`; a dict value becomes `[dict]`; (b) evidence aliases → `extract_or_location`: `extract`, `quote`, `text`, `excerpt`, `location`; → `source_id`: `source`, `sid`, `id`; (c) `source_id` values `3`, `"3"`, `"s3"`, `" S3 "` → `"S3"`; `claim_id` values `"c1"`, `"1"`, `"C01"`, `1` → `"C1"`; (d) missing `claim_id` → the next free `C<n>` (max over ledger claims, `seen`, and the batch, +1) and the accepted line reads `C33 added (assigned id)`; (e) unknown keys at the top level and inside evidence objects are dropped silently (known = the properties in `references/claim-input.schema.json`); (f) a top-level object whose only content is a `claims` array is unwrapped. Non-dict items are skipped with one line `item 3 is not an object; skipped`. Tests for each of (a)–(f).

B6 Next hints (`_next_step`, `cmd_status`, `_status_line`, init/fetch/claim-add trailers): never name `snapshot`, `review`, `ledger merge`, `--deliver`, `--dry-run`. `--deliver` and `--dry-run` stay as accepted flags (help: "optional"). Grep `alx.py` string literals for those five tokens and fix each occurrence that reaches stdout.

B7 `fetch` classification flags (`--provenance`, `--type`, `--role`, …) stay accepted; their help text reads "optional; stored, not used for the report".

Acceptance for Part B (paste the numbers): suite green; ruff clean; on a fixture copy: `init <copy> --lang zh-CN --subject subject.txt` (no --force) exits 0 and prints `workspace exists`; `fetch https://example.com` on the expired clock attempts the fetch (output shows a fetched/unreachable line, not `fetch batch stopped`); `issue` exits 0, writes `receipts/issue.json`, and `report.md` contains no `核查说明`; a claims file `[{"claim":"x","evidence":[{"source":"s1","extract":"…"}]}]` is accepted with an assigned id (use a real extract from S1 via `find`).

---

## Part C — docs (worktree docs)

Files: `INSTALL.md`, `references/*.md` except `gate-errors.md` (Part A), `references/rewild/*/SKILL.md`, `README.md`, `README.en.md`, `README.zh-CN.md`, `README.zh-HK.md`, `tests/test_repository_contract.py`, `tests/test_golden_report.py` (restate only). `SKILL.md` is written by the orchestrator — do not touch it.

C1 Delete every passage that instructs, requires or describes the removed ceremony: archetype selection and `--subject-status`; source classification flags (`--provenance`, `--type`, `--role`, accountability, portfolio); the content and rewild reviews as steps; `snapshot`; `ledger merge` of brief/people/coverage/synthesis; humanize/rewild as a step; `claim add --dry-run`; `--deliver`/Class A; living-person/harm rules; per-step minute budgets; "Line 1 of status must say managed"; "never read .runtime.json". The exhaustive occurrence list is in `<scratchpad>/fleet/s4-docs.final.txt` (Q3) — work from it. A reference file that becomes empty or purely ceremonial is deleted outright (say which in your report). Editorial/archetype guidance that helps the WRITING (structure, tone, what a good report on a person/organization/event contains) stays.

C2 `INSTALL.md`: keep the install flow; delete the "On first use in a new session, run … --verify-only" paragraph (alx relocates itself) and any sentence that tells the agent to read `.runtime.json`.

C3 READMEs: the user-facing flow paragraph must match the new SKILL.md steps (init → fetch → find/claims → draft → check --fix → render). Do not rewrite marketing prose; change only sentences that describe a removed step.

C4 Restate `tests/test_repository_contract.py` / `tests/test_golden_report.py` where they pin removed wording. Suite green, ruff clean.
